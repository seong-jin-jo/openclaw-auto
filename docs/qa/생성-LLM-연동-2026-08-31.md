# 생성실 실제 LLM 연동 증거

STAMP | line: osmu-llm0831 | 생성: 2026-08-31 04:00 KST | model: gpt-codex/gpt-5.6-sol | agent: code-builder | skill: 없음 | 고민: LLM 장애를 템플릿 성공으로 위장하지 않고 사용량 기록도 실패하면 호출 전 중단하도록 닫았다.

## 무엇을 붙였나

후보 A, B, C와 글, 카드뉴스, 영상 파생을 실제 Anthropic Messages API 또는 Claude CLI로 생성한다. 후보 프롬프트는 S0, S1, U2, U3, X4, L5, R6과 요청 시점 채널 규격을 모두 포함한다. 세 후보는 문제 우선, 증거 우선, 절차 우선 각도를 쓰되 제목, 첫 전개, outline이 모두 달라야 저장된다. 영상 파생은 실제 대본과 3개 이상 장면을 만들고 `asset_url`은 `pending:render`로 남겨 렌더링 미제공을 명시한다.

## 실패와 비용 기록

자격증명, 승인, quota, timeout, 제공자 장애, 계약 밖 JSON, 사용량 장부 장애를 서로 다른 API 오류로 반환한다. 템플릿으로 조용히 대체하는 production 경로는 없다. 사용량 장부에 `studioLlmAttempt`를 호출 전에 만들고 성공 또는 실패, member, operation, provider, model, attempt, input/cache/output/total token, USD 비용을 기록한다. 장부를 만들지 못하면 LLM을 호출하지 않는다. 모델 fallback과 재시도는 기본 2회, 최대 3회이며 요청별 timeout 기본값은 90초다. 같은 idempotency key의 후보 및 파생 재요청은 LLM 호출 전에 기존 결과를 반환한다.

## 운영 설정

- 모델: `STUDIO_LLM_MODEL`, `STUDIO_LLM_FALLBACK_MODELS`, 또는 `config/openclaw.json`의 `agents.defaults.model`
- 실행 제한: `STUDIO_LLM_MAX_ATTEMPTS`, `STUDIO_LLM_TIMEOUT_MS`, `STUDIO_LLM_MAX_OUTPUT_TOKENS`
- API 경로: tenant별 Anthropic integration
- 공유 CLI 경로: `CLAUDE_CODE_OAUTH_TOKEN` 또는 읽기 전용 Claude profile mount와 실행 가능한 `claude`
- 비용 표시: 기존 `STUDIO_PREVIEW_COST_*`, `STUDIO_DERIVATION_COST_*`, `STUDIO_COST_CURRENCY`

모델 기본값과 fallback은 `studio-llm.defaults.json`에 있으며 TypeScript 코드에는 모델명을 넣지 않았다. 운영 컨테이너에는 ANTHROPIC, OPENAI 계열 환경변수가 없지만 `CLAUDE_CODE_OAUTH_TOKEN`, `/root/.claude` 읽기 전용 mount, Claude CLI가 있다. 운영 컨테이너 내부 `claude -p` JSON 호출을 직접 실행해 응답과 token usage를 관찰했다.

## 직접 관찰

- 로컬 API: HTTP 201. 후보 A `자동화가 멈췄을 때, 여기 세 곳을 확인하세요`, B `자동화 실행 기록에서 찾는 원인`, C `자동화 실패, 체계적으로 진단하는 법`. 세 후보의 도입과 전개가 달랐다.
- 로컬 화면: 생성 버튼 클릭 뒤 HTTP 201과 서로 다른 후보 3개를 확인했다. [화면 캡처](captures/생성-LLM-연동-2026-08-31/로컬-실제-LLM-후보-3개-1440.png)
- 영상 파생: HTTP 201, KRW 1,200 선견적과 동일 청구, 실제 대본 및 장면 구성 생성. 렌더링은 하지 않았다.
- 사용량: 후보 성공 행과 영상 파생 성공 행에서 실제 model, total token, USD 비용을 확인했다.
- 회귀: Vitest 207파일 1,554건 통과, 조건부 1건 제외. TypeScript 0, Next production build 177/177, design lint 위반 0.

운영 배포와 운영 Studio UI의 후보 및 파생 클릭은 미검증이다. production build에는 기존 NFT 추적 warning 1건이 남아 있다. 구현과 증거는 `7fa3fb55`, `65b6abf6`에 커밋했다. 원격 push는 실행 정책의 승인 요구로 차단돼 미실행이다.

## 셀프심문

1. LLM이 실패하면 사용자는 원인을 한국어 오류로 확인한다. 템플릿 후보는 저장되지 않는다. 회귀 테스트 `LLM-03`이 이를 검증한다.
2. 실제 LLM 응답을 눈으로 확인했다. 로컬 API 본문, 로컬 화면 후보 3개, 영상 파생 draft, 운영 컨테이너 CLI JSON 응답을 각각 관찰했다. 운영 배포 후 Studio UI는 아직 보지 않았으므로 미검증으로 남긴다.

SKILLS_USED: 없음. 설치된 스킬 중 build 코드 구현에 직접 대응하는 스킬이 없다. / SKILLS_SKIPPED: qa는 QA 단계 소유라 사용하지 않았다.

SOURCES: `~/.claude/standards/dev.md` | `dashboard/src/lib/studio/generation/` | `dashboard/tests/studio/generation-fixture.ts` | `CLAUDE.md` | https://docs.anthropic.com/en/docs/claude-code/cli-usage | https://platform.claude.com/docs/en/api/typescript/messages | https://platform.claude.com/docs/en/build-with-claude/structured-outputs

MODEL: gpt-codex/gpt-5.6-sol / code-builder
