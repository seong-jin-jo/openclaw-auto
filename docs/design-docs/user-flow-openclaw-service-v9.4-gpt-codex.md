# OpenClaw Service 유저플로우 v9.4

> 상태: 저충실 설계 개정, 제품구조 §9.6 확정 5건과 §9.7 요청 조립 4층 반영
>
> 작성 범위: 고객·운영자 유저플로우, 화면 목록, 화면별 상태와 인터랙션
>
> 제외 범위: HTML 프로토타입, 코드, API 계약, DB 스키마
>
> 기준일: 2026-08-15

## 0. STAMP

| 항목 | 값 |
|---|---|
| 생성시각 | 2026-08-15 20:22 KST |
| 모델 | gpt-codex/gpt-5.6-sol |
| 에이전트 | product-designer |
| 작업 라인 | openclaw-service |
| 산출물 | user-flow-openclaw-service-v9.4-gpt-codex.md |
| 상류 정본 | user-flow v9.3, OpenClaw PRD v8.2.1, 제품구조 결정 2026-08-15 §9.6·§9.7, Studio PRD v1.2.1, 레드팀 제품구상, DESIGN.md 현행, prototype v24, two-service-boundary |
| 근거 URL | Shape of AI 비용 견적 패턴, Nielsen Norman Group 의사결정·점진적 공개, Adobe Firefly 생성 크레딧, GenUI 저충실·폭넓은 탐색 연구, AI 온보딩 사례 조사 |
| 고민 한 줄 | v9.3의 모든 기능을 보존하면서 타깃 시장부터 시작하는 최대 3질문 흐름과 4층 요청 조립을 넣고, 저해상도 3안에서 선택한 1안만 고해상도로 만드는 비용 통제 흐름을 발행·성과·운영까지 닫았다. |

## 1. 한 줄 결론

기존 v24의 셸, Studio 7채널 미리보기, 다계정 데이터 구조, 채널별 capability 탭과 현재 운영자 콘솔을 보존하면서, 선택 피로를 줄이는 제작 흐름과 1인 운영자가 장애·과금·고객 막힘·지원 요청을 먼저 발견해 복구하는 운영 흐름을 하나의 막힘 없는 체계로 연결한다. v9.4는 여기에 타깃 시장과 출력 언어 선입력, 요청 조립 4층, 저해상도 후보 3개와 선택본 단일 고해상도 렌더, 승인 후 발행 기본값, 다국어 품질 피드백, 6개 운영 예외 중심 홈을 추가한다.

v9.4의 35장은 제품구조 결정서 §9.6의 회장 확정 5건과 §9.7 요청 조립 4층을 최종값으로 사용한다. 앞선 장의 `결정 대기`, `후보`, `목표 UX` 또는 후보 수·해상도·발행 기본값이 35장과 충돌하면 35장이 우선한다. API 계약, DB 스키마, 보관 기간, 체험 크레딧 수량과 만료값은 이 문서에서 확정하지 않는다.

## 2. 목차

1. 설계 범위와 정본
2. 벤치마크에서 차용하고 바꾼 것
3. 기존 v24 기능 대조표
4. 흐름 원칙과 관심사 경계
5. 전체 화면 목록
6. 공통 화면 계약
7. 제작 흐름
8. 편집 흐름
9. 채널 연결과 계정 전환 흐름
10. 탭 일관 규칙
11. 발행과 예약 흐름
12. 성과 흐름
13. SNS 무연결 제작 체험 흐름
14. 반응형, 밀도, overflow 계약
15. 상태와 복구 계약
16. 추적성 및 dead-end 감사
17. 회장 결정 필요
18. 레드팀과 셀프심문
19. 품질 판정과 남은 gap
20. v9.1 운영자 화면 원칙과 정보구조
21. v9.1 운영자 화면 목록
22. v9.1 운영자 핵심 흐름과 상태
23. v9.1 질문 엔진 화법 준수 감사
24. v9.1 벤치마크 근거와 설계 반영
25. v9.1 반응형·밀도·overflow 계약
26. v9.1 추적성·dead-end 재감사
27. v9.1 레드팀·셀프심문·품질 판정
28. v9.2 공식 벤치마크 근거 보강
29. v9.3 확정 정책과 전체 화면·플로우 보강
30. v9.4 시장·요청 조립·3후보·다국어 품질·운영 예외 보강

## 3. 설계 범위와 정본

### 3.1 이번 문서가 정하는 것

- 사용자가 어떤 화면을 어떤 순서로 만나는지
- 각 화면에서 보는 정보, 선택, 추천, 근거, 다음 행동
- happy, edge, empty, error, loading 상태와 복구 경로
- 390과 1024 뷰포트에서의 주축, 열수, 정보 우선순위
- 데이터량 0, 정상, 과다에서의 표시 규칙
- 기존 기능을 유지하면서 v9에서 추가할 UX 차이

### 3.2 이번 문서가 정하지 않는 것

- OAuth API 파라미터와 콜백 저장 방식
- 크레딧 원장 및 환불 트랜잭션
- 채널 정책 판정 API
- 성과 데이터의 통계적 인과 모델
- 실제 숫자 견적을 만드는 Studio 계약
- 프로토타입 픽셀과 최종 카피

위 항목은 이 흐름을 승인한 뒤 기술설계에서 확정한다. 이 문서에 표시된 데이터 필드는 사용자에게 필요한 정보 계약이며 API나 DB를 미리 확정하는 선언이 아니다.

### 3.3 진실원 우선순위

1. `docs/제품구조-결정-2026-08-15.md`의 3.7, 3.8, 3.85, 3.9
2. `docs/prd-openclaw-service-v8.1.1-gpt-codex.md`
3. 현재 코드와 `docs/구현현황.md`
4. `DESIGN.md` v24와 `docs/prototype/` v24
5. 기존 `docs/user-flow.md`
6. 관련 wiki와 공식 외부 벤치마크

정본 간 표현이 다르면 최신 제품구조 결정을 따른다. 특히 창작 텍스트는 Studio, 채널 자격증명과 발행은 OpenClaw가 맡는 3.85와 3.9 경계를 따른다.

## 4. 벤치마크에서 차용하고 바꾼 것

| 공식 사례 | 확인한 사실 | 차용 | OpenClaw에서 변경 또는 차별화 |
|---|---|---|---|
| [Buffer 채널 연결](https://support.buffer.com/article/564-connecting-your-channels-to-buffer) | 같은 네트워크의 복수 계정을 연결할 수 있지만, 제공자에 이미 로그인한 계정이 선택될 수 있어 로그아웃이나 비공개 창을 안내한다. | 연결 전에 현재 브라우저 세션을 확인시키고 잘못 연결된 계정을 복구하는 명시적 경로 | 도움말에만 의존하지 않는다. OAuth 복귀 뒤 실제 계정 식별자를 확인하고, 사용자가 승인하기 전 기존 기본 계정을 바꾸지 않는다. 제공자 쿠키를 서비스가 지울 수 있다고 약속하지 않는다. |
| [Later 연결 상태](https://help.later.com/hc/en-us/articles/1500003115942-Social-Profile-Connection-Statuses) | 연결됨, 만료됨, 권한 활성화 필요처럼 상태와 복구 행동을 구분한다. | 채널 상태와 복구 CTA를 한 화면에서 분리 | 상태 센터뿐 아니라 발행 직전과 실패 결과 화면에서도 같은 상태 이름과 같은 복구 행동을 재사용한다. D-08의 노출 범위는 회장 결정 전이다. |
| [Later 계정 추가와 제거](https://help.later.com/hc/en-us/articles/360043244733-Adding-Removing-Transferring-Social-Profiles-in-Later) | 브라우저에 저장된 잘못된 계정과 비공개 브라우징 우회가 실제 운영 문제다. | 계정 추가, 재연결, 제거를 서로 다른 의도로 표현 | `다른 계정 연결`을 기존 계정 교체로 오해하지 않게 한다. 삭제 전 영향을 받는 예약과 초안을 알려준다. |
| [Google OpenID Connect](https://developers.google.com/identity/openid-connect/openid-connect) | `prompt=select_account`로 계정 선택을 요청할 수 있다. | 제공자가 공식 지원할 때 계정 선택기를 우선 사용 | 모든 SNS가 같은 선택 기능을 지원한다고 일반화하지 않는다. Google 계열에만 공식 선택기를 쓰고 나머지는 외부 세션 점검과 복귀 확인을 사용한다. |
| [Buffer 다채널 예약](https://support.buffer.com/article/644-how-do-i-schedule-posts-for-multiple-social-channels-at-the-same-time) | 여러 채널을 한 작성 흐름에서 선택하고 채널별 콘텐츠를 조정한다. | 공통 작성물에서 채널별 변형으로 내려가는 구조 | 공통 작성물을 `마스터`로 명시하고, 파생물이 마스터를 덮어쓰지 않게 한다. 캡션과 해시태그의 공통 기본값 여부는 D-10으로 남긴다. |
| [Vrew 텍스트로 비디오 만들기](https://vrew.ai/ko/feature/text-to-video/) | 주제에서 대본, 이미지, 음성, 스타일을 만들고 후속 편집으로 이어진다. | 생성과 편집의 연속성 | 빈 프롬프트부터 시작하지 않는다. 업계, 목적, 공개 사례, 스타일, 비용, 시간을 선택지로 먼저 보여준다. |
| [Markety](https://markety.co.kr/) | 국내 서비스가 템플릿 기반 자동 생성과 발행을 한 흐름으로 제시한다. | 제작과 발행의 연결 기대 | 연결하지 않고 제작만 시험하는 길을 먼저 보장하고, 연결은 결과 확인 뒤에도 가능하게 둔다. |
| [Sigmine](https://sigmine.ai/) | 국내 서비스가 생성, 발행, 다음 전략을 연결한다. | 성과 이후 다음 추천으로 이어지는 순환 | 성과를 선택의 결과와 연결하되 인과로 단정하지 않는다. 표본, 기간, 외부 변수를 함께 표시한다. |

벤치마크 해석의 한계: 제공자별 OAuth 동작과 상품 기능은 바뀔 수 있다. 구현 전 공식 제공자 문서와 실제 샌드박스에서 다시 확인해야 한다.

## 5. 기존 v24 기능 대조표

### 5.1 기존 구현 확인

| 영역 | v24 또는 현재 구현에서 관찰된 것 | v9 처리 |
|---|---|---|
| 앱 셸 | 1024에서 224 너비 사이드바와 메인, 390에서 상단 메뉴와 단일 열 | 유지 |
| Studio | 입력, 가이드, 생성, 저장, 발행, 예약, 드래프트 복원 | 유지하고 8단계 선택 흐름을 앞단에 추가 |
| 미리보기 | Instagram, Threads, YouTube, TikTok, X, Facebook, LinkedIn 7채널 미리보기 | 유지하고 마스터와 파생물 관계를 명시 |
| 발행 계정 | 플랫폼에 복수 계정이 있으면 정확한 계정 선택 | 유지 |
| 계정 관리 | `channel_accounts` 기반 복수 계정, 기본 계정 설정, 삭제, 토큰 비노출 | 유지하고 연결 전 의도와 콜백 뒤 계정 확인을 추가 |
| 연결 동작 | Google 계열 계정 선택 요청, Meta 등은 외부 로그인 세션 안내 | 제공자 차이를 숨기지 않고 공통 단계 이름으로 정리 |
| 채널 탭 | Queue, Analytics, Growth, Popular, Settings 공통 축, 일부 Editor 특수 탭, capability에 따른 숨김과 비활성 | 현재 순서와 capability 원칙을 정식 UX 규칙으로 승격 |
| Home 성과 | 핵심 지표, 최근 게시물, 다음 아이디어 | 선택과 결과의 연결 화면을 추가하되 기존 Home 블록 유지 |
| 상태 | 연결됨, 미연결, 준비 중, 발행 대기, 오류 및 제작 상태 | 같은 상태어를 전 화면에서 재사용 |
| v24 검수 | 23개 라우트, 390/1024 캡처, 62개 점검과 overflow 0 기록 | 기존 근거로만 사용. v9는 HTML을 만들지 않아 렌더 미검증 |

### 5.2 v9에서 추가하는 것

| 신규 설계 | 기존을 지우지 않는 접합 방식 |
|---|---|
| 업계와 목적부터 결과까지 8단계 제작 | 기존 Studio 진입 뒤 `새로 만들기`의 앞단 오케스트레이션으로 추가 |
| 성공 채널과 영상 사례, 스타일별 비용과 시간 | 기존 가이드 패널을 선택형 사례 보드로 확장 |
| A/B/C 강제 선택 | 생성 직전 하나의 방향을 명시적으로 승인하는 관문으로 추가 |
| 원본 보존 편집과 추가 크레딧 0 표시 | 기존 결과와 히스토리 위에 새 revision을 쌓음 |
| 계정 오연결 방지 | 기존 다계정 저장과 기본 계정 기능 앞에 의도 확인, 복귀 확인, 불일치 보류를 추가 |
| 마스터와 채널 파생 검수 | 기존 7채널 미리보기를 마스터에서 파생된 검수 화면으로 연결 |
| 선택과 성과의 연결 | 기존 Home 성과에서 선택 이력 상세로 drill-down |
| SNS 무연결 체험 | 기존 제작 기능은 사용하되 발행 단계만 연결 CTA로 대체 |

### 5.3 유지한 기존 기능

- 사이드바와 모바일 메뉴의 정보구조
- Studio의 초안 저장과 복원
- 7채널 시각 미리보기
- 복수 계정 선택과 정확한 발행 대상
- 예약과 발행 이력
- 채널별 capability 기반 탭
- Home 성과 요약과 최근 게시물
- 토큰을 DOM에 노출하지 않는 보안 경계

### 5.4 현재와 정합하지 않아 확장으로만 표기한 것

- 8단계 질문 엔진과 사례 데이터는 현재 구현되지 않았다.
- Studio 견적 API와 실제 시간 범위의 진실원은 정의되지 않았다.
- OAuth 복귀 뒤 계정을 확인만 하고 보류하는 상태는 현재 UI와 기술 계약이 필요하다.
- 선택 이력과 성과를 연결하는 분석 데이터는 현재 구현 확인이 없다.
- 실제 운영 OAuth, 토큰 갱신, 외부 발행 성공은 현 문서만으로 검증되지 않았다.

## 6. 흐름 원칙과 관심사 경계

### 6.1 선택 피로 감소 규칙

모든 질문 화면은 다음 순서를 지킨다.

1. 질문의 답 후보를 먼저 보여준다.
2. 추천 후보에 `추천` 배지와 짧은 이유를 붙인다.
3. 근거는 `우리 실험 로그`, `공개 사례`, `가설` 중 하나로 표시한다.
4. 추천과 사용자의 기존 선택이 충돌하면 조용히 덮지 않고 충돌 이유를 말한다.
5. 자유 입력은 `직접 입력` 보조 행동으로만 둔다.
6. 선택하지 않으면 다음으로 갈 수 없는 단계는 이유와 선택 수를 표시한다.
7. 선택 뒤에는 언제든 이전 단계로 돌아갈 수 있고, 바뀌는 견적과 파생물을 미리 경고한다.

근거: Hick의 법칙에 따라 한 화면의 주요 선택 수를 제한하고, Nielsen의 인지보다 재인을 우선하며, Fogg 모델에 따라 능력이 낮은 첫 사용자는 한 번에 하나의 명확한 행동만 요구한다.

### 6.2 생성과 편집의 두 모드

| 구분 | 생성 모드 | 편집 모드 |
|---|---|---|
| 입력 | 업계, 목적, 사례, 스타일, 제작 방식, A/B/C | 현재 결과물과 수정 지시 |
| 핵심 신호 | A/B/C 중 강제 선택 | 자연어 수정 지시와 수정 범위 확인 |
| 결과 | 새 마스터 v1 | 원본을 보존한 새 revision |
| 크레딧 | 생성 전 범위 표시 | 자막, 컷, 오버레이 등 허용 범위는 `추가 크레딧 0` 표시 |
| 되돌리기 | 이전 선택으로 돌아가 재견적 | 이전 revision으로 언제든 복원 |

`추가 크레딧 0`은 서버 렌더 비용이 없다는 뜻이 아니다. 사용자 과금만 추가하지 않는다는 뜻으로 제한한다.

### 6.3 관심사 분리 3기준

| 질문 | 소유 화면과 서비스 |
|---|---|
| 채널 자격증명이 필요한가 | OpenClaw의 연결, 계정, 예약, 발행 화면 |
| 창작 정보를 읽거나 쓰는가 | Studio의 질문, 생성, 편집, 캡션, 해시태그 화면 |
| 미디어 바이트를 만지는가 | Studio의 생성, 변환, 편집, 파생물 화면 |

OpenClaw는 채널 제한과 계정, 시각, 발행 상태를 Studio 요청에 제약으로 전달한다. Studio는 플랫폼 계정과 토큰을 알지 않는다. Studio가 만든 마스터와 창작 텍스트를 OpenClaw가 받아 정확한 계정으로 발행한다.

### 6.4 마스터와 채널 파생 원칙

- 결과물 하나를 `마스터`로 고정한다.
- 비율, 길이, 안전영역, 캡션, 해시태그는 채널 파생으로 저장한다.
- 파생물 편집은 마스터를 덮어쓰지 않는다.
- 마스터를 바꾸면 영향받는 파생물 수를 먼저 보여주고 재생성 여부를 묻는다.
- 기존 소재 재사용 범위의 파생은 추가 크레딧 0 후보지만, 소재 재생성은 별도 견적이다.
- 캡션과 해시태그는 Studio가 만들고, OpenClaw는 채널 한도와 발행 가능 여부를 전달한다.

## 7. 전체 화면 목록

### 7.1 진입과 제작

| ID | 화면 | 목적 | 다음 화면 |
|---|---|---|---|
| G-00 | 시작 방식 선택 | SNS 연결 여부와 무관하게 제작부터 시작 | C-01 또는 A-01 |
| C-01 | 업계와 목적 선택 | 제작 맥락을 선택지로 수집 | C-02 |
| C-02 | 잘 되는 채널과 영상 사례 | 공개 사례와 우리 근거를 비교 | C-03 |
| C-03 | 스타일과 제작 방식 비교 | 예시마다 비용 범위와 시간 범위를 비교 | C-04 |
| C-04 | 추천 확정 | 직접 고르거나 근거 있는 추천을 수락 | C-05 |
| C-05 | A/B/C 후보 | 세 방향을 비교하고 하나를 강제 선택 | C-06 |
| C-06 | 생성 전 확인 | 선택, 예상 비용, 시간, 결과 범위를 최종 확인 | C-07 |
| C-07 | 생성 진행 | 단계별 진행, 취소, 재개 상태 제공 | C-08 |
| C-08 | 마스터 결과 | 결과 확인, 편집, 파생, 연결, 저장 진입 | E-01, P-01, A-01 |

### 7.2 편집

| ID | 화면 | 목적 | 다음 화면 |
|---|---|---|---|
| E-01 | 수정 지시 | `자막 키워줘`, `이 컷 짧게`처럼 현재 결과에 지시 | E-02 |
| E-02 | 수정 영향 확인 | 바뀌는 범위, 추가 크레딧, 처리시간 확인 | E-03 |
| E-03 | 원본과 수정본 비교 | revision 비교, 채택, 다시 수정, 원본 복원 | C-08 또는 E-01 |

### 7.3 채널 연결과 계정 전환

| ID | 화면 | 목적 | 다음 화면 |
|---|---|---|---|
| A-01 | 채널 상태 센터 | 채널, 계정, 토큰, 권한 상태를 한곳에서 확인 | A-02, A-07, A-08 |
| A-02 | 연결 의도 선택 | 기존 계정 사용, 다른 계정 추가, 재연결을 분리 | A-03 또는 A-04 |
| A-03 | 외부 세션 사전 점검 | 잘못 로그인된 제공자 세션을 연결 전에 확인 | A-04 |
| A-04 | 제공자 인증 이동 | 공식 OAuth에서 계정과 권한 승인 | A-05 또는 A-08 |
| A-05 | 돌아온 계정 확인 | 실제 계정명과 식별자를 사용자가 최종 확인 | A-07 또는 A-06 |
| A-06 | 다른 계정이 돌아옴 | 후보 연결을 보류하고 기존 계정을 보호 | A-03, A-04 또는 A-01 |
| A-07 | 연결 계정 관리 | 기본 계정, 상태, 예약 영향, 삭제 관리 | A-01 또는 P-03 |
| A-08 | 연결 복구 | 토큰 만료, 권한 부족, 연결 오류 복구 | A-04, A-01 또는 이전 화면 |

### 7.4 발행과 예약

| ID | 화면 | 목적 | 다음 화면 |
|---|---|---|---|
| P-01 | 마스터와 파생물 검수 | 7채널 파생물의 비율, 길이, 안전영역 확인 | P-02 |
| P-02 | 채널별 텍스트 검수 | 캡션, 해시태그, 첫 댓글 후보 확인 | P-03 |
| P-03 | 계정과 정책 사전 점검 | 정확한 계정, 토큰, 정책, 한도 확인 | P-04 또는 P-05 |
| P-04 | 예약 시각 선택 | 시간대와 채널별 발행 시각 확인 | P-05 |
| P-05 | 발행 진행 | 채널별 독립 진행과 취소 가능 범위 표시 | P-06 |
| P-06 | 발행 결과 조정 | 성공, 실패, 정책 거부를 채널별로 복구 | R-01, P-03 또는 A-08 |

### 7.5 성과

| ID | 화면 | 목적 | 다음 화면 |
|---|---|---|---|
| R-01 | 성과 개요 | 기간과 채널별 결과를 비교 | R-02 |
| R-02 | 선택과 결과 연결 | 어떤 선택 뒤 어떤 성과가 관찰됐는지 확인 | R-03 또는 C-01 |
| R-03 | 다음 실험 추천 | 근거, 표본, 한계가 있는 다음 선택 제안 | C-04 또는 종료 |

### 7.6 SNS 무연결 체험

| ID | 화면 | 목적 | 다음 화면 |
|---|---|---|---|
| T-01 | 무연결 체험 안내 | 제작 가능 범위와 발행 제한을 알림 | C-01 |
| T-02 | 무연결 결과 | 결과와 편집을 사용하고 연결은 선택으로 남김 | E-01, A-01 또는 종료 |
| T-03 | 나중에 연결 | 결과를 보존한 채 연결을 시작하거나 계속 미룸 | A-01 또는 T-02 |

## 8. 공통 화면 계약

### 8.1 선택형 질문 화면

모든 C-01부터 C-05 화면은 다음 슬롯을 같은 순서로 쓴다.

1. 현재 단계와 전체 8단계 중 위치
2. 답을 먼저 제시하는 한 문장
3. 3개에서 5개의 선택 카드
4. 추천 카드의 이유와 근거 유형
5. 선택 충돌 안내
6. `직접 입력` 보조 행동
7. 이전, 다음 CTA

선택 카드에는 라벨, 한 줄 결과, 추천 여부, 근거, 비용 영향, 시간 영향 중 해당 값을 담는다. 값이 아직 계산되지 않았으면 임의 숫자를 쓰지 않고 `견적 계산 중`으로 표시하며 선택과 다음 CTA를 잠시 비활성화한다.

### 8.2 공통 질문 화법 예시

| 잘못된 방식 | v9 방식 |
|---|---|
| 무엇을 만들까요? | 첫 방문 전환이 목적이면 `제품 사용 장면`, `고객 전후 비교`, `창업자 설명` 중 제품 사용 장면을 추천합니다. 공개 사례에서 첫 3초에 제품이 등장하는 구성이 반복됐기 때문입니다. |
| 스타일을 입력하세요. | 빠른 검증에는 `실사 혼합형`을 추천합니다. 현재 소재를 재사용해 순수 생성형보다 비용 변동을 줄일 수 있습니다. 각 카드에서 예상 비용과 시간을 확인하세요. |
| 알아서 추천해줘 | 추천 1은 도달 우선, 추천 2는 전환 우선, 추천 3은 제작비 우선입니다. 무엇을 가장 지키고 싶은지 하나를 골라주세요. |

### 8.3 공통 상태

| 상태 | 표시 | 허용 행동 | 금지 |
|---|---|---|---|
| loading | 현재 처리 단계, 남은 단계, 안전한 이탈 여부 | 백그라운드로 보내기, 가능한 경우 취소 | 무한 스피너 |
| empty | 왜 비었는지, 첫 행동 하나 | 제작 시작, 계정 연결, 기간 변경 | 빈 표만 노출 |
| error | 무엇이 실패했는지, 보존된 것, 다음 행동 | 재시도, 복구, 저장 후 나가기 | `오류가 발생했습니다`만 노출 |
| disabled | 비활성 이유와 열리는 조건 | 조건 해결 화면으로 이동 | 설명 없는 회색 버튼 |
| excess | 요약, 필터, 검색, 내부 스크롤 | 더 보기, 선택 비교 | 페이지 가로 넘침 |

## 9. 제작 흐름

### 9.1 Happy 경로

`G-00 제작부터 시작` -> `C-01 업계와 목적` -> `C-02 잘 되는 채널과 영상 사례` -> `C-03 스타일, 비용, 시간` -> `C-04 직접 선택 또는 추천 수락` -> `C-05 A/B/C 강제 선택` -> `C-06 생성 전 확인` -> `C-07 생성` -> `C-08 마스터 결과`

| 화면 | 사용자가 보는 것 | 선택과 인터랙션 | 이탈과 복구 |
|---|---|---|---|
| C-01 | 최근 작업이 있으면 업계 후보, 목적 후보, 추천 조합과 근거 | 업계 하나, 목적 하나를 선택. 직접 입력은 보조 | 이전 G-00. 임시 선택 자동 저장 |
| C-02 | 해당 업계에서 잘 되는 채널 3개 이내와 영상 사례 3개에서 6개. 출처, 날짜, 관찰 포인트 | 사례 1개 이상 선택 또는 `추천 조합 사용` | 사례를 열어도 현재 선택 유지. 출처 없는 사례는 추천 근거로 사용 금지 |
| C-03 | 스타일 카드마다 실제 미리보기, 예상 크레딧 범위, 현금 환산 범위, 소요시간 범위, 포함과 제외 | 스타일과 제작 방식 하나 선택 | Studio 견적이 없으면 선택 불가. 새로 계산 또는 다른 방식 선택 |
| C-04 | 도달, 전환, 비용 우선의 복수 추천과 이유 | 직접 고르기 또는 추천 하나 수락 | 이전 선택과 충돌 시 영향 비교 뒤 확인 |
| C-05 | 같은 목적의 A/B/C 방향. 훅, 톤, 첫 장면, 비용과 시간 차이 | 하나를 강제 선택. 미선택 시 다음 비활성 | 세 후보 모두 부적합이면 `후보 다시 받기`로 C-04를 유지한 채 재생성 |
| C-06 | 선택 요약, 마스터 예상 결과, 비용 범위, 시간 범위, 파생 가능 채널 | `이 방향으로 생성` 또는 단계별 수정 | 비용 상한 초과 시 C-03으로 바로 이동 |
| C-07 | 대본, 소재, 음성, 편집, 검수 단계별 진행 | 백그라운드 전환, 안전 구간에서 취소 | 실패 단계부터 재시도. 완료 단계 재과금 금지 여부는 기술설계 필요 |
| C-08 | 마스터 결과, 선택 근거 요약, 실제 사용 크레딧, revision 1 | 편집, 채널 파생, 채널 연결, 저장 후 종료 | 항상 초안으로 보존. 발행을 강제하지 않음 |

### 9.2 질문 단계별 선택 계약

| 단계 | 선택지 예 | 추천 방식 | 근거 표시 | 필수 여부 |
|---|---|---|---|---|
| 업계 | 교육, 이커머스, 뷰티, 음식, 전문서비스 | 최근 작업과 명시된 사업 정보가 있으면 상단 추천 | 우리 실험 로그 또는 사용자 저장 정보 | 1개 필수 |
| 목적 | 인지도, 프로필 방문, 문의, 구매, 재방문 | 목적과 측정 가능한 지표를 함께 추천 | 가설 또는 우리 실험 로그 | 1개 필수 |
| 채널 | Instagram, Threads, YouTube, TikTok 등 | 목적과 사례 분포로 최대 3개 | 공개 사례와 관찰일 | 1개 이상 |
| 영상 사례 | 첫 장면, 구조, 길이가 다른 카드 | 복제할 요소와 피할 요소를 분리 | 공개 URL, 캡처 시점, 관찰 | 1개 이상 |
| 스타일 | 실사 혼합, 템플릿, 순수 생성 등 | 비용, 시간, 품질 우선 3안 | Studio 견적과 가설 | 1개 필수 |
| A/B/C | 방향 A, B, C | 목표 적합도가 가장 높은 1안 표시 | 앞선 선택과 근거 요약 | 정확히 1개 필수 |

실제 비용과 시간은 Studio 견적 응답을 사용한다. 모든 예시 카드에 최소값과 최대값, 산정 시각, 포함 범위를 표시한다. 숫자가 없으면 카드를 완성된 선택지처럼 보이지 않는다.

### 9.3 Edge 경로

| 조건 | 화면 분기 | 복구 |
|---|---|---|
| 업계가 목록에 없음 | C-01의 `직접 입력`을 열고 유사 업계 3개 추천 | 사용자가 유사 업계를 선택하거나 직접 업계명을 확정 |
| 목적이 둘 이상 | C-01에서 1차 목적 강제, 2차 목적은 메모로 보관 | 결과 뒤 별도 파생 실험 제안 |
| 사례 출처가 오래됨 | C-02 카드에 오래된 사례 배지와 날짜 표시 | 최신 사례만 필터하거나 그대로 참고할 이유를 확인 |
| 추천과 사용자 선택이 충돌 | C-04에 예상 영향 비교 | 사용자 선택 유지 또는 추천 수락. 조용한 자동 변경 금지 |
| 비용 상한이 중간에 바뀜 | C-03과 C-06 견적 갱신 | 영향받는 스타일만 다시 선택 |
| A/B/C 모두 싫음 | C-05에서 `후보 다시 받기` | 기존 브리프는 보존하고 후보만 갱신 |
| 브라우저 이탈 | 모든 단계 임시 저장 | 돌아오면 마지막 완료 단계와 미완료 선택을 구분해 복원 |

### 9.4 Empty 경로

| 빈 데이터 | 표시 | 다음 행동 |
|---|---|---|
| 처음이라 우리 실험 로그 없음 | `아직 우리 데이터가 없어 공개 사례와 가설로 추천합니다` | 공개 사례로 계속하기 |
| 해당 업계 공개 사례 없음 | 빈 갤러리 대신 인접 업계 3개와 차이 설명 | 인접 업계 선택 또는 업계 수정 |
| 스타일 미리보기 없음 | 카드 숨김이 아니라 제작 불가 사유 표시 | 지원 스타일 선택 또는 견적 재조회 |
| 저장된 브랜드 정보 없음 | 필요한 항목을 선택 카드로 제시 | 기본값 선택 후 계속 |

### 9.5 Error 경로

| 오류 | 화면 | 보존되는 것 | 복구 |
|---|---|---|---|
| 사례 조회 실패 | C-02 inline error | 업계와 목적 | 다시 불러오기, 공개 사례 없이 가설로 계속하기 |
| 견적 실패 | C-03 해당 카드 error | 이전 선택 | 다시 계산, 다른 스타일 선택. 숫자 추정 금지 |
| 크레딧 부족 | C-06 차단 패널 | 브리프, A/B/C 선택, 견적 | 충전 또는 비용 낮은 스타일로 C-03 이동. 결제 후 C-06 복귀 |
| 생성 실패 | C-07 실패 단계 | 완료된 중간 산출물과 선택 | 실패 단계 재시도, 저장 후 종료, 지원 요청 |
| 결과 검수 실패 | C-07 검수 error | 생성 결과는 격리 보존 | 자동 수정 재시도 또는 사람 검수 요청 |

### 9.6 Loading 경로

- C-02는 사례 카드 skeleton과 `공개 사례 확인 중`을 표시한다.
- C-03은 카드별 견적을 독립적으로 채우며 계산 전 카드는 선택할 수 없다.
- C-05는 A, B, C를 동시에 비워두지 않고 각 후보 생성 상태를 분리한다.
- C-07은 현재 단계, 완료 단계, 남은 단계, 안전한 취소 가능 여부를 표시한다.

## 10. 편집 흐름

### 10.1 Happy 경로

`C-08 수정하기` -> `E-01 수정 지시` -> `E-02 영향과 추가 크레딧 확인` -> `E-03 원본과 수정본 비교` -> `C-08 수정본 채택`

| 화면 | 핵심 요소 | 인터랙션 | 보존 규칙 |
|---|---|---|---|
| E-01 | 현재 revision, 미리보기, 추천 수정 칩, 자연어 입력 | `자막 키워줘`, `이 컷 짧게`, `로고를 오른쪽 위로` 선택 또는 입력 | 현재 revision 잠금 |
| E-02 | 바뀌는 컷, 예상 처리시간 범위, 추가 크레딧 | 자막, 전환, 컷 길이, 오버레이, 비율 변경은 허용 범위일 때 `추가 크레딧 0` | 원본 revision 유지 |
| E-03 | 원본과 수정본, 변경점 목록, 재생 위치 동기화 | 수정본 채택, 다시 수정, 원본 유지 | 채택해도 이전 revision 삭제 금지 |

### 10.2 추가 크레딧 표시 규칙

- 자막 크기, 폰트, 색, 위치, 자막 문구 수정은 내레이션을 바꾸지 않으면 `추가 크레딧 0`.
- 전환과 효과, 컷 순서, 컷 길이, 컷 삭제, 오버레이, 비율과 크롭은 기존 소재 재사용 범위에서 `추가 크레딧 0`.
- 내레이션 내용 변경이나 새 소재 생성이 필요하면 E-02에서 새 비용 범위를 먼저 보여주고 확인을 받는다.
- `추가 크레딧 0` 옆 보조 설명은 `기존 결과물을 다시 편집합니다. 원본은 유지됩니다.`로 통일한다.

### 10.3 Edge 경로

| 조건 | 분기 | 복구 |
|---|---|---|
| 지시가 모호함 | E-01이 해석 2개에서 3개를 선택지로 제시 | 하나를 고르거나 직접 범위 지정 |
| 한 지시에 무과금과 유과금 변경이 섞임 | E-02에서 두 묶음으로 분리 | 무과금만 적용 또는 전체 견적 승인 |
| 채널 파생물만 수정하고 싶음 | E-01에서 마스터와 해당 파생물 범위를 선택 | 파생물만 새 revision, 마스터 유지 |
| 수정 중 원본으로 돌아감 | E-03의 `원본 유지` | 새 revision을 폐기하지 않고 히스토리에 미채택으로 보존 |
| 긴 수정 이력 | revision 최신 8개 우선, 나머지 접기 | 날짜, 작성자, 변경 종류로 검색 |

### 10.4 Empty 경로

| 빈 데이터 | 표시 | 다음 행동 |
|---|---|---|
| 편집할 결과물 없음 | 빈 편집기가 아니라 `먼저 결과물을 만드세요` | C-01로 이동 |
| 추천 수정 없음 | 자연어 입력과 기본 수정 범주만 표시 | 사용자가 수정 범주 선택 |
| 변경점 없음 | `원본과 같은 결과입니다` | 지시 수정 또는 원본 유지 |

### 10.5 Error 경로

| 오류 | 보존되는 것 | 복구 |
|---|---|---|
| 렌더 실패 | 원본과 수정 지시 | 같은 지시 재시도, 지시 수정, 원본으로 돌아가기 |
| 일부 컷만 실패 | 성공한 revision 후보와 실패 컷 목록 | 실패 컷만 재처리 또는 전체 미채택 |
| 저장 실패 | 로컬 임시 수정과 원본 | 다시 저장, 지시 복사, 안전하게 나가기 |
| 크레딧 판정 불일치 | 적용 전 정지 | 비용 상세 재계산. 사용자 승인 전 유과금 처리 금지 |

### 10.6 Loading 경로

E-02는 영향 분석과 비용 판정을 별도 상태로 표시한다. E-03은 저해상도 비교를 먼저 열고 최종 렌더가 준비되면 교체한다. 처리 중에도 원본 재생과 이력 탐색은 허용한다.

## 11. 채널 연결과 계정 전환 흐름

### 11.1 해결해야 하는 실측 결함

브라우저에 다른 소셜 계정 세션이 있으면 그 계정으로 인증이 진행되고, 서비스 안에서 로그아웃하거나 계정을 바꾸지 못했다. 다계정 운영자에게 `연결 성공`은 충분하지 않다. `어느 계정이 돌아왔는지 확인했고 의도한 계정인지 승인했다`까지가 성공이다.

서비스는 외부 제공자의 쿠키와 로그인 세션을 강제로 지울 수 없다. 따라서 제공자가 공식 계정 선택기를 지원하면 사용하고, 지원하지 않으면 연결 전 세션 점검과 연결 뒤 실제 계정 확인을 결합한다.

### 11.2 Happy 경로

`A-01 채널 상태 센터` -> `A-02 다른 계정 추가` -> `A-03 외부 세션 확인` -> `A-04 OAuth` -> `A-05 돌아온 계정 확인` -> `A-07 연결 계정 관리`

| 화면 | 사용자가 보는 것 | 선택과 추천 | 안전장치 |
|---|---|---|---|
| A-01 | 플랫폼별 연결 계정 수, 기본 계정, 만료, 권한, 마지막 확인 | 복수 계정 운영자는 `다른 계정 추가` 추천 | 계정명과 외부 식별자 끝자리로 구분. 토큰 표시 금지 |
| A-02 | `기존 계정 사용`, `다른 계정 추가`, `만료 계정 재연결` | 새 브랜드 계정이면 `다른 계정 추가` 추천. 이유 표시 | 추가와 교체를 같은 버튼으로 표현하지 않음 |
| A-03 | 브라우저에서 현재 로그인된 제공자 세션을 서비스가 확정할 수 없다는 설명 | 제공자 계정 센터 확인, 비공개 창 사용, Google 공식 선택기 중 가능한 옵션 | `서비스에서 로그아웃`처럼 통제할 수 없는 약속 금지 |
| A-04 | 제공자 공식 화면 | 필요한 최소 권한 승인 | 돌아올 위치와 취소 시 보존 항목을 출발 전에 안내 |
| A-05 | 돌아온 실제 계정의 프로필명, 사용자명, 외부 식별자 일부, 권한 | `이 계정을 추가` 또는 `다른 계정으로 다시 연결` | 사용자 확인 전 기본 계정 변경 금지 |
| A-07 | 플랫폼별 모든 계정, 기본 배지, 상태, 예약 영향 | 향후 기본 계정 변경과 특정 작업 계정 선택을 분리 | 기존 예약은 지정된 계정을 유지. 계정 삭제는 영향 건수 확인 뒤 별도 승인 |

### 11.3 제공자별 계정 전환 규칙

| 제공자 능력 | 연결 방식 | 화면 규칙 |
|---|---|---|
| 공식 계정 선택 지원 | 계정 선택 요청을 사용 | A-03에서 `계정 선택 창이 열립니다` 표시. 현재 확인된 공식 근거는 Google 계열 |
| 선택기 없이 현재 세션을 재사용할 수 있음 | 제공자 계정 센터 또는 비공개 창 안내 | A-03에 현재 계정 점검 체크리스트. 복귀 뒤 A-05 확인 필수 |
| 여러 자산을 한 사용자 계정이 관리 | OAuth 뒤 실제 페이지나 비즈니스 자산 선택 | 사용자 계정과 발행 자산을 두 단계로 구분 |
| 제공자 상태 불명 | 자동 전환을 시도하지 않음 | A-03에 불명 상태와 수동 확인 경로 표시 |

### 11.4 잘못된 계정이 돌아온 경우

`A-04` -> `A-05 의도한 계정 아님` -> `A-06 후보 연결 보류` -> `A-03 세션 변경 안내` -> `A-04 재인증`

A-06 규칙:

- 새 후보는 활성 또는 기본 계정으로 확정하지 않는다.
- 기존 기본 계정과 예약, 초안은 그대로 둔다.
- `후보 폐기`, `제공자 계정 바꾸기`, `비공개 창에서 다시 연결`, `지금은 그만두기`를 제공한다.
- 사용자가 그만두면 A-01로 돌아가며 미완료 연결은 `확인 필요`로 남기지 않고 폐기한다.
- 후보 토큰을 언제 저장하고 폐기할지는 기술설계에서 보안 검토가 필요하다.

### 11.5 Edge 경로

| 조건 | 화면 분기 | 복구 |
|---|---|---|
| 같은 제공자의 복수 계정 | A-02에서 다른 계정 추가, A-05에서 정확한 식별자 확인 | A-07에서 기본 계정과 작업별 계정 분리 |
| 같은 표시 이름 | 외부 식별자 끝자리와 프로필 이미지, 페이지명 함께 표시 | 사용자가 실제 계정 확인 |
| OAuth 중 취소 | A-04에서 A-02로 복귀 | 기존 연결과 작업 보존, 다시 시도 |
| 권한 일부만 승인 | A-05에 가능한 기능과 막힌 기능 분리 | 추가 권한 요청 또는 읽기 전용으로 보류 |
| 기본 계정 변경 | A-07에서 `향후 기본값`임을 명시 | 기존 예약은 지정 계정 유지, 새 예약만 새 기본값 |
| 연결 계정 삭제 | 영향받는 예약, 초안, 실패 이력 수 표시 | 영향 확인 후 삭제 또는 비활성 유지 |

### 11.6 Empty 경로

| 빈 데이터 | 표시 | 다음 행동 |
|---|---|---|
| 연결 계정 0개 | 채널별 빈 카드와 `첫 계정 연결` | A-02 |
| 선택 가능한 자산 0개 | OAuth는 성공했지만 발행 자산이 없음을 설명 | 제공자에서 자산 생성 또는 다른 계정 연결 |
| 발행 권한 0개 | 연결됨으로 표시하지 않음 | 권한 다시 승인 또는 연결 취소 |

### 11.7 Error 경로

| 오류 | 화면 | 보존되는 것 | 복구 |
|---|---|---|---|
| 토큰 만료 | A-08 및 영향받은 작업의 repair card 후보 | 초안, 마스터, 예약 시각, 계정 식별자 | 같은 계정 재연결, 다른 계정 선택, 작업 저장 |
| 상태값 불일치 | A-08 `확인 필요` | 기존 기본 계정 | 새 발행 차단, 상태 재조회 |
| OAuth 콜백 실패 | A-08 | 연결 전 상태와 작업 | 다시 인증, 상태 센터로 돌아가기 |
| 잘못된 계정 강제 로그인 | A-06 | 기존 연결 전체 | 후보 폐기, 외부 세션 변경, 비공개 창 재시도 |
| 권한 철회 | A-08 | 콘텐츠와 예약 | 권한 재승인. 실패한 발행은 P-06에서 별도 재시도 |

### 11.8 Loading 경로

- A-01은 계정 목록과 상태 점검을 분리한다. 계정명은 보이되 상태만 확인 중일 수 있다.
- A-04로 이동하기 전 `외부 제공자 화면으로 이동합니다`를 표시한다.
- A-05는 계정 식별과 권한 검증이 끝나기 전 `연결됨`을 표시하지 않는다.
- A-08 재연결 중에도 다른 정상 계정으로 발행하는 경로는 유지한다.

## 12. 탭 일관 규칙

### 12.1 공통 기본 탭과 순서

콘텐츠 채널은 다음 기본 순서를 유지한다.

1. Queue
2. Analytics
3. Growth
4. Popular
5. Settings

Editor가 구조적으로 필요한 채널은 Queue 다음에 삽입한다. 따라서 Instagram은 `Queue, Editor, Analytics, Growth, Popular, Settings` 순서다. 기존 v24와 `channel-capabilities.ts`의 순서를 계승하며 새 정보구조를 만들지 않는다.

### 12.2 숨김과 비활성 규칙

| capability 상태 | 표현 | 예 |
|---|---|---|
| 구조적으로 불가능 | 탭 숨김 | 메시징 채널에서 공개 인기 콘텐츠나 성장 분석이 성립하지 않는 경우 |
| 개념상 가능하지만 미구현 | 위치를 유지하고 비활성, `연동 예정` 이유 표시 | 표준 텍스트 채널의 Growth와 Popular |
| 사용 가능하지만 데이터 0 | 탭 활성, 내부 empty state | Analytics에 아직 발행 데이터가 없음 |
| 일시 오류 | 탭 활성, 내부 repair state | 토큰 만료나 분석 조회 실패 |
| 권한에 따라 사용 불가 | 탭 활성, 권한 해결 CTA | Analytics 권한 미승인 |

숨김은 capability 자체가 성립하지 않을 때만 허용한다. 구현되지 않았다는 이유로 숨기면 사용자가 플랫폼마다 정보구조가 다르다고 느끼므로 비활성과 이유 표시를 쓴다.

### 12.3 현재 채널군에 적용한 예시

| 채널군 | 탭 구성 |
|---|---|
| Threads | Queue, Analytics, Growth, Popular, Settings |
| Instagram | Queue, Editor, Analytics, Growth 비활성, Popular 비활성, Settings |
| 표준 텍스트 채널 | Queue, Analytics, Growth 비활성, Popular 비활성, Settings |
| 비디오 채널 | Queue, Analytics, Growth 비활성, Popular 비활성, Settings |
| 메시징 채널 | Settings만 표시. 나머지는 구조적으로 성립하지 않는 경우에 한함 |

### 12.4 반응형 탭

- 1024에서는 한 줄 탭을 유지하고 콘텐츠 본문과 시작 정렬선을 맞춘다.
- 390에서는 탭 순서를 유지한 가로 내부 스크롤을 사용한다.
- 선택된 탭이 진입 시 보이도록 스크롤 위치를 맞춘다.
- 페이지 전체의 가로 overflow는 금지한다.
- 비활성 탭을 눌렀을 때 같은 위치에서 이유와 열리는 조건을 보여준다.

## 13. 발행과 예약 흐름

### 13.1 Happy 경로

`C-08 채널 파생 만들기` -> `P-01 마스터와 파생물` -> `P-02 채널별 캡션과 해시태그` -> `P-03 계정과 정책 점검` -> `P-04 예약 시각` 또는 `P-05 지금 발행` -> `P-06 결과`

| 화면 | 핵심 요소 | 인터랙션 | 안전장치 |
|---|---|---|---|
| P-01 | 마스터, 7채널 파생물, 비율, 길이, 안전영역, 변경 배지 | 채널 선택, 파생물 재생성, 해당 파생만 편집 | 파생물 수정이 마스터를 덮지 않음 |
| P-02 | 채널별 캡션, 해시태그, 첫 댓글, 글자수와 정책 한도 | 공통 내용 검토 후 채널별 override 후보 | 생성은 Studio, 제약은 OpenClaw |
| P-03 | 각 채널의 정확한 계정, 토큰, 권한, 정책 검사, 예상 발행 수 | 계정 변경, 만료 복구, 거부 항목 수정 | 한 채널 문제로 정상 채널까지 조용히 막지 않음 |
| P-04 | 사용자 시간대, 채널별 현지 시각, 충돌, 권장 시간과 근거 | 공통 시각 또는 채널별 시각 설정 | 과거 시각, DST, 중복 예약 방지 |
| P-05 | 채널별 독립 progress | 아직 요청 전인 채널 취소, 백그라운드 전환 | 플랫폼 응답 전 성공 표시 금지 |
| P-06 | 성공, 실패, 정책 거부, 확인 중을 채널별 표시 | 실패만 재시도, 콘텐츠 수정, 계정 복구, 성과로 이동 | 성공한 채널 중복 발행 금지 |

### 13.2 Edge 경로

| 조건 | 분기 | 복구 |
|---|---|---|
| 한 파생물만 비율 불일치 | P-01 해당 카드 경고 | 해당 파생만 다시 만들기 |
| 플랫폼 글자수 초과 | P-02 초과 위치와 자동 축약 후보 | 직접 수정 또는 축약 후보 선택 |
| 같은 플랫폼 복수 계정 | P-03 정확한 계정 강제 확인 | A-07에서 기본 변경 또는 이번 작업만 변경 |
| 채널별 시각이 다름 | P-04 채널별 모드 | 공통 시각으로 되돌리거나 개별 유지 |
| 일부만 발행 가능 | P-03 요약에서 가능과 차단 분리 | 가능 채널만 진행 또는 모두 보류 |
| 예약 뒤 마스터 변경 | 영향받는 예약과 파생물 표시 | 기존 예약 유지, 파생 갱신 후 재승인, 예약 취소 중 선택 |

### 13.3 Empty 경로

| 빈 데이터 | 표시 | 다음 행동 |
|---|---|---|
| 선택된 채널 0개 | `발행할 채널을 선택하세요`와 연결 없이 저장 가능 안내 | 채널 선택, A-01 연결, 저장 후 종료 |
| 연결 계정 0개 | 콘텐츠를 그대로 보존한 연결 CTA | A-01, 또는 결과로 돌아가기 |
| 캡션 후보 0개 | Studio 생성 실패 상태 | 다시 생성 또는 직접 입력 |
| 예약 0개 | 첫 예약 CTA와 바로 발행 | P-04 또는 P-05 |

### 13.4 Error 경로

| 오류 | 화면 | 보존되는 것 | 복구 |
|---|---|---|---|
| 토큰 만료 | P-03 또는 P-06 repair card 후보 | 마스터, 파생물, 캡션, 예약 시각 | A-08 재연결 후 정확한 화면 복귀 |
| 발행 실패 | P-06 채널별 실패 | 성공 채널 상태와 실패 요청 | 실패 채널만 재시도. 크레딧 처리는 D-09 결정 전 |
| 크레딧 부족 | P-01 파생 생성 전 또는 유과금 재생성 전 | 기존 마스터와 무과금 파생 | 충전, 기존 소재 범위로 축소, 해당 채널 제외 |
| 채널 정책 거부 | P-03 사전 검사 또는 P-06 제공자 응답 | 다른 채널과 콘텐츠 원본 | 위반 위치, 정책 출처, 수정 후보를 보여주고 해당 채널만 재검수 |
| 예약 생성 실패 | P-04 | 시각, 계정, 콘텐츠 | 같은 설정 재시도, 초안 저장, 바로 발행 전환 |
| 응답 시간 초과 | P-06 `확인 중` | 요청 식별자와 콘텐츠 | 상태 재조회. 즉시 중복 재발행 금지 |

### 13.5 Loading 경로

- P-01은 파생물마다 생성 상태를 독립 표시한다.
- P-03은 계정, 토큰, 정책 검사를 행 단위로 갱신한다.
- P-05는 `요청 전`, `전송 중`, `플랫폼 확인 중`, `성공`, `실패`를 구분한다.
- P-06의 `확인 중`은 성공도 실패도 아니며 상태 재조회만 허용한다.

## 14. 성과 흐름

### 14.1 Happy 경로

`P-06 성과 보기` -> `R-01 성과 개요` -> `R-02 선택과 결과 연결` -> `R-03 다음 실험 추천` -> `C-04 추천 수락 또는 수정`

| 화면 | 핵심 요소 | 인터랙션 | 해석 가드 |
|---|---|---|---|
| R-01 | 기간, 채널, 계정, 목적 지표, 게시물 목록 | 기간과 비교군 선택 | 노출 수와 전환을 같은 의미로 합치지 않음 |
| R-02 | 업계, 목적, 선택 사례, 스타일, A/B/C, revision, 채널 파생과 결과 | 선택 하나를 눌러 관련 결과만 비교 | `이 선택 뒤 관찰된 결과`라고 표현. 인과 단정 금지 |
| R-03 | 다음 선택 3안, 예상 효과, 비용, 근거, 표본, 불확실성 | 추천 선택, 직접 변경, 이번에는 건너뛰기 | 우리 로그, 공개 사례, 가설을 구분 |

### 14.2 Edge 경로

| 조건 | 분기 | 복구 |
|---|---|---|
| 게시 뒤 콘텐츠를 수정함 | revision별 결과 분리 | 동일 revision 기간만 비교 |
| 여러 채널에 다른 파생 | 채널과 파생별 성과 | 마스터 수준과 파생 수준을 전환 |
| 목표가 중간에 바뀜 | 목표 변경 시점을 세로 기준으로 표시 | 변경 전후를 별도 기간으로 비교 |
| 외부 캠페인이 겹침 | 외부 변수 메모 | 메모 포함 또는 제외한 해석 보기 |
| 표본이 작음 | 추천 신뢰도 낮음 표시 | 더 모으기 또는 저비용 실험 추천 |

### 14.3 Empty 경로

| 빈 데이터 | 표시 | 다음 행동 |
|---|---|---|
| 발행 0건 | `제작 결과는 있지만 비교할 발행이 없습니다` | P-01 또는 계속 제작 |
| 성과 수집 전 | 예상 수집 시점과 마지막 동기화 | 나중에 알림 또는 제작으로 돌아가기 |
| 선택 이력 없음 | 과거 게시물은 성과만 표시하고 연결 분석 비활성 | 새 제작부터 선택 기록 시작 |
| 해당 기간 데이터 없음 | 기간 변경 추천 | 최근 데이터 있는 기간으로 이동 |

### 14.4 Error 경로

| 오류 | 보존되는 것 | 복구 |
|---|---|---|
| 분석 토큰 만료 | 저장된 마지막 성과와 게시 이력 | A-08 재연결, 마지막 동기화 시각 표시 |
| 일부 채널 조회 실패 | 정상 채널 데이터 | 실패 채널만 재조회 |
| 선택 이력과 게시물 연결 실패 | 원시 게시 결과 | 연결되지 않은 항목 목록, 수동 확인 후보 |
| 지표 정의 불일치 | 해당 비교 차단 | 정의와 기간을 맞춘 뒤 재계산 |

### 14.5 Loading 경로

R-01은 마지막 저장 데이터와 동기화 상태를 함께 표시한다. R-02는 선택 이력과 성과 조인을 계산 중일 때 원시 성과를 숨기지 않는다. R-03은 근거가 준비된 추천부터 열고 근거 없는 카드는 표시하지 않는다.

## 15. SNS 계정 연결 없이 제작만 해 보는 흐름

### 15.1 Happy 경로

`G-00 제작부터 시작` -> `T-01 SNS 연결 없이 계속` -> `C-01`부터 `C-08` -> `T-02 결과 확인` -> `E-01 편집` 또는 `저장 후 종료`

OpenClaw 로그인과 작업공간은 기존 제품 진입을 따른다. 새 익명 인증 방식을 이 문서에서 만들지 않는다. 단, 어떤 SNS 계정도 연결하지 않은 상태로 제작, 결과 확인, 원본 보존 편집까지 갈 수 있어야 한다.

| 화면 | 핵심 요소 | 인터랙션 | 마찰 감소 |
|---|---|---|---|
| G-00 | `연결 없이 제작부터`, `채널 연결하고 시작` | 기본 추천은 제작부터 | 연결이 제작의 선행 조건처럼 보이지 않음 |
| T-01 | 가능한 것과 나중에 필요한 것 | 계속 제작, 연결하기 | 한 문장 안내 후 C-01로 이동 |
| T-02 | 마스터, 편집, 저장, 파생 미리보기 | 편집, 결과 보존, 나중에 연결 | 연결 모달을 자동으로 띄우지 않음 |
| T-03 | 연결하면 열리는 기능과 현재 결과 보존 안내 | 연결 시작, 계속 미룸 | 연결 취소 시 T-02로 복귀 |

### 15.2 Edge 경로

| 조건 | 분기 | 복구 |
|---|---|---|
| 제작 중 발행을 누름 | 현재 단계를 저장하고 연결 안내 | A-01 또는 제작으로 복귀 |
| 결과 뒤 채널을 하나만 연결 | 해당 채널 파생과 발행만 활성 | 다른 채널은 미리보기 또는 미연결 상태 유지 |
| 연결을 취소함 | T-03에서 결과로 복귀 | C-08과 편집 유지 |
| 무연결 상태로 여러 결과 보유 | 최근 결과와 저장 목록 | 검색, 정렬, 오래된 항목 접기 |

### 15.3 Empty 경로

| 빈 데이터 | 표시 | 다음 행동 |
|---|---|---|
| SNS 계정 0개 | 오류가 아닌 정상 `무연결` 상태 | 제작 시작 |
| 결과 0개 | 첫 제작 CTA | C-01 |
| 파생 가능한 채널 0개 | 마스터 편집과 저장만 표시 | 결과 편집 또는 종료 |

### 15.4 Error 경로

| 오류 | 보존되는 것 | 복구 |
|---|---|---|
| 제작 실패 | 모든 선택과 완료 단계 | C-07 재시도 또는 저장 후 종료 |
| 크레딧 부족 | 브리프와 후보 | 충전 또는 저비용 스타일 선택 |
| 저장 실패 | 현재 세션의 결과와 수정 지시 | 다시 저장, 안전 복사, 지원 요청 |
| 연결 시도 실패 | 제작 결과 전체 | T-02로 돌아가 계속 편집하거나 A-08 재시도 |

### 15.5 Loading 경로

무연결 경로의 생성 loading은 연결 경로와 동일하다. 연결 상태를 조회하느라 제작 시작을 막지 않는다. 결과 화면에서 채널 연결 상태를 불러오는 동안에도 재생, 편집, 저장은 가능하다.

## 16. 반응형, 밀도, overflow 계약

### 16.1 공통 토큰

- spacing은 `4, 8, 12, 16, 24, 32, 48`만 사용한다.
- 페이지 섹션 간격은 24 또는 32, 카드 내부는 16 또는 24, 밀접한 라벨과 값은 4 또는 8을 쓴다.
- 임의 spacing 값은 금지한다.
- 색, 타이포, radius는 DESIGN.md v24 토큰을 그대로 사용한다.
- 질문 카드, 견적 범위, 계정 확인, repair card, revision compare와 v9.1 운영자 패턴은 DESIGN.md v24.2까지 반영했다.

### 16.2 1024 레이아웃

| 화면군 | 주축 | 열수 | 배치 |
|---|---|---|---|
| 앱 공통 | row | 사이드바 1 + 메인 1 | 기존 224 사이드바, 메인 콘텐츠 |
| C-01, C-02, C-03, C-04 | column 안의 12열 grid | 콘텐츠 8 + 근거 요약 4 | 선택이 주축, 근거가 보조 |
| C-05, E-03, P-01 | row | 비교 3열 또는 미리보기 7 + 상세 5 | 비교 대상의 시작선과 높이 일치 |
| A-01, A-07 | column | 목록 8 + 상태 요약 4 | 계정 목록이 먼저 |
| P-02, P-03, P-04 | column | 본문 8 + 요약 4 | 오른쪽 요약은 sticky 가능, 본문을 가리지 않음 |
| R-01, R-02 | column | 그래프 8 + 필터 4 | 필터 변경 시 결과 보존 skeleton |

선택 카드가 3개면 3열, 4개 이상이면 2열을 기본으로 한다. 카드 높이를 억지로 고정하지 않고 내부 콘텐츠에 맞추되 행 시작선을 맞춘다.

### 16.3 390 레이아웃

| 화면군 | 주축 | 열수 | 순서 |
|---|---|---|---|
| 모든 질문 화면 | column | 1열 | 단계, 답, 추천 이유, 카드, CTA |
| 비교 화면 | column | 1열 | 추천 후보 먼저, 나머지 후보, 상세 비교 |
| 계정 화면 | column | 1열 | 상태, 실제 계정, 행동, 도움말 |
| 발행 화면 | column | 1열 | 차단 항목, 채널 카드, 요약, CTA |
| 성과 화면 | column | 1열 | 목적 지표, 선택 필터, 결과, 한계 |

- 하단 CTA는 콘텐츠를 가리지 않도록 안전영역과 16 spacing을 확보한다.
- 탭, 긴 표, 타임라인만 컴포넌트 내부 가로 스크롤을 허용한다.
- 선택 카드는 잘라서 carousel로 숨기지 않고 세로 목록으로 모두 보인다.
- 긴 계정명과 캡션은 줄바꿈하고 외부 식별자는 말줄임과 전체 보기 행동을 제공한다.

### 16.4 데이터량 0, 정상, 과다

| 데이터 | 0 | 정상 | 과다 |
|---|---|---|---|
| 사례 | 빈 이유와 인접 업계 3개 | 3개에서 6개 | 추천 6개 우선, 필터와 더 보기 |
| 스타일 | 지원 불가 이유와 업계 수정 | 3개에서 5개 | 목적별 묶음, 최대 5개 비교 |
| 연결 계정 | 첫 연결 CTA | 플랫폼당 1개에서 5개 | 검색, 상태 필터, 플랫폼별 접기 |
| 채널 파생 | 마스터만 표시 | 선택 채널 1개에서 7개 | 채널 필터와 문제 있는 항목 우선 |
| 발행 결과 | 첫 발행 CTA | 처음 3건에서 8건까지 확장 | 최근 8건, 기간 필터, 페이지 또는 더 보기 |
| revision | 원본 없음 안내 | 최근 3개에서 8개 | 최신 8개 우선, 날짜와 변경 종류 검색 |
| 성과 | 첫 발행 또는 기간 변경 | 핵심 3지표와 게시물 3개에서 8개 | 집계 우선, 표 내부 스크롤, 내보내기 |

### 16.5 Overflow 점검 기준

- 한글 40자 계정명, URL, 다국어 해시태그, 2,200자 캡션을 넣어도 페이지 가로 넘침 0.
- 비용과 시간 범위는 한 줄을 우선하되 390에서 라벨과 값을 두 줄로 분리할 수 있다.
- A/B/C 카드의 근거가 길면 3줄 요약과 `근거 보기`를 쓴다.
- 오류 항목이 20개여도 상단 요약과 오류 필터를 유지한다.
- 이미지 비율이 달라도 고정된 미디어 프레임 안에서 contain 또는 crop 기준을 명시한다.

## 17. 상태와 복구 계약

### 17.1 전역 복구 원칙

1. 실패 화면마다 `무엇이 보존됐는지`를 먼저 말한다.
2. 재시도는 실패한 단위만 대상으로 한다.
3. 성공한 채널이나 revision을 중복 처리하지 않는다.
4. 복구 뒤 사용자가 떠난 화면과 필드 위치로 돌아온다.
5. 외부 상태를 모르면 성공으로 낙관하지 않고 `확인 중` 또는 `확인 필요`로 둔다.
6. 초안, 마스터, 파생, 예약은 삭제 행동이 아니면 유지한다.

### 17.2 필수 실패 상태 추적

| 필수 상태 | 첫 발견 화면 | 복구 화면 | 종료점 |
|---|---|---|---|
| 토큰 만료 | A-01, P-03, P-06, R-01 | A-08 | 원래 화면 또는 A-01 |
| 발행 실패 | P-05, P-06 | P-06, P-03 | 재시도 성공, 초안 저장, 실패 유지 중 하나 |
| 크레딧 부족 | C-06, P-01, E-02 | 충전 또는 C-03 | 비용 범위 재확인 뒤 진행 또는 저장 종료 |
| 채널 정책 거부 | P-03, P-06 | P-02 또는 E-01 | 수정 후 재검수, 해당 채널 제외, 저장 종료 |

## 18. 추적성 및 dead-end 감사

### 18.1 요구사항 추적

| 요구 | 화면 | 상태 커버리지 | 판정 |
|---|---|---|---|
| 업계와 목적 선택 | C-01 | happy, edge, empty, error, loading | 충족 |
| 성공 채널과 영상 예시 | C-02 | happy, edge, empty, error, loading | 충족 |
| 스타일 여러 개 | C-03 | happy, edge, empty, error, loading | 충족 |
| 예시별 비용과 시간 범위 | C-03, C-06 | 견적 있음, 계산 중, 실패 | 충족. 실제 데이터 계약은 미정 |
| 직접 선택 또는 추천 | C-04 | 충돌, 추천 없음, 재추천 | 충족 |
| A/B/C 강제 선택 | C-05 | 선택, 재생성, 생성 실패 | 충족 |
| 결과물 | C-08 | 생성, 실패, 저장 | 충족 |
| 원본 유지 편집 | E-01부터 E-03 | happy, edge, empty, error, loading | 충족 |
| 추가 크레딧 없음 표시 | E-02 | 무과금, 혼합, 판정 실패 | 충족 |
| 다계정 강제 로그인 결함 | A-02부터 A-06 | 오계정, 취소, 권한 부족, token expiry | 충족. 제공자별 기술 검증 필요 |
| 탭 일관성 | 12장 | 숨김, 비활성, empty, error | 충족 |
| 채널별 파생물 | P-01 | 0, 정상, 과다, 생성 실패 | 충족 |
| 채널별 캡션과 해시태그 | P-02 | 0, 정상, 초과, 생성 실패 | D-10 결정 대기 |
| 예약 | P-04 | happy, edge, empty, error, loading | 충족 |
| 선택과 성과 | R-01부터 R-03 | happy, edge, empty, error, loading | 충족 |
| SNS 무연결 제작 | G-00, T-01부터 T-03 | happy, edge, empty, error, loading | 충족 |

### 18.2 화면별 종료와 탈출구

| 화면군 | 성공 종료 | 사용자가 그만둘 때 | 오류가 계속될 때 |
|---|---|---|---|
| 제작 | C-08 | 선택 자동 저장 후 Studio 목록 | 초안 저장, 실패 단계 표시, 지원 |
| 편집 | 수정본 채택 또는 원본 유지 | 원본으로 C-08 | 지시 저장, 원본 복원 |
| 연결 | A-07 또는 원래 작업 복귀 | 기존 연결을 유지하고 A-01 | 연결 후보 폐기, 무연결 계속 |
| 발행 | P-06 성공 또는 R-01 | 초안 저장 | 실패 채널 유지, 정상 채널 보호, 재시도 또는 제외 |
| 성과 | R-03 다음 실험 또는 종료 | Home으로 복귀 | 마지막 저장 데이터 표시, 재연결 또는 기간 변경 |
| 무연결 체험 | T-02 저장 또는 편집 | 결과 저장 후 종료 | 결과와 선택 보존, 재시도 |

감사 결과: 정의된 모든 화면에는 이전 화면, 저장 후 종료, 복구 화면 중 최소 하나가 있다. 오류가 외부 제공자에서 해결되지 않아도 콘텐츠를 잃지 않고 무연결 제작 또는 저장 상태로 끝낼 수 있다. 문서 수준 dead-end는 0이다.

## 19. 회장 결정 필요

### D-08. 토큰 만료와 채널 실패를 어디에 노출할지

무엇을 정할지: 영향받은 화면의 repair card와 전체 채널 상태 센터를 함께 쓸지 결정해야 한다.

추천안: 둘 다 사용한다. 작업 중에는 해당 화면에서 바로 복구하고, A-01에서는 계정 전체 상태를 관리한다.

선택 시 결과: 사용자가 발행 맥락을 잃지 않으면서도 다계정 전체를 점검할 수 있다.

미선택 시 리스크: 상태 센터만 쓰면 작업 복귀 비용이 크고, inline만 쓰면 다른 만료 계정을 놓친다.

이 문서에서는 후보 화면 A-08과 영향 화면 repair card를 모두 그렸지만 제품 계약으로 확정하지 않았다.

### D-09. 생성 성공 뒤 발행 실패의 크레딧 처리

무엇을 정할지: 생성 비용 유지, 발행 재시도 비용, 우리 결함일 때 복구 범위를 정해야 한다.

추천안: 생성 결과가 사용 가능하면 생성 비용은 유지한다. 같은 산출물의 발행 재시도에는 추가 크레딧을 받지 않는다. 우리 결함으로 결과 자체가 사용할 수 없을 때만 복구 또는 환급한다.

선택 시 결과: 제작 가치와 발행 전송 실패를 분리해 사용자에게 설명할 수 있다.

미선택 시 리스크: P-06이 정확한 비용 약속을 할 수 없어 분쟁과 중복 과금 불안을 만든다.

이 문서에서는 P-06에 `크레딧 처리 확인 중` 상태를 허용하며 숫자나 환급을 확정하지 않았다.

### D-10. 다채널 캡션과 해시태그의 기본 구조

무엇을 정할지: 공통 기본값에서 채널별 override를 만들지, 처음부터 채널별 독립본을 만들지 결정해야 한다.

추천안: Studio가 공통 마스터 텍스트를 만들고, 채널 제약을 받아 채널별 파생본을 만든다. 사용자는 공통 변경과 채널 한정 변경을 명시적으로 선택한다.

선택 시 결과: 반복 입력을 줄이면서 플랫폼별 길이와 화법을 지킬 수 있다.

미선택 시 리스크: 공통 편집이 파생본을 덮거나, 채널별 편집이 서로 다른 원본으로 갈라질 수 있다.

이 문서의 P-02는 두 구조를 수용하는 검수 화면만 정의하며 저장 계약은 확정하지 않았다.

## 20. 레드팀

### 20.1 까다로운 다계정 운영자의 공격

공격: `다른 계정 연결`을 눌러도 Meta가 이미 로그인된 개인 계정으로 다시 들어가면 화면만 늘고 해결되지 않는다.

수정: A-03에서 서비스가 외부 쿠키를 통제하지 못한다는 사실을 먼저 밝힌다. 제공자 계정 센터와 비공개 창 경로를 연결 전에 제공하고, A-05에서 실제 돌아온 계정을 승인하기 전 활성화하지 않는다. 잘못 돌아오면 A-06에서 후보만 폐기하고 기존 기본 계정과 예약을 보존한다.

### 20.2 회의적 투자자의 공격

공격: 8단계는 선택 피로를 줄이는 것이 아니라 전환을 떨어뜨리는 긴 설문일 수 있다.

수정: 각 단계는 빈 입력이 아니라 최대 3개에서 5개의 답을 먼저 보여주고, 이전 선택을 임시 저장한다. G-00에서 SNS 연결을 빼고 제작부터 시작하게 한다. 단계 이탈률, 추천 수락률, A/B/C 재생성률을 계측해야 하며, 실제 데이터 없이 8단계가 최적이라고 단정하지 않는다.

### 20.3 경쟁자의 공격

공격: 비용과 시간 범위를 붙여도 실제 견적이 없으면 신뢰를 더 잃는다.

수정: C-03의 모든 선택 카드에 실제 Studio 견적 범위를 필수화한다. 값이 없으면 `계산 중` 또는 실패 상태로 두며 가짜 숫자를 채우지 않는다. 범위의 산정 시각과 포함 범위를 표시한다.

### 20.4 정책 담당자의 공격

공격: 정책 거부를 자동 수정하면 사용자가 의도하지 않은 표현으로 발행할 수 있다.

수정: P-03과 P-06은 위반 위치, 정책 출처, 수정 후보만 제공한다. 수정본은 다시 미리보고 사용자가 승인해야 하며 자동 발행하지 않는다.

## 21. 셀프심문

질문: 이 결론이 틀렸다면 가장 그럴듯한 이유는 무엇인가?

답: 가장 load-bearing한 가정은 제공자 인증에서 돌아온 계정을 서비스가 안전하게 식별하고, 사용자 확인 전 연결을 보류할 수 있다는 것이다. 현재 구현은 복수 계정을 저장할 수 있지만 첫 연결의 기본값 처리와 후보 보류 상태가 이 UX를 그대로 지원한다는 증거는 없다. 또한 제공자마다 계정 선택과 자산 선택의 의미가 다르다.

수정: 문서에서 `후보 연결 보류`를 사용자 안전 요구로 정의하되 API나 저장 방식을 확정하지 않았다. A-05와 A-06을 구현하기 전 제공자별 샌드박스 검증과 보안 기술설계를 필수 gap으로 올렸다. 공식 선택기는 Google 계열에만 확인된 사실로 제한했다.

두 번째 질문: 성과 화면이 선택의 효과를 과장할 위험은 없는가?

답: 있다. 같은 기간의 캠페인, 계절, 계정 규모가 성과를 바꿀 수 있다.

수정: R-02의 문구를 `이 선택이 만든 결과`가 아니라 `이 선택 뒤 관찰된 결과`로 고정하고, 표본, 기간, 외부 변수, revision을 함께 표시한다. 다음 추천은 검증된 결론이 아니라 실험 후보로만 제시한다.

## 22. 품질 판정

### 22.1 Design Score

| 축 | 점수 | 근거 |
|---|---|---|
| 위계와 흐름 | B+ | 6개 흐름을 화면 ID와 종료점으로 연결 |
| 일관성 | A- | v24 셸, 탭 순서, 토큰, 상태어를 계승 |
| 접근성과 상태 | B+ | empty, loading, error, disabled, excess와 복구 정의 |
| 반응형 | B+ | 390/1024 주축, 열수, 순서, overflow 계약 정의 |
| 실제 시각 검증 | 미검증 | 이번 위임은 HTML과 렌더를 금지했으므로 v9 픽셀 검증 없음 |

**Design Score: B+ (저충실 흐름 설계 후보)**

이 등급은 정보구조와 상태 계약에 대한 자체 판정이다. 실제 프로토타입의 픽셀 품질 등급으로 오해하면 안 된다.

### 22.2 문서 루브릭

| 항목 | 점수 | 근거 |
|---|---|---|
| 완결성 | 5/5 | 필수 6흐름과 5상태, 필수 오류를 열거 |
| 정밀성 | 5/5 | 화면 ID, 입력, 행동, 보존, 복구를 화면 단위로 정의 |
| 근거 | 5/5 | 정본과 현재 구현, 공식 벤치마크를 대조 |
| 추적성 | 5/5 | 요구사항과 화면, 상태, 종료점을 매핑 |
| 출고 완성도 | 4/5 | 회장 결정과 기술 gap을 분리했으나 v9 렌더는 범위 밖이라 미검증 |

**RUBRIC_SCORE: completeness=5/5 precision=5/5 evidence=5/5 traceability=5/5 professionalism=4/5 total=24/25**

**WEAKEST_LINE:** A-05와 A-06의 후보 연결 보류는 필요한 UX지만 제공자별 콜백과 보안 저장 계약이 아직 없어 기술 검증 전에는 구현 가능성이 미검증이다.

## 23. 남은 gap과 다음 stage

### 23.1 회수 필요

- ⛔ 회수 필요: D-08, D-09, D-10을 회장이 결정해야 저충실 흐름을 확정할 수 있다.
- ⛔ 회수 필요: 제공자별 계정 선택, 외부 세션 전환, 콜백 뒤 후보 보류를 샌드박스에서 검증하고 보안 기술설계를 해야 한다.
- ⛔ 회수 필요: Studio가 스타일별 실제 비용과 시간 범위를 반환하는 데이터 계약이 없다. 거짓 견적 없이 C-03을 구현하려면 상류 합의가 필요하다.
- ⛔ 회수 필요: 선택 이력과 성과를 연결하는 데이터 계보가 없다. R-02는 기술설계 전에 구현할 수 없다.
- ⛔ 회수 필요: 이번 위임은 단일 user-flow 파일만 허용해 신규 질문 카드, 견적 카드, 계정 확인, repair card, revision compare 패턴을 DESIGN.md에 반영하지 않았다. 회장 리뷰 뒤 프로토타입 전에 DESIGN.md를 최신화해야 한다.
- ⛔ 회수 필요: `wiki/product/studio.md`의 일부 장문과 소유권 설명이 2026-08-15 제품구조 결정과 어긋날 수 있다. 정본을 바꾸지 말고 별도 wiki 컴파일에서 정리해야 한다.

### 23.2 다음 stage 가능 여부

현재 산출물은 회장과 티키타카할 저충실 기반으로 사용 가능하다.

프로토타입 stage 진입은 아직 불가하다. D-08부터 D-10 결정, 계정 전환 기술 가능성 확인, 비용과 시간 데이터 소유자 합의, DESIGN.md 최신화가 종료 증거다.

## 24. SOURCES / MODEL / SKILLS

### SOURCES

- `/Users/sj/sj_code_master/openclaw-auto/docs/제품구조-결정-2026-08-15.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prd-openclaw-service-v8.1.1-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/DESIGN.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/v24-brief.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/openclaw-auto-marketing-agent-fidelity-v24-gpt-codex.html`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/`
- `/Users/sj/sj_code_master/openclaw-auto/docs/user-flow.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/구현현황.md`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/lib/channel-capabilities.ts`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/ChannelTabs.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/AccountManager.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/SocialConnectButton.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/studio/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/studio/SchedulePanel.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/studio.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/marketing-hub-surface-map.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/reference/channel-status.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/decisions/004-social-connect-oauth-not-passwords.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/architecture/data-model.md`
- [Buffer 채널 연결](https://support.buffer.com/article/564-connecting-your-channels-to-buffer)
- [Buffer 다채널 예약](https://support.buffer.com/article/644-how-do-i-schedule-posts-for-multiple-social-channels-at-the-same-time)
- [Later 연결 상태](https://help.later.com/hc/en-us/articles/1500003115942-Social-Profile-Connection-Statuses)
- [Later 계정 추가와 제거](https://help.later.com/hc/en-us/articles/360043244733-Adding-Removing-Transferring-Social-Profiles-in-Later)
- [Google OpenID Connect](https://developers.google.com/identity/openid-connect/openid-connect)
- [Vrew 텍스트로 비디오 만들기](https://vrew.ai/ko/feature/text-to-video/)
- [Markety](https://markety.co.kr/)
- [Sigmine](https://sigmine.ai/)
- `/Users/sj/.claude/standards/design.md`
- `/Users/sj/.claude/standards/doc-review.md`
- `/Users/sj/.claude/standards/benchmarks.md`
- `/Users/sj/.claude/standards/artifact-stamp.md`

### MODEL

MODEL: gpt-codex/gpt-5.6-sol

### SKILLS

SKILLS_USED: 없음. 현재 설치된 스킬 중 저충실 제품 유저플로우와 상태 설계를 직접 수행하는 매칭 스킬이 없었다.

SKILLS_SKIPPED: `openclaw-creative-brief`는 자동 생성 에이전트용 창작 브리프 스킬로 이번 제품 화면 흐름 설계와 범위가 달라 스킵했다. `imagegen`은 이미지 생성이 금지된 문서 과제라 스킵했다.

## 25. v9.1 운영자 화면 원칙과 정보구조

### 25.1 신규 범위의 기준

`docs/제품구조-결정-2026-08-15.md` 3.95절은 운영자 화면을 고객 화면의 부록이 아니라 서비스 운영의 필수 경로로 정의한다. v9.1은 다음 여덟 범위를 추가한다.

1. 회원과 테넌트 관리
2. 크레딧, 결제, 환불, 잔액 조정
3. 사용량과 원가
4. 장애 알람
5. 유저가 막힌 지점 조회
6. 운영 루프 집계 조회
7. 유저 지원 요청 처리
8. 장애 조치와 고객 복구 결과의 감사 이력

핵심 원칙은 `자동으로 눈에 띄지 않으면 대응이 늦는다`다. 운영자는 여러 메뉴를 순회해 이상을 찾지 않는다. 시스템이 이상을 운영 홈의 첫 번째 결정으로 올리고, 운영자는 영향, 원인 후보, 보존된 것, 다음 행동을 한 흐름에서 확인한다.

### 25.2 기존 구현 확인과 확장 경계

| 현재 `/operator/customers`와 운영자 셸에서 관찰된 기능 | v9.1 처리 |
|---|---|
| 고객 셸과 분리된 `Admin` 전용 사이드바 | 유지. 고객 화면과 섞지 않는다. |
| 가입자, 워크스페이스, 활성 상태, 연결 계정, 발행, 실패의 6개 요약 | O-00 운영 홈의 기초 지표로 유지하되 장애 우선순위를 위에 추가 |
| Auth 가입자 목록, 이메일 확인, 최근 로그인 | O-03 회원·테넌트 검색과 필터로 확장 |
| 사용자 정지·재개, 공유 AI 승인·회수 | O-04 테넌트 상세의 기존 조치로 보존 |
| 워크스페이스별 연결 앱, 사용 이벤트, 초안, 발행, 실패, 생성, 쇼츠 | O-04 상세와 O-07 사용량·원가의 진입점으로 보존 |
| 중앙 OAuth 개발자 앱, 마스킹, 30초 원문 확인, 전체 세트 저장 | 기존 보안 경계 그대로 유지. O-01 장애에서 자격증명 저장소 장애로 연결 |
| 운영자 메뉴는 현재 `고객 관리` 한 개 | 기존 목적지는 유지하고, 승인 뒤 O-00부터 O-12를 같은 셸에 점진 추가 |

현재 코드에서 확인되지 않은 기능은 크레딧 원장 조정, 결제와 환불, 공급자 원가 집계, 단계 이탈 집계, 운영 알람, 지원 요청함이다. v9.1은 이들을 구현 완료처럼 표시하지 않고 `목표 화면`으로 구분한다.

### 25.3 정보 우선순위

운영 홈은 아래 순서로 편집한다.

1. 지금 놓치면 고객 작업이 멈추는 장애
2. 오늘 안에 처리해야 할 고객 지원과 과금 분쟁
3. 손실이 커지는 원가 이상과 큐 적체
4. 사용자가 반복해서 막히는 단계
5. 정상 운영 지표와 장기 학습

가입자 수나 누적 발행량은 정상 상태 요약이다. 미확인 치명 장애보다 위에 둘 수 없다. 경고가 0개일 때만 정상 지표가 첫 접힘선의 주인공이 된다.

### 25.4 운영자 셸 계약

| 영역 | 1024 이상 | 390 | 이유 |
|---|---|---|---|
| 셸 | 기존 224 사이드바 + fluid main | 상단 메뉴 + 단일 열 | v24 셸 계승 |
| 전역 위험 표시 | 사이드바 상단 `미확인 N` 배지 + 메인 위험 레일 | 상단 고정 위험 요약 1줄 | Fogg의 prompt와 system status 가시성 |
| 주요 탐색 | 운영 홈, 알람, 고객, 과금, 사용량·원가, 막힘, 운영 루프, 지원 | 같은 순서의 전체 화면 메뉴 | 위치 기억 보존 |
| 조치 | 상세 오른쪽 4열 조치 레일 | 상세 아래 sticky CTA | 한 화면 한 결정 |

사이드바 배지는 숫자만 보여주지 않는다. 색, `치명` 또는 `주의` 텍스트, 마지막 감지 시각을 함께 제공한다. 색만으로 상태를 전달하지 않는다.

### 25.5 알람 심각도와 상태

| 심각도 | 의미 | 화면 우선순위 | 운영자 행동 |
|---|---|---|---|
| 치명 | 여러 고객의 발행·생성 중단 또는 금전 오처리 가능 | 접힘선 최상단, 별도 외부 알림 필요 | 즉시 확인, 영향 범위 고정, 복구 시작 |
| 높음 | 한 고객 또는 한 채널의 핵심 흐름 중단 | 치명 아래, 같은 날 처리 | 고객·작업 상세에서 복구 |
| 주의 | 만료 임박, 실패율 상승, 원가 이상 조짐 | 기한순 정렬 | 예방 조치 또는 근거 있는 보류 |
| 정보 | 회복됨, 정책 변화 후보, 추세 관찰 | 기본 접힘 | 기록 확인 또는 닫기 |

알람 상태는 `미확인`, `확인함`, `처리 중`, `관찰 중`, `해결 확인`, `오탐` 여섯 가지다. `확인함`은 해결이 아니다. `해결 확인`은 상태가 정상으로 돌아온 관측 증거와 확인 시각이 있을 때만 허용한다. 수동 종료는 사유를 필수로 남긴다.

### 25.6 필수 장애 알람 계약

| 알람 | 요약에 반드시 보이는 것 | 상세에서 보는 것 | 즉시 행동 |
|---|---|---|---|
| 토큰 만료 임박 | 남은 기간, 영향 계정·예약 수, 마지막 정상 확인 | provider, 계정, 만료시각, refresh 가능 여부, 영향 작업 | 고객 재연결 안내, 영향 예약 보류, 다른 계정 전환 |
| 발행 실패율 급증 | 기준 기간 대비 현재 실패율, 영향 채널·테넌트 수 | 실패 코드 묶음, 최근 성공 시각, 제공자별 분포 | 실패 재현 금지, 안전한 상태 조회, 정상 채널 보호 |
| 채널 정책 변경 의심 | 새 실패 패턴, 최초 감지 시각, 정책 근거 상태 | 이전 성공 요청과 현재 실패의 차이, 공식 문서 확인 상태 | 관련 채널 신규 발행 경고, 정책 조사 시작 |
| 생성 실패율 | 실패 단계, 공급자, 영향 요청 수, 크레딧 처리 상태 | 요청 방식, 재시도 횟수, 보존된 중간 결과 | 실패 단계만 재시도, 공급자 전환 후보, 지원 연결 |
| 큐 적체 | 가장 오래된 대기, 대기 건수, 정상 처리율 대비 편차 | 테넌트·채널·우선순위별 큐, 처리 중 고착 항목 | 중복 실행 방지, worker 상태 확인, 안전한 재개 |

임계값 숫자는 기술설계와 운영 데이터 없이 확정하지 않는다. 화면에는 임계값 값, 계산 기간, 비교 기준, 마지막 계산 시각을 반드시 보여준다. 값이 없으면 `기준 미설정`으로 알리고 치명 등급을 임의로 만들지 않는다.

## 26. v9.1 운영자 화면 목록

| ID | 화면 | 목적 | 기존 또는 목표 | 다음 화면 |
|---|---|---|---|---|
| O-00 | 운영 홈 | 지금 대응할 장애와 오늘 처리할 일을 한눈에 판정 | 기존 요약 확장 | O-01, O-03, O-05, O-10 |
| O-01 | 통합 알람함 | 장애 5종을 심각도·미확인·기한순으로 정렬 | 목표 | O-02 |
| O-02 | 장애 상세·조치 | 영향, 타임라인, 근거, 보존 항목, 다음 행동 확인 | 목표 | O-04, A-08, P-06, O-10, O-01 |
| O-03 | 회원·테넌트 목록 | 회원, 워크스페이스, 상태, 연결, 최근 사용 검색 | 기존 확장 | O-04 |
| O-04 | 테넌트 상세 | 계정, 연결, 제작, 발행, 과금, 지원을 한 고객 단위로 조회 | 기존 확장 | O-05, O-07, O-08, O-10 |
| O-05 | 크레딧·결제·환불 | 잔액, 충전, 차감, 환불, 결제 상태, 최근 원장 조회 | 목표 | O-06 또는 O-04 |
| O-06 | 잔액 조정 확인 | 조정 전후, 사유, 증거, 고객 통지 여부를 확인 후 실행 | 목표 | O-05 |
| O-07 | 사용량·원가 | 테넌트·제작방식·공급자별 사용량과 원가, 마진 위험 조회 | 목표 | O-04, O-09 |
| O-08 | 유저 막힘 퍼널 | 질문·연결·생성·편집·발행 단계별 이탈과 실패 조회 | 목표 | O-04, O-02, O-09 |
| O-09 | 운영 루프 집계 | 제작 방식별 성과, 실패 프롬프트, 재시도율, 선택 결과 조회 | 목표 | C-04 근거 후보 또는 O-07 |
| O-10 | 지원 요청함 | 신규, 대기, 고객 답변 대기, 해결 요청을 기한순 처리 | 목표 | O-11 |
| O-11 | 지원 요청 상세 | 고객 맥락과 실패 이력을 보며 답변·복구·환불 분기 | 목표 | O-02, O-04, O-05, O-10 |
| O-12 | 알람 규칙·수신 | 임계값, 수신 경로, 조용한 시간, 테스트 알림 관리 | 목표 | O-01 |

### 26.1 O-00 운영 홈

첫 접힘선은 다음 네 블록으로 제한한다.

1. 미확인 치명·높음 알람과 가장 오래된 발생 시각
2. 오늘 기한이 지난 지원 요청
3. 오늘 금전 조치 대기와 원가 이상
4. 시스템 건강 요약과 마지막 계산 시각

정상 지표는 두 번째 블록부터 배치한다. 기존 6개 요약 카드는 `정상 운영 요약`으로 유지하되 치명 알람이 있으면 접힌다. 알람 0개일 때는 `현재 미확인 장애 0개, 마지막 확인 시각`을 보여주고 빈 빨간 영역을 남기지 않는다.

### 26.2 O-01 통합 알람함

- 기본 필터는 `미확인 + 처리 중`이다.
- 정렬은 심각도, 영향 고객 수, 오래된 순서다.
- 같은 근본 원인의 반복 이벤트는 한 사건으로 묶고 횟수와 최초·최근 시각을 표시한다.
- 목록 행은 알람 종류, 심각도, 영향 범위, 발생 시각, 마지막 변화, 현재 주인, 다음 행동을 한눈에 보여준다.
- 1인 운영 전제라 사람 배정 UI를 만들지 않는다. 소유자는 `운영자` 또는 `시스템 대기` 둘뿐이다.
- `모두 읽음`은 제공하지 않는다. 치명·높음 알람은 개별 확인이 필요하다.

### 26.3 O-02 장애 상세·조치

상단 질문은 하나다. `지금 고객 작업을 보호하려면 무엇을 해야 하는가?`

| 영역 | 내용 |
|---|---|
| 사실 | 감지 시각, 마지막 정상, 영향 고객·채널·요청, 현재 상태 |
| 근거 | 오류 묶음, 기준 대비 편차, 공식 정책 확인 여부, 데이터 신선도 |
| 보존 | 초안, 예약, 성공 채널, 크레딧, 외부 게시 결과 중 유지된 것 |
| 다음 행동 | 안전한 상태 조회, 재연결 안내, 실패 단위 재시도, 고객 알림, 지원 요청 연결 |
| 타임라인 | 감지, 확인, 조치, 관찰, 해결 확인. 역순 최신 기록이 위 |

재시도 버튼은 중복 실행 위험을 검사한 뒤에만 활성화한다. 외부 게시 여부가 불명인 `unknown` 상태는 재발행을 금지하고 reconcile만 허용한다.

### 26.4 O-03과 O-04 회원·테넌트

O-03 기본 열은 고객명, 상태, 연결 계정, 최근 사용, 미확인 장애, 지원 요청, 잔액이다. 이메일과 내부 ID는 검색 가능하지만 한 행에 모두 노출해 밀도를 높이지 않는다.

O-04는 기존 정보와 조치를 보존하고 아래 탭을 추가한다.

1. 개요
2. 연결·발행
3. 제작·사용량
4. 크레딧·결제
5. 막힘·지원
6. 감사 이력

탭 위치는 고정한다. 데이터가 없으면 탭을 숨기지 않고 empty state와 필요한 첫 행동을 보여준다. 정지·재개와 공유 AI 승인·회수는 `개요`의 위험 조치 영역에 유지한다.

### 26.5 O-05와 O-06 크레딧·결제·환불

O-05의 상단은 `사용 가능 잔액`, `보류`, `최근 차감`, `환불 대기`, `마지막 원장 반영`을 분리한다. 잔액과 사용량 데이터의 갱신 시각이 다르면 각각 표시한다.

원장 행은 발생시각, 유형, 증감, 이전 잔액, 이후 잔액, 관련 작업, 사유, 처리 주체를 보여준다. 생성 비용, 발행 재시도, 환불, 수동 보정은 같은 유형으로 합치지 않는다.

O-06은 의도된 마찰을 둔다.

1. 조정 유형 선택
2. 금액 또는 크레딧 입력
3. 관련 작업·결제·지원 요청 연결
4. 고객에게 보일 사유와 내부 사유 분리
5. 조정 전후 잔액 미리보기
6. 실행 확인
7. 성공 뒤 원장 행과 감사 ID 표시

조정 실패 시 중복 제출을 막고 상태 재조회만 허용한다. 환불 가능 범위와 승인 권한, 결제 제공자 계약은 제품·기술 합의 전 확정하지 않는다.

### 26.6 O-07 사용량과 원가

- 고객 크레딧과 공급자 원가를 같은 숫자로 표현하지 않는다.
- 기간, 테넌트, 제작 방식, 공급자, 성공·실패를 필터로 제공한다.
- 핵심 지표는 사용 크레딧, 공급자 원가, 성공 건당 원가, 재시도 원가, 실패 원가다.
- 집계 수치마다 계산 기간, 포함 범위, 마지막 집계 시각을 표시한다.
- 원가 급증은 총액만으로 판단하지 않고 성공 건당 원가와 재시도율을 함께 본다.
- 과다 데이터는 상위 위험 8개를 먼저 보여주고 전체는 검색·필터·내보내기로 연다.

### 26.7 O-08 유저가 막힌 지점

질문, 연결, 생성, 편집, 발행을 같은 퍼널로 합치지 않는다. 각 흐름별로 진입, 완료, 반복, 이탈, 오류를 분리한다.

| 흐름 | 먼저 보는 지표 | 상세 진입 |
|---|---|---|
| 질문 엔진 | 단계별 이탈, 추천 거절, 자유입력 전환, A/B/C 재생성 | 해당 세션의 선택·근거 이력 |
| 계정 연결 | 제공자 이동, 콜백, 실제 계정 확인, 오계정 보류, 재연결 | A-02부터 A-08 이벤트 타임라인 |
| 생성 | 단계 실패, 재시도, 크레딧 판정, 보존 결과 | C-07과 공급자 오류 |
| 편집 | 모호 지시, 유과금 전환, revision 미채택 | E-01부터 E-03 |
| 발행 | 사전 차단, 전송 중, 확인 중, 실패, 복구 | P-03부터 P-06 |

이탈은 실패로 단정하지 않는다. 사용자가 저장 후 정상 종료했는지, 브라우저를 닫았는지, 오류 뒤 멈췄는지를 분리한다.

### 26.8 O-09 운영 루프

운영 루프는 개인 취향 프로필과 분리된 집계만 사용한다.

- 제작 방식별 성과와 표본
- 프롬프트 유형별 실패율
- 재시도 횟수와 이유
- 추천 채택·거절·반박 수용
- 비용 범위 예측과 실제 차이
- 결과 수정 종류와 미채택 이유

개별 고객을 식별할 수 있는 원문을 기본 집계 화면에 노출하지 않는다. 추천 엔진에 돌려보낼 후보는 `우리 실험 로그`, 표본, 기간, 불확실성을 갖춘 경우만 승인 후보가 된다.

### 26.9 O-10과 O-11 유저 지원

지원 요청은 고객이 `지원` 메뉴에서 새로 만들거나 C-07, A-08, P-06의 오류 화면에서 현재 맥락을 붙여 보낸다. 오류 화면에서 보낼 때 아래 정보를 자동 포함한다.

- 테넌트와 사용자 식별자
- 화면 ID와 요청 ID
- 발생 시각과 마지막 정상 행동
- 오류 분류와 보존된 항목
- 이미 시도한 복구 행동
- 비밀값을 제거한 환경·provider 정보

O-10 정렬은 미확인, 응답 기한 초과, 결제·발행 중단, 오래된 순서다. O-11은 고객 메시지와 시스템 사실을 시각적으로 분리한다. 운영자는 답변, 복구 조치, 환불 검토, 고객 답변 대기, 해결 확인 중 하나를 선택한다. 답변만 보냈다고 해결로 닫지 않는다.

### 26.10 O-12 알람 규칙·수신

각 알람은 활성 여부, 임계값, 계산 기간, 지연 허용, 수신 경로, 테스트 시각을 가진다. 치명 알람은 대시보드 밖 수신 경로가 최소 하나 필요하다. 이메일, Slack, 문자 중 어느 제공자를 쓸지는 기술설계와 비용 합의로 회수한다.

조용한 시간에도 치명 알람은 차단하지 않는다. 테스트 알림은 실제 사건과 명확히 구분하고 마지막 성공·실패 시각을 남긴다.

## 27. v9.1 운영자 핵심 흐름과 상태

### 27.1 장애 발견과 복구 Happy 경로

`시스템 감지` -> `O-00 위험 레일` -> `O-01 알람함` -> `O-02 장애 상세` -> `영향 고객 O-04 또는 고객 복구 A-08/P-06` -> `관찰 중` -> `정상 신호 확인` -> `해결 확인`

| 단계 | 운영자가 보는 것 | 행동 | 보존·안전장치 |
|---|---|---|---|
| 감지 | 알람 종류, 심각도, 영향, 최초 시각 | 알람 열기 | 중복 사건 묶기 |
| 확인 | 사실, 근거, 보존된 것, 다음 행동 | `확인하고 처리 시작` | 확인을 해결로 표시하지 않음 |
| 조치 | 안전한 복구 후보 | 하나 선택 또는 지원 연결 | 외부 상태 unknown이면 재발행 금지 |
| 관찰 | 상태 변화, 실패율, 큐 감소 | 계속 관찰 또는 추가 조치 | 계산 시각 표시 |
| 종료 | 정상 신호와 확인 시각 | 해결 확인 | 수동 종료는 사유 필수 |

### 27.2 과금 조정 Happy 경로

`O-04 고객 상세` -> `O-05 원장` -> `O-06 조정 유형` -> `관련 증거 연결` -> `전후 잔액 확인` -> `실행` -> `원장·감사 ID 확인` -> `필요 시 O-11 고객 안내`

### 27.3 지원 요청 Happy 경로

`고객 오류 화면 또는 지원 메뉴` -> `O-10 신규 요청` -> `O-11 사실·메시지 대조` -> `O-02/O-04/O-05 복구` -> `고객 답변` -> `고객 또는 운영자 해결 확인` -> `O-10 종료`

### 27.4 공통 Edge 경로

| 조건 | 처리 | 탈출구 |
|---|---|---|
| 같은 장애가 여러 테넌트에서 반복 | 하나의 사건과 영향 테넌트 목록으로 묶기 | 개별 고객 상세와 전체 사건 사이 왕복 |
| 알람이 회복됐다가 재발 | 새 사건으로 숨기지 않고 이전 사건에 재발 표시 | 재개 또는 별도 근본원인 사건 분리 |
| 운영자가 이미 외부에서 조치 | 수동 조치 시각·사유 기록, 상태 재조회 | 정상 확인 또는 관찰 중 |
| 원장 반영 응답이 시간 초과 | 성공·실패 단정 금지 | 최신 잔액과 idempotency 상태 재조회 |
| 고객 메시지와 시스템 기록이 충돌 | 양쪽 사실을 병렬 표시 | 추가 증거 요청, 사건 연결 |
| 한 고객이 요청을 여러 개 보냄 | 자동 병합하지 않고 연관 요청 후보 표시 | 운영자가 병합 또는 분리 |

### 27.5 공통 Empty 경로

| 화면 | 빈 상태 | 다음 행동 |
|---|---|---|
| O-00/O-01 | `미확인 장애가 없습니다`와 마지막 점검 시각 | 정상 지표 보기, 알람 테스트 |
| O-03 | 조건에 맞는 회원·테넌트 없음 | 필터 초기화 |
| O-05 | 원장 없음 | 결제 상태 확인 또는 이벤트 수집 점검 |
| O-07 | 해당 기간 사용량 없음 | 최근 데이터 기간으로 이동 |
| O-08 | 계측 데이터 없음 | 계측 범위와 첫 수집 시각 확인 |
| O-09 | 집계할 표본 부족 | 최소 표본 전까지 가설로 유지 |
| O-10 | 처리할 지원 요청 없음 | 최근 해결 요청 보기 |

### 27.6 공통 Error 경로

| 오류 | 화면 표현 | 복구 |
|---|---|---|
| 알람 집계 실패 | 마지막 저장된 알람과 `현재 갱신 실패` 분리 | 다시 조회, 수신 테스트 |
| 고객 상세 일부 실패 | 정상 블록 유지, 실패 블록만 error | 해당 블록 재조회 |
| 원장 조회 실패 | 잔액 조정 버튼 비활성 | 원장 복구 후 재조회 |
| 잔액 조정 실패 | 입력과 사유 보존, 처리 여부 미확정 표시 | 상태 재조회, 중복 실행 금지 |
| 원가 집계 실패 | 마지막 정상 집계와 실패 시각 | 재집계 요청, 원시 사용량 확인 |
| 지원 답변 전송 실패 | 답변 초안 보존 | 다시 전송, 복사, 내부 메모 전환 |

### 27.7 공통 Loading 경로

- O-00은 전체 페이지를 비우지 않고 마지막 정상 스냅샷과 갱신 중 배지를 함께 보여준다.
- O-01은 행별 상태 갱신을 허용하고 목록 필터를 막지 않는다.
- O-04는 탭별 skeleton을 사용하며 고객명과 상태는 유지한다.
- O-06은 실행 중 같은 조정의 재제출을 비활성화한다.
- O-07과 O-09는 집계 중 기간·필터와 이전 결과를 유지한다.
- O-11은 답변 전송 중 시스템 이력 탐색을 허용한다.

### 27.8 운영자 dead-end 방지

모든 운영자 화면은 다음 중 하나를 제공한다.

- 이전 목록으로 돌아가기
- 고객 상세로 이동
- 장애 상세로 이동
- 지원 요청으로 연결
- 안전하게 보류하고 관찰 상태로 전환
- 변경 없이 종료

오류가 계속돼도 원본 고객 콘텐츠, 성공 채널, 기존 잔액, 이전 감사 이력을 삭제하지 않는다.

## 28. v9.1 질문 엔진 화법 준수 감사

### 28.1 적용 규칙

질문 엔진에 해당하는 화면은 네 슬롯을 모두 갖는다.

1. 선택지
2. 추천
3. 추천 근거
4. 자유입력 보조

질문이 아니라 상태 확인, 실행 확인, 보안·과금 승인인 화면은 자유입력을 기본 경로로 강제하지 않는다. 단, 사유 입력이 필요한 고위험 조치는 별도 의도된 마찰로 둔다.

### 28.2 화면별 준수 점검표

| 화면 | 선택지 | 추천 | 근거 | 자유입력 보조 | 판정·보정 |
|---|---|---|---|---|---|
| G-00 시작 방식 | `제작부터`, `채널 연결 후 시작` | 제작부터 | 연결 없이도 첫 가치를 볼 수 있음 | `내 상황 설명` | 충족 |
| C-01 업계·목적 | 업계·목적 카드 | 최근 작업과 저장 정보 기반 조합 | 우리 로그 또는 저장 정보 | 직접 업계·목적 입력 | 충족 |
| C-02 사례 | 공개 사례 3개에서 6개 | 목적 적합 사례 묶음 | URL, 관찰일, 관찰 포인트 | 참고 링크 붙이기 | v9.1 보정: 자유 링크 입력을 명시 |
| C-03 스타일·제작 방식 | 스타일 3개에서 5개 | 비용·시간·품질 우선안 | Studio 견적과 가설 | 원하는 스타일 설명·링크 | v9.1 보정: 자유 설명과 링크를 보조로 명시 |
| C-04 추천 확정 | 도달·전환·비용 우선 | 복수 추천 중 목적 적합 1안 강조 | 선택 이력, 사례, 가설 구분 | 다른 방향 적기 | 충족 |
| C-05 A/B/C | 방향 A, B, C | 목적 적합 1안 | 앞선 선택과 근거 요약 | 후보에 없는 방향 적기 | v9.1 보정: 재생성 외 직접 방향 입력 추가 |
| C-06 생성 전 확인 | 생성, 이전 단계 수정, 저장 종료 | 현재 방향으로 생성 | 선택·비용·시간·결과 범위 | 수정 메모 | 확인 화면이지만 네 슬롯 유지 |
| E-01 모호한 수정 해석 | 해석 2개에서 3개 | 영향이 가장 작은 해석 | 바뀌는 컷과 크레딧 | 원문 수정 지시 | 충족 |
| A-02 연결 의도 | 기존, 추가, 재연결 | 현재 상태에 맞는 1안 | 계정 수, 만료, 작업 영향 | 다른 상황 설명 | v9.1 보정: 지원 연결용 상황 설명 추가 |
| P-04 예약 방식 | 공통 시각, 채널별 시각, 지금 발행 | 목적과 채널 상태에 맞는 안 | 시간대, 충돌, 과거 성과 | 직접 시각 입력 | 충족 |
| R-03 다음 실험 | 효과·비용·근거가 다른 3안 | 표본이 허용할 때 1안 | 우리 로그, 공개 사례, 가설 | 직접 실험 적기 | 충족 |

### 28.3 반박 화법 점검

추천과 사용자 선택이 목적, 비용 상한, 채널 정책, 권리 범위와 충돌하면 화면은 다음 순서로 말한다.

1. 사용자가 고른 것을 정확히 재진술한다.
2. 충돌하는 목적이나 제약을 한 문장으로 말한다.
3. 대안 두 개에서 세 개를 제시한다.
4. 추천안과 근거를 붙인다.
5. 그래도 원안을 유지하거나 다른 생각을 적을 길을 연다.

예시: `TikTok용 90초 영상을 고르셨습니다. 현재 선택한 목표는 첫 방문 도달이라 첫 장면 이탈 위험이 큽니다. 30초 압축안을 추천합니다. 같은 목적의 공개 사례에서 짧은 시작 구조가 반복됐기 때문입니다. 90초를 유지하거나 원하는 길이를 직접 적을 수도 있습니다.`

### 28.4 질문 화면 금지 패턴

- 빈 textarea 하나와 `다음`
- 추천 하나를 사실처럼 강제
- `AI 추천`만 있고 근거·표본·시점 없음
- 자유입력만 크게 두고 선택지를 접음
- 사용자 선택과 충돌해도 조용히 덮어씀
- 질문 한 화면에 주요 선택 7개 이상을 동등 위계로 노출
- 추천 근거와 광고성 카피를 구분하지 않음

## 29. v9.1 벤치마크 근거와 설계 반영

### 29.1 실조사 결과

| 범주 | 공식 출처와 확인 사실 | 차용 | 그대로 베끼지 않은 것 |
|---|---|---|---|
| 다계정 연결 | [Buffer Instagram 연결](https://support.buffer.com/en-us/articles/connecting-your-instagram-account-to-buffer-n9Ad6veXsu)은 Buffer 안에 계정 선택기가 없고 현재 브라우저에서 활성인 계정이 연결될 수 있다고 명시한다. 잘못된 계정 방지를 위해 로그아웃·재로그인과 비공개 창을 안내한다. | A-03 연결 전 세션 점검, A-05 돌아온 실제 계정 확인, A-06 오계정 후보 보류 | 도움말만 던지지 않는다. 기존 기본 계정과 예약을 보호하고 확인 전 활성화하지 않는다. |
| 복수 채널 | [Buffer 채널 연결](https://support.buffer.com/en-us/articles/connecting-your-channels-to-buffer-HvWLgAJvL9)은 같은 네트워크의 복수 계정을 연결할 수 있지만 정확한 계정으로 미리 로그인해야 하며 계정 선택기가 없다고 설명한다. | 플랫폼·계정·테넌트를 분리한 식별, 복수 계정 수와 기본 계정 표시 | 플랫폼 아이콘만으로 계정을 식별하지 않는다. 표시명 중복 시 외부 ID 일부를 함께 표시한다. |
| 질문 온보딩 | [Stitch Fix UX 연구](https://multithreaded.stitchfix.com/blog/2016/11/30/us-design-capture-style-preferences-during-sign-up/)는 24개 이미지 평가가 선호 폭을 잡는 데 유용했지만 모바일 결정 피로와 이탈을 연구 질문으로 두고 실제 인터랙티브 프로토타입으로 검증했다. | 말로 스타일을 쓰게 하기보다 예시를 고르게 하고, 데이터가 많으면 목적별 묶음과 점진 공개 사용 | 24개를 그대로 노출하지 않는다. 한 화면 주요 선택은 3개에서 5개, 추가 사례는 더 보기로 제한한다. |
| 취향 학습 | [Spotify 취향 온보딩](https://newsroom.spotify.com/2018-08-27/5-things-free-users-need-to-be-taking-advantage-of-on-spotify/)은 가입 뒤 좋아하는 아티스트 5명 이상을 고르게 하고, 이후 like·hide 행동으로 추천을 계속 조정한다. | 초기에 선택으로 시작하고 이후 채택·거절·수정 행동을 학습신호로 사용 | 좋아요 수 하나로 취향을 단정하지 않는다. 선택, 수정, 성과를 분리한다. |
| 크레딧 잔액 | [OpenAI 크레딧 도움말](https://help.openai.com/en/articles/12642688-using-credits-for-flexible-usage-in-chatgpt-free-go-plus-pro-sora)은 Usage Dashboard에서 잔액과 최근 사용을 함께 보여주고, 자동 충전의 최소 잔액·목표 잔액·월 상한을 구분한다. 동시 사용으로 음수 잔액이 생길 수 있음도 밝힌다. | O-05 잔액과 최근 원장 동시 표시, 보류·음수·갱신 시각 분리, O-06 전후 잔액 확인 | 자동충전을 기본 활성화하거나 환불 가능성을 추정하지 않는다. 정책은 별도 합의로 남긴다. |
| 관리자 사용량 | [OpenAI Global Admin Console](https://help.openai.com/en/articles/12289294-global-admin-console)은 사용량을 사용자·제품·계량 항목·모델로 내려보고, 데이터가 1시간에서 6시간 지연될 수 있다고 명시한다. | O-07 다축 drill-down과 각 수치의 신선도 표시 | 사용자 순위를 성과 평가처럼 사용하지 않는다. 원가 위험과 지원을 위한 진단에 한정한다. |
| 발행 실패 알림 | [Buffer 알림 설정](https://support.buffer.com/en-us/articles/enabling-email-notifications-in-buffer-kuKhKBU7c3)은 발행 실패와 채널 연결 해제를 별도 이메일 알림으로 제공한다. | 장애 종류 분리, 치명 알람의 대시보드 밖 수신 경로 요구 | 모든 이벤트를 즉시 알리지 않는다. 심각도와 기한으로 묶어 알람 피로를 줄인다. |
| 만료 복구 | [Buffer 연결 유지](https://support.buffer.com/en-us/articles/best-practices-for-keeping-your-social-channels-connected-UgsmoudZJj)는 조치가 필요한 채널을 Settings 목록 최상단에 올린다. [Buffer 채널 새로고침](https://support.buffer.com/en-us/articles/refreshing-a-channel-in-buffer-7oDS4jk7l1)은 연결 갱신 뒤 실패 게시물을 자동 재시도하지 않고 사용자가 명시적으로 다시 넣거나 즉시 재시도하게 한다. 예약 콘텐츠는 보존된다. | O-00/O-01 예외 우선, A-08 복구 뒤 P-06 명시적 재시도, 성공·예약 보존 | 만료 복구가 성공했다고 실패 발행을 자동 재실행하지 않는다. |

### 29.2 국내 검색 결과의 사용 경계

한국어와 `국내`를 포함한 추가 검색을 수행했다. 공개 검색에서는 제품 내부 화면을 직접 검증할 수 있는 국내 동종 공식 자료가 충분하지 않았다. 국내 자료의 일반론을 화면 계약의 근거로 과장하지 않고, 현재 구현과 공식 글로벌 제품 자료를 주 근거로 유지한다.

### 29.3 벤치마크가 바꾼 우선순위

1. 알람은 별도 메뉴 안에만 두지 않고 운영 홈과 사이드바에 함께 노출한다.
2. 만료·실패 항목은 정상 목록 위로 올린다.
3. 복구 뒤 자동 재시도하지 않고 영향 작업을 다시 확인한다.
4. 크레딧 잔액은 최근 사용, 보류, 데이터 시각과 함께 본다.
5. 질문은 자유입력보다 예시 선택을 먼저 보여주되 모바일에서 한 화면 선택 수를 제한한다.

## 30. v9.1 반응형·밀도·overflow 계약

### 30.1 1024 이상

| 화면군 | 주축 | 열수 | 정보 배치 |
|---|---|---|---|
| O-00 | column 안의 12열 grid | 위험 8 + 오늘 할 일 4 | 위험이 먼저, 정상 지표는 아래 |
| O-01 | column | 목록 8 + 필터·요약 4 | 목록이 주축, 필터 sticky 가능 |
| O-02 | row | 사실·타임라인 8 + 조치 4 | 조치는 본문을 가리지 않음 |
| O-03 | column | 검색·필터 12, 목록 12 | 테이블 또는 밀도 높은 행 |
| O-04 | column | 탭 12, 본문 8 + 위험 조치 4 | 탭 위치 고정 |
| O-05/O-07/O-09 | column | 차트·원장 8 + 요약 4 | 상세 수치가 주축 |
| O-08 | column | 흐름 8 + 필터 4 | 흐름별 퍼널을 섞지 않음 |
| O-10/O-11 | row | 요청 5 + 상세 7 | 선택한 요청 상세 유지 |

### 30.2 390

- 모든 운영자 화면은 column 1열이다.
- 위험 요약, 핵심 사실, 다음 행동, 상세 근거 순서다.
- O-01 행은 제목, 심각도, 영향, 시각, 다음 행동 순으로 세로 배치한다.
- O-03 테이블은 카드 행으로 바꾸고 내부 ID와 보조 메타는 `상세 보기`에 둔다.
- O-05 원장은 한 행을 시각, 유형·증감, 이후 잔액, 관련 작업 순으로 2단 이상 줄바꿈한다.
- O-10/O-11은 목록과 상세를 동시에 압축하지 않고 별도 화면으로 이동한다.
- sticky CTA는 16 안전 여백을 확보하고 마지막 행을 가리지 않는다.

### 30.3 spacing·정렬

- spacing은 기존 `4, 8, 12, 16, 24, 32, 48`만 사용한다.
- 페이지 좌우 inset은 기존 v24와 맞춘다.
- 카드 내부 16 또는 24, 행 간 8 또는 12, 큰 섹션 24 또는 32다.
- 위험 배지, 상태 배지, 시간 표시는 모든 운영 화면에서 같은 열 순서를 쓴다.
- 운영자 신규 컴포넌트는 DESIGN.md v24.2 inventory와 state contract에 반영했다. prototype은 이를 그대로 상속한다.

### 30.4 데이터량 0·정상·과다

| 데이터 | 0 | 정상 | 과다 |
|---|---|---|---|
| 알람 | 마지막 점검 시각과 테스트 | 미확인 1개에서 8개 | 사건 묶음, 심각도 필터, 최신 50행, 페이지 |
| 테넌트 | 필터 초기화 | 1개에서 20개 | 검색, 상태·장애 필터, 페이지 |
| 원장 | 데이터 소스 점검 | 최근 8개에서 20개 | 기간·유형 필터, 내보내기, 페이지 |
| 사용량·원가 | 최근 데이터 기간 이동 | 핵심 5지표와 상위 8개 | 집계 우선, drill-down, 내보내기 |
| 막힘 이벤트 | 계측 상태 안내 | 흐름별 단계 5개에서 8개 | 집계 우선, 세션 상세는 선택 후 로드 |
| 지원 요청 | 최근 해결 보기 | 신규 1개에서 20개 | 상태·기한 필터, 페이지, 검색 |
| 장애 타임라인 | 감지 이벤트만 | 최근 8개 | 최신 20개 우선, 이전 기록 접기 |

### 30.5 Overflow 수용기준

- 한글 60자 고객명, 120자 오류 요약, 64자 외부 계정명, 긴 요청 ID에서도 페이지 가로 넘침 0.
- URL과 식별자는 `overflow-wrap:anywhere`와 전체 복사를 제공한다.
- 숫자는 단위를 잘라 다음 줄로 보내지 않는다. 좁은 폭에서는 라벨과 값 묶음 전체가 줄바꿈한다.
- 50개 알람, 200개 테넌트, 1,000개 원장 행을 한 DOM 목록으로 모두 펼치지 않는다.
- 미확인 치명 알람은 필터·페이지 이동 중에도 전역 배지에서 사라지지 않는다.
- 차트가 없어도 표와 요약만으로 같은 판단을 할 수 있어야 한다.

## 31. v9.1 추적성·dead-end 재감사

### 31.1 신규 요구 추적

| 제품구조 3.95 요구 | 화면 | 상태 | 판정 |
|---|---|---|---|
| 회원·테넌트 관리 | O-03, O-04 | happy, edge, empty, error, loading | 충족 |
| 크레딧·결제·환불·잔액 조정 | O-05, O-06 | 조회, 보류, 실패, 중복 방지 | 흐름 충족. 정책·권한 미정 |
| 사용량과 원가 | O-07 | 0, 정상, 과다, 집계 실패 | 충족 |
| 토큰 만료 임박 | O-00, O-01, O-02, A-08 | 예방, 만료, 복구, 재확인 | 충족 |
| 발행 실패율 급증 | O-00, O-01, O-02, P-06 | 감지, 영향, 안전 조회, 복구 | 충족 |
| 채널 정책 변경 의심 | O-01, O-02, P-03, P-06 | 근거 없음, 조사 중, 확인 | 충족 |
| 생성 실패율 | O-01, O-02, C-07 | 단계 실패, 보존, 재시도 | 충족 |
| 큐 적체 | O-00, O-01, O-02 | 경고, 고착, 감소, 회복 | 충족 |
| 유저가 막힌 지점 | O-08, O-04 | 이탈·오류·정상 종료 분리 | 충족 |
| 운영 루프 집계 | O-09 | 표본 부족, 정상, 과다, 실패 | 충족 |
| 유저 지원 요청 | O-10, O-11 | 신규, 처리, 고객 대기, 해결 | 충족 |
| 자동으로 눈에 띄기 | 전역 배지, O-00 위험 레일, 외부 알림 요구 | 미확인, 치명, 테스트 실패 | 충족. 수신 제공자 미정 |

### 31.2 질문 화법 추적

제품구조 3.7의 `선택지 + 추천 + 근거 + 자유입력 보조`는 G-00, C-01부터 C-06, E-01 모호 해석, A-02, P-04, R-03에 매핑했다. v9에서 자유입력 보조가 명시되지 않았던 C-02, C-03, C-05, A-02는 v9.1에서 보정했다. 질문 화면 감사상 누락은 0이다.

### 31.3 운영자 화면 종료와 탈출구

| 화면군 | 성공 종료 | 보류·이탈 | 반복 오류 |
|---|---|---|---|
| 알람 | 해결 확인 | 관찰 중 | 지원·근본원인 사건으로 연결 |
| 고객 관리 | 상태 확인 또는 기존 조치 | 목록 복귀 | 감사 이력 보존, 지원 연결 |
| 과금 | 원장·감사 ID 확인 | 변경 없이 취소 | 상태 재조회, 중복 실행 금지 |
| 원가·루프 | 분석 저장 또는 종료 | 필터 유지 | 마지막 정상 집계 표시 |
| 막힘 | 고객·사건 상세 이동 | 퍼널 복귀 | 계측 오류 표시 |
| 지원 | 해결 확인 | 고객 답변 대기 | 초안 보존, 내부 메모·재전송 |

재감사 결과: 신규 O-00부터 O-12는 이전 목록, 안전한 보류, 관련 고객·장애·지원 상세 중 최소 하나를 가진다. 문서 수준 운영자 dead-end는 0이다.

## 32. v9.1 레드팀·셀프심문·품질 판정

### 32.1 레드팀 1: 1인 운영자의 공격

공격: 메뉴를 여덟 개 늘리면 운영자가 더 많은 화면을 돌아다녀야 한다. 알람 메뉴가 있어도 바쁠 때 안 열면 소용없다.

수정: O-00 첫 접힘선과 사이드바 전역 배지에서 미확인 위험을 먼저 드러낸다. 치명 알람은 대시보드 밖 수신 경로를 최소 하나 요구한다. 각 알람 상세에서 고객, 발행 복구, 지원, 과금으로 바로 이동한다.

### 32.2 레드팀 2: 까다로운 고객의 공격

공격: 운영자가 잔액을 마음대로 조정하거나 실패 게시물을 자동 재발행하면 돈과 브랜드 신뢰를 동시에 잃는다.

수정: O-06은 관련 증거, 고객용 사유, 전후 잔액, 실행 확인, 감사 ID를 요구한다. 연결 복구 뒤 실패 게시물은 자동 재시도하지 않는다. 외부 결과가 unknown이면 재발행을 막고 상태 조회만 허용한다.

### 32.3 레드팀 3: 경쟁자의 공격

공격: 알람 5종과 사용량 대시보드는 일반적인 운영 도구다. 제품 차별점이 아니다.

수정: 차별점은 알람 자체가 아니라 고객 질문 선택, 제작 방식, 수정, 채널 파생, 발행 결과, 성과를 한 사건과 운영 루프로 연결하는 데 둔다. O-08과 O-09는 단순 오류 수가 아니라 어디서 어떤 선택 뒤 막혔고 무엇이 성과·원가에 연결됐는지 보여준다.

### 32.4 셀프심문

질문: 이 결론이 틀렸다면 가장 그럴듯한 이유는 무엇인가?

답: 가장 load-bearing한 가정은 다섯 장애를 신뢰할 수 있게 감지하고 고객·요청·크레딧에 연결할 데이터가 존재한다는 것이다. 현재 구현에는 고객·발행·사용 이벤트 일부만 있고 임계값, 사건 묶음, 생성 원가, 질문 단계 이탈, 지원 요청 원장이 확인되지 않았다. 화면만 먼저 만들면 정확해 보이는 빈 운영 콘솔이 된다.

수정: 모든 신규 화면을 목표로 표시하고, 값마다 데이터 소스·계산 기간·마지막 계산 시각을 요구한다. 임계값이 없으면 `기준 미설정`, 집계가 실패하면 마지막 정상 스냅샷과 실패 상태를 분리한다. 기술설계 전에 이벤트·원장·사건 연결 가능성을 검증하도록 회수 항목을 추가한다.

두 번째 질문: 모든 질문 화면에 자유입력을 열면 다시 백지공포가 커지지 않는가?

답: 자유입력이 시각적으로 주 입력과 동등하면 그렇다.

수정: 자유입력은 선택 카드 아래의 보조 행동으로만 둔다. 먼저 3개에서 5개의 선택과 추천을 보여주고, 선택지에 없을 때만 연다. 직접 입력 뒤에도 해석 후보와 추천을 다시 제시해 백지 상태로 방치하지 않는다.

### 32.5 Design review

현재 Codex 세션의 등록 스킬과 호출 도구에는 `design-review`와 `design-consultation`이 노출되지 않았다. 로컬 Claude 경로의 `design-review/SKILL.md` 존재와 디자인 헌법은 확인했지만, 이를 등록된 Skill 호출로 위조하지 않는다. 따라서 아래 평가는 `design.md`의 동일 rubric을 적용한 정적 자체평가이며, 하네스의 `Skill call 1회` 조건은 미충족이다.

| 축 | 점수 | 근거 |
|---|---|---|
| 위계와 흐름 | A- | 운영 홈에서 위험, 오늘 할 일, 정상 지표 순서를 고정 |
| 기존 계승 | A | 실제 운영자 셸, 고객 관리, OAuth, v24 토큰을 보존 |
| 상태·복구 | A- | 운영 흐름의 happy, edge, empty, error, loading과 unknown 재실행 금지 |
| 질문 화법 | A- | 질문 화면별 네 슬롯 전수 감사, v9 누락 4곳 보정 |
| 반응형·overflow | B+ | 390·1024 주축과 0·정상·과다 계약, 실제 렌더 없음 |
| 벤치마크 | A- | 5회 검색과 공식 페이지 원문 확인, 차용·비차용 분리 |
| 실제 시각 검증 | 미검증 | 이번 과제는 Markdown 흐름 개정이며 prototype과 렌더를 만들지 않음 |

**Design Score: B+ (저충실 흐름 설계 리테이크, 등록 design 스킬 호출 미충족)**

### 32.6 문서 루브릭

| 항목 | 점수 | 근거 |
|---|---|---|
| 완결성 | 5/5 | 기존 24개 장 전량 유지, 운영자 8개 장 확장 |
| 정밀성 | 5/5 | O-00부터 O-12, 상태, 주축, 열수, 종료점 정의 |
| 근거 | 5/5 | 현재 코드·프로토타입·wiki·제품구조와 공식 UX 실조사 연결 |
| 추적성 | 5/5 | 3.7 질문 화법과 3.95 운영 범위를 화면·상태에 매핑 |
| 출고 완성도 | 4/5 | 실제 prototype 렌더와 등록 design 스킬 호출은 미검증 |

**RUBRIC_SCORE: completeness=5/5 precision=5/5 evidence=5/5 traceability=5/5 professionalism=4/5 total=24/25**

**WEAKEST_LINE:** 장애 임계값, 사건 묶음, 크레딧 원장 권한, 외부 알림 제공자는 실제 데이터와 기술설계 합의 전이라 화면 계약만으로 구현 가능성을 증명할 수 없다.

### 32.7 회수 필요와 다음 stage

- ⛔ 회수 필요: 치명 알람의 외부 수신 제공자와 비용, 수신 실패 백업 경로를 기술설계 전에 합의해야 한다.
- ⛔ 회수 필요: 토큰 만료, 실패율 급증, 생성 실패율, 큐 적체의 임계값과 계산 기간은 실제 baseline 데이터로 정해야 한다.
- ⛔ 회수 필요: 환불 가능 범위, 잔액 조정 권한, 고객 통지 의무, 발행 실패 크레딧 정책은 제품 결정이 필요하다.
- ⛔ 회수 필요: 질문 단계 이벤트, 사건 묶음, 공급자 원가, 지원 요청 원장의 구현현황이 없다. 기술설계 전 데이터 수집 가능성을 확인해야 한다.
- DESIGN.md 정합: `OperatorAlertRail`, `AlertRow`, `IncidentTimeline`, `BalanceLedger`, `BalanceAdjustmentReview`, `BlockageFunnel`, `SupportThread`를 DESIGN.md v24.2 component inventory와 state contract에 반영했다.
- ⛔ 회수 필요: `design-review` 또는 `design-consultation` 등록 Skill 실제 호출 1회와 Design Score B 이상 증거가 없다.

현재 문서는 design 단계 저충실 리테이크 후보로 회장 티키타카에 사용할 수 있다. prototype stage 진입은 운영·과금 결정, 데이터 가능성 확인, 등록 design 스킬 review B 이상 뒤에만 가능하다. DESIGN.md는 v24.2로 최신화했다.

### 32.8 개정 회귀 감사

- 원본 v9 최상위 장: 24개
- 개정 v9.1 최상위 장: 32개
- 원본 v9 줄 수: 959줄
- 개정본은 원본 전량을 유지한 뒤 운영자·질문 화법·벤치마크·상태·반응형·추적성 장을 추가했다.
- 유지한 기존 기능: 고객 제작·편집·연결·발행·성과·무연결 체험 6흐름, v24 셸, 7채널 미리보기, 다계정, capability 탭, D-08부터 D-10, 기존 SOURCES·MODEL·SKILLS.
- 추가·변경한 것: 운영자 O-00부터 O-12, 장애 5종, 과금·원가·막힘·운영 루프·지원, 질문 화면 감사, 공식 UX 근거, 운영자 390·1024 계약.
- 디자인 시스템 정합: `DESIGN.md` v24.2에 신규 운영 컴포넌트·상태·레이아웃·금지 패턴을 반영했다.

### 32.9 v9.1 추가 SOURCES / MODEL / SKILLS

SOURCES:

- `/Users/sj/sj_code_master/openclaw-auto/docs/design-docs/user-flow-openclaw-service-v9-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/제품구조-결정-2026-08-15.md` 3.7, 3.95
- `/Users/sj/sj_code_master/openclaw-auto/docs/구현현황.md`
- `/Users/sj/sj_code_master/openclaw-auto/DESIGN.md` v24, v24.1
- `/Users/sj/sj_code_master/openclaw-auto/DESIGN.md` v24.2
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/v24-brief.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/home-desktop-1024.png`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/studio-desktop-1024.png`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/settings-desktop-1024.png`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v22/v22-operator-1024.png`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/operator/customers/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/layout/Sidebar.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/studio/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/AccountManager.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/SocialConnectButton.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/lib/channel-capabilities.ts`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/studio.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/marketing-hub-surface-map.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/reference/channel-status.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/decisions/004-social-connect-oauth-not-passwords.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/architecture/data-model.md`
- `/Users/sj/.claude/standards/design.md`
- `/Users/sj/.claude/standards/doc-review.md`
- `/Users/sj/.claude/standards/benchmarks.md`
- `/Users/sj/.claude/standards/artifact-stamp.md`
- [Buffer Instagram 연결](https://support.buffer.com/en-us/articles/connecting-your-instagram-account-to-buffer-n9Ad6veXsu)
- [Buffer 채널 연결](https://support.buffer.com/en-us/articles/connecting-your-channels-to-buffer-HvWLgAJvL9)
- [Stitch Fix 질문 온보딩 UX 연구](https://multithreaded.stitchfix.com/blog/2016/11/30/us-design-capture-style-preferences-during-sign-up/)
- [Spotify 취향 온보딩](https://newsroom.spotify.com/2018-08-27/5-things-free-users-need-to-be-taking-advantage-of-on-spotify/)
- [OpenAI 크레딧 도움말](https://help.openai.com/en/articles/12642688-using-credits-for-flexible-usage-in-chatgpt-free-go-plus-pro-sora)
- [OpenAI Global Admin Console](https://help.openai.com/en/articles/12289294-global-admin-console)
- [Buffer 연결 유지](https://support.buffer.com/en-us/articles/best-practices-for-keeping-your-social-channels-connected-UgsmoudZJj)
- [Buffer 알림 설정](https://support.buffer.com/en-us/articles/enabling-email-notifications-in-buffer-kuKhKBU7c3)
- [Buffer 채널 새로고침](https://support.buffer.com/en-us/articles/refreshing-a-channel-in-buffer-7oDS4jk7l1)

MODEL: gpt-codex/gpt-5.6-sol

SKILLS_USED: 없음. `design-review`와 `design-consultation`은 로컬 Claude 스킬 파일로 존재하지만 현재 Codex 등록 스킬·호출 도구에 노출되지 않아 실제 Skill 호출을 수행할 수 없었다. 디자인 헌법과 동일 rubric의 정적 review는 수행했으나 Skill 호출로 주장하지 않는다.

SKILLS_SKIPPED: `imagegen`은 이미지 생성이 금지된 Markdown 유저플로우 과제라 스킵했다. `openclaw-creative-brief`는 자동 생성 에이전트용 브리프라 화면·플로우 설계와 범위가 다르다.

🏷 STAMP | line: openclaw-service | 생성: 2026-08-15 18:10 KST | model: gpt-codex/gpt-5.6-sol | agent: product-designer

skills: design-review 등록 호출 불가, design.md 정적 rubric review, WebSearch 5회 | 근거: v9 + 제품구조 3.7·3.95 + 현재 operator 구현 + 공식 UX 9건

고민: 운영 메뉴를 늘리는 대신 미확인 위험을 운영 홈의 첫 결정으로 올리고, 고위험 조치는 상태 확인과 감사 이력을 거쳐 실행하도록 설계했다.

## 33. v9.2 공식 벤치마크 근거 보강

### 33.1 개정 범위와 회귀 방지

v9.2는 v9.1의 화면 ID, 흐름 순서, 상태, 운영자 화면, 질문 화법, 반응형 계약을 변경하지 않는다. 이번 개정은 다음 네 근거 축을 공식 제품 자료로 다시 조사해 설계 이유와 적용 경계를 보강한다.

1. 여러 소셜 계정을 다루는 제품의 계정 전환과 계정 맥락 유지
2. 사용자가 답하기 쉬우면서 이후 행동으로 계속 학습하는 온보딩 질문
3. 크레딧 잔액, 최근 사용, 소진 경고, 자동 충전 설정
4. 발행 실패, 연결 만료, 권한 부족의 알림과 복구

신규 화면 ID, 신규 컴포넌트, 신규 토큰은 0개다. v9.1의 A-01부터 A-08, C-01부터 C-06, P-03부터 P-06, O-00부터 O-06, O-12에 근거를 추가했다. 따라서 `DESIGN.md`는 v24.2와 정합하며 이번 리테이크에서 추가 개정하지 않는다.

기존 구현과의 경계도 유지한다. `dashboard/src/components/channel/SocialConnectButton.tsx`는 provider 세션을 우리 origin이 지울 수 없다고 명시하고, `AccountManager.tsx`는 복수 계정과 기본 계정을 관리한다. `channel-capabilities.ts`와 `ChannelTabs.tsx`는 플랫폼별 가능한 탭만 같은 순서로 보여준다. v9.2는 이 구현 위에 근거를 얹으며 새로운 계정 선택기나 알림 인프라가 이미 구현됐다고 주장하지 않는다.

### 33.2 WebSearch 실호출 추적

2026-08-15에 WebSearch를 주제별로 분리해 다섯 번 호출하고, 공식 페이지 원문을 후속 WebFetch로 열었다. 검색 결과 페이지가 아니라 아래 공식 문서를 사실 근거로 사용했다.

| 호출 | 검색 목적 | 대표 검색어 | 채택한 공식 출처 | 확인 결과 |
|---|---|---|---|---|
| 1 | 다계정·계정 전환 | `Buffer Hootsuite Later multiple social accounts switch official` | Buffer 연결·Instagram·신규 대시보드, Later 프로필 관리 | 현재 브라우저 세션, 계정 식별, 채널별 맥락, 삭제 영향이 핵심 |
| 2 | 온보딩 질문 | `Stitch Fix Spotify onboarding questions official` | Stitch Fix UX 연구, Spotify Taste Onboarding | 선택지를 먼저 보여주고 실제 사용 행동으로 계속 학습 |
| 3 | 크레딧·잔액 | `credits balance usage dashboard auto recharge low balance official` | OpenAI credits·prepaid billing, Twilio usage trigger | 잔액과 최근 사용, 최소선, 목표선, 월 상한, 데이터 지연을 분리 |
| 4 | 발행 실패·토큰 만료 | `failed posts disconnected channels token expired official` | Buffer refresh·notifications, Later connection status | 만료와 권한 부족을 다른 상태로 표시하고 복구 뒤 수동 재시도 |
| 5 | Hootsuite 보강 | `Hootsuite account switching reconnect failed post official` | Hootsuite X 연결·이메일 알림 도움말 | 연결 전 provider 로그아웃과 실패·연결 해제 알림 분리 |

국내 검색어는 이번 네 축의 제품 내부 UX를 직접 설명하는 공식 동종 문서가 부족해 주 근거로 쓰지 않았다. v9.1의 국내 검색 경계는 그대로 유지한다. 현재 설계는 국내 관행을 일반화하지 않고 글로벌 동종 제품의 공식 도움말과 현재 코드만으로 판단한다.

### 33.3 계정 전환 UX 근거

#### 33.3.1 공식 제품에서 확인한 사실

| 제품·공식 URL | 확인한 사실 | v9.1에서 이미 대응한 화면 |
|---|---|---|
| [Buffer 채널 연결](https://support.buffer.com/en-us/articles/connecting-your-channels-to-buffer-HvWLgAJvL9) | 같은 네트워크의 복수 계정을 연결할 수 있다. 연결 전에 다른 탭에서 정확한 계정으로 로그인해야 하며, Buffer 자체 계정 선택기가 나오지 않는 경우 로그아웃 또는 비공개 창을 안내한다. | A-02 의도 선택, A-03 외부 세션 사전 점검, A-05 복귀 계정 확인 |
| [Buffer Instagram 연결](https://support.buffer.com/en-us/articles/connecting-your-instagram-account-to-buffer-n9Ad6veXsu) | 현재 브라우저에서 활성인 Instagram 계정이 연결된다. 잘못된 계정이 활성화돼 있으면 다른 계정 연결, 한 계정만 노출, 이미 연결됨 오류가 생길 수 있다. | A-03 현재 세션 확인, A-06 잘못 돌아온 계정 보류 |
| [Buffer 신규 대시보드](https://support.buffer.com/en-us/articles/navigating-buffers-new-dashboard-layout-JsRJX4s6QQ) | 왼쪽 사이드바에서 특정 채널을 고르면 그 채널이 지원하는 Publish·Community·Analytics 맥락만 보인다. 조직과 채널은 같은 사이드바에서 전환한다. | A-01 계정 상태 센터, A-07 계정 관리, P-03 현재 발행 계정 반복 표시 |
| [Later 프로필 추가·제거·이전](https://help.later.com/hc/en-us/articles/360043244733-Add-Remove-Transfer-Social-Profiles-in-Later) | 잘못된 브라우저 자동 로그인이 오계정 연결을 만들 수 있어 비공개 창을 안내한다. 제거 시 예약 게시물이 발행되지 않는 영향을 경고하고, 프로필 이전 시 예약과 분석 데이터를 함께 옮긴다. | A-06 후보 폐기, A-07 삭제 영향, P-03 예약 영향 확인 |
| [Hootsuite X 연결](https://help.hootsuite.com/hc/pt-br/articles/1260804308109-Connect-an-X-account) | X 연결 전에 별도 브라우저 탭에서 기존 X 세션을 로그아웃하도록 안내한다. 소셜 계정은 한 조직에 연결되며 여러 팀과 공유할 수 있다. | A-02 연결 의도, A-03 provider 세션 확인, A-07 테넌트·계정 경계 |

#### 33.3.2 차용, 변경, 비차용

| 구분 | v9.2 판정 |
|---|---|
| 차용 | 계정 전환 전에 현재 provider 세션을 먼저 확인한다. 복귀 뒤에는 플랫폼 아이콘만 보지 않고 표시명, handle, 외부 식별자 일부, 권한, 기본 계정 여부를 함께 확인한다. |
| 차용 | 선택한 채널과 계정 맥락을 사이드바, P-03 사전 점검, P-06 결과에 반복한다. 사용자가 화면을 옮겨도 발행 대상이 사라지지 않는다. |
| 변경 | Buffer처럼 도움말로 빠지게 두지 않고 A-03 안에서 provider별 세션 경계와 비공개 창 경로를 제시한다. A-05에서 실제 돌아온 계정을 승인하기 전 활성화하지 않는다. |
| 변경 | Later의 제거 영향 경고를 확장해 예약, 초안, 기본 계정, 성공 채널, 분석 연결 중 영향을 받는 항목을 수량과 이름으로 보여준다. |
| 비차용 | 모든 provider에 공통 계정 선택기가 있다고 가정하지 않는다. Google의 `select_account`와 Meta의 현재 세션 의존을 같은 기능으로 표현하지 않는다. |
| 비차용 | 잘못 돌아온 후보를 즉시 기본 계정으로 바꾸지 않는다. 기존 기본 계정과 예약은 A-06에서 보존한다. |

#### 33.3.3 화면 계약 보강

- A-01의 계정 행은 `플랫폼`, `표시명`, `@handle`, `외부 식별자 끝자리`, `기본 계정`, `연결 상태`, `마지막 확인`, `필요 행동` 순서를 유지한다.
- A-03은 사용자가 선택한 대상 계정과 현재 provider 세션이 일치하는지 확인한다. 일치 여부를 우리 앱이 읽을 수 없으면 `직접 확인 필요`로 표시하고 로그아웃을 수행했다고 주장하지 않는다.
- A-05는 OAuth 성공 여부와 계정 승인 여부를 분리한다. callback이 성공해도 사용자가 실제 계정을 확인하기 전에는 `연결 완료`가 아니다.
- A-06은 후보 연결을 보류 또는 폐기할 수 있어야 한다. 기존 기본 계정, 예약, 초안에는 변경을 만들지 않는다.
- A-07의 삭제는 영향을 받는 예약과 초안을 먼저 보여준다. 영향 조회가 실패하면 삭제 CTA를 비활성화하고 이유를 표시한다.
- P-03과 P-06은 `이번 작업에 쓰는 계정`을 반복한다. 같은 플랫폼 복수 계정에서 플랫폼 로고만으로 발행 대상을 대신하지 않는다.

UX 심리 근거는 Jakob의 법칙과 recognition over recall이다. 사용자는 provider 로그인 규칙을 외우지 않고 화면 안에서 현재 대상과 다음 행동을 인식해야 한다. 삭제와 기본 계정 변경은 손실회피가 작동하는 지점이므로 영향을 숨기지 않는 의도된 마찰을 둔다.

### 33.4 온보딩 질문 설계 근거

#### 33.4.1 공식 제품에서 확인한 사실

| 제품·공식 URL | 확인한 사실 | v9.1 적용 |
|---|---|---|
| [Stitch Fix 스타일 선호 온보딩 UX 연구](https://multithreaded.stitchfix.com/blog/2016/11/30/us-design-capture-style-preferences-during-sign-up/) | 24개 의상 이미지를 모바일에서 평가하는 1열과 2열을 실제 온보딩 맥락에서 비교했다. 12명 연구에서 대부분은 2열이 더 빠르고 결정하기 쉽다고 봤고, 1열은 스크롤과 세부 검토 부담이 커졌다. | C-02 시각 사례는 한 번에 비교 가능한 묶음으로 제시. 텍스트가 긴 카드에는 그대로 일반화하지 않음 |
| [Spotify Taste Onboarding](https://newsroom.spotify.com/2018-08-27/5-things-free-users-need-to-be-taking-advantage-of-on-spotify/) | 첫 로그인 뒤 좋아하는 아티스트 5명 이상을 고르게 해 초기 추천을 만들고, 이후 청취·좋아요·숨기기로 계속 개인화한다. | C-01부터 C-05의 초기 선택, 이후 채택·거절·수정·성과를 학습 신호로 분리 |

#### 33.4.2 차용, 변경, 비차용

| 구분 | v9.2 판정 |
|---|---|
| 차용 | 자유입력 전에 인식 가능한 사례와 선택지를 보여준다. 사용자는 추상적인 스타일 이름보다 결과 예시를 보고 고른다. |
| 차용 | 온보딩에서 완벽한 프로필을 만들려고 하지 않는다. 첫 결과를 만들 만큼의 신호만 받고, 이후 채택·거절·수정 행동으로 추천을 갱신한다. |
| 변경 | Spotify의 5명 이상 선택을 숫자 규칙으로 복제하지 않는다. OpenClaw는 각 단계의 다음 결정에 필요한 최소 선택 수만 요구하고 이유를 표시한다. |
| 변경 | Stitch Fix의 2열 결과는 이미지 중심 질문에만 적용 후보로 둔다. C-01·C-03처럼 근거·비용·시간 설명이 긴 카드는 390에서 1열을 유지한다. |
| 비차용 | Tinder식 한 장씩 넘기기를 쓰지 않는다. 비교 대상 전체를 기억해야 해 인지 부하와 되돌리기 비용이 커진다. |
| 비차용 | 8단계 완료 자체를 성공 지표로 삼지 않는다. 첫 결과 도달 시간, 단계 이탈, 추천 수락, 결과 채택을 함께 본다. |

#### 33.4.3 질문 화면 계약 보강

- C-01부터 C-05는 `답 후보`, `추천`, `추천 근거`, `직접 입력` 순서를 유지한다.
- C-02의 시각 사례는 이미지와 짧은 결과 라벨만 있을 때 390에서 2열 실험 후보가 될 수 있다. 근거, 비용, 설명이 펼쳐지면 1열 상세로 전환한다.
- C-02의 2열 후보는 prototype에서 1열과 실제 과업 완료시간, 오선택, 되돌리기, 스크롤 길이를 비교하기 전 확정하지 않는다.
- 사용자가 선택을 건너뛰면 추천 근거가 약해지는 범위를 말한다. 건너뛰기를 막아 거짓 선호를 받지 않는다.
- 결과 뒤 E-03의 `채택`, `원본 유지`, `다시 수정`과 R-03의 추천 수락·거절을 후속 학습 신호로 사용한다. 좋아요 하나만으로 취향을 단정하지 않는다.

Fogg 모델에서는 답 후보가 능력을 높이고, 근거 있는 추천이 동기를 보완하며, 한 화면 한 CTA가 prompt가 된다. Zeigarnik 진행 표시는 남은 단계를 알려주되 긴 설문 완주를 강요하는 압박으로 쓰지 않는다.

### 33.5 크레딧·잔액과 소진 경고 근거

#### 33.5.1 공식 제품에서 확인한 사실

| 제품·공식 URL | 확인한 사실 | v9.1 적용 |
|---|---|---|
| [OpenAI Credits 도움말](https://help.openai.com/en/articles/12642688-using-credits-for-flexible-usage-in-chatgpt-free-go-plus-pro-sora) | Usage Dashboard에서 크레딧 잔액과 최근 사용을 함께 본다. 자동 충전은 최소 잔액 아래에서 목표 잔액까지 채우며 월 최대 지출을 따로 둔다. 동시 사용으로 음수 잔액이 생길 수 있음을 설명한다. | O-05 잔액·최근 원장·보류·음수, C-06 생성 전 비용 확인, O-06 전후 잔액 확인 |
| [OpenAI 선불 결제 도움말](https://help.openai.com/en/articles/8264644-manage-your-chatgpt-subscription) | 자동 충전의 충전액, 임계값, 월 한도를 분리한다. 잔액 반영 지연과 소진 뒤 quota error, 지연 차단에 따른 음수 잔액 가능성을 공개한다. | 갱신 시각, 데이터 지연, 부족 경고, 자동 충전 상태를 별도 필드로 표시 |
| [Twilio UsageTrigger](https://www.twilio.com/docs/usage/api/usage-trigger) | 사용량이 지정 임계값을 넘으면 webhook으로 알릴 수 있으며, 평가와 callback은 지연되거나 중복될 수 있어 현재값·임계값·발생시각·idempotency를 구분한다. | O-12 임계값과 수신 경로, O-01 알람 발생·중복 묶음, O-05 데이터 시각 |

#### 33.5.2 차용, 변경, 비차용

| 구분 | v9.2 판정 |
|---|---|
| 차용 | 잔액을 단독 숫자로 보여주지 않는다. 사용 가능, 보류, 최근 차감, 마지막 원장 반영, 자동 충전 상태를 같은 판단 범위에 둔다. |
| 차용 | 최소 잔액, 목표 잔액, 월 상한을 서로 다른 입력과 설명으로 보여준다. 하나의 `자동 충전 금액`으로 합치지 않는다. |
| 차용 | 음수와 stale을 비정상 상태로 명시하고 발생 이유와 복구를 제공한다. 0으로 보정하거나 마지막 정상값을 현재값처럼 보이지 않는다. |
| 변경 | 고객 크레딧과 공급자 원가를 분리한다. O-05는 고객 가치 단위, O-07은 공급자 비용과 성공 건당 원가를 다룬다. |
| 비차용 | 자동 충전을 기본 활성으로 확정하지 않는다. 결제 동의와 월 상한은 제품·법무 결정 전 목표 UX다. |
| 비차용 | 예측 소진일을 충분한 사용 이력 없이 표시하지 않는다. 표본이 부족하면 `예측 불가`와 필요한 기간을 보여준다. |

#### 33.5.3 고객과 운영자 화면 계약 보강

- C-06은 현재 사용 가능 잔액, 이번 생성 예상 범위, 생성 뒤 예상 잔액, 견적 시각을 보여준다. 예상 범위가 없으면 생성 CTA를 잠그고 `견적 계산 다시 하기`를 제공한다.
- E-02의 `추가 크레딧 0`은 이번 수정에서 고객 추가 차감이 없다는 뜻이다. 공급자 원가 0이나 무제한 수정을 뜻하지 않는다.
- P-06 발행 실패는 생성 크레딧과 발행 재시도 정책을 분리한다. D-09 확정 전에는 환급 숫자를 약속하지 않는다.
- O-05 상단은 `사용 가능`, `보류`, `최근 차감`, `환불 대기`, `마지막 원장 반영`을 서로 다른 라벨로 유지한다.
- O-06은 조정 전후 잔액, 사유, 관련 작업, 고객 통지 여부, 처리 결과의 확정·불확정을 보여준다. timeout이면 중복 조정을 금지하고 상태 조회만 허용한다.
- O-12는 낮은 잔액 알림의 임계값, 수신 경로, 마지막 발생, 마지막 성공 전달을 분리한다. 알림 발송 실패를 잔액 정상으로 오해하지 않는다.

손실회피는 잔액 경고를 빨갛게 키우는 근거가 아니다. 사용자가 무엇을 잃을지, 언제 멈출지, 어떤 작업이 보존되는지와 다음 행동을 정확히 보여줄 때만 신뢰 장치가 된다.

### 33.6 발행 실패·토큰 만료 알림 근거

#### 33.6.1 공식 제품에서 확인한 사실

| 제품·공식 URL | 확인한 사실 | v9.1 적용 |
|---|---|---|
| [Later 연결 상태](https://help.later.com/hc/en-us/articles/1500003115942-Social-Profile-Connection-Statuses) | 연결됨, 만료돼 새로고침 필요, 연결됐지만 권한 활성화 필요, 만료 임박 상태를 서로 분리하고 상태마다 직접 복구 행동을 제공한다. | A-01 상태어 분리, A-08 상태별 복구, P-03 발행 전 검사 |
| [Buffer 연결 유지](https://support.buffer.com/en-us/articles/best-practices-for-keeping-your-social-channels-connected-UgsmoudZJj) | 조치가 필요한 채널을 Settings 목록 위로 올리고, 게시 실패 이메일 알림 활성화를 권한다. | O-00·O-01 예외 우선, A-01 문제 계정 상단, O-12 외부 수신 |
| [Buffer 채널 새로고침](https://support.buffer.com/en-us/articles/refreshing-a-channel-in-buffer-7oDS4jk7l1) | 연결 갱신은 예약 콘텐츠를 삭제하지 않는다. 갱신 뒤 실패 게시물을 자동 재시도하지 않으며 사용자가 `Re-add to Queue` 또는 `Retry Now`를 선택한다. | A-08 복구 뒤 P-06 복귀, 성공·예약 보존, 명시적 실패 건 재시도 |
| [Buffer 이메일 알림](https://support.buffer.com/en-us/articles/enabling-email-notifications-in-buffer-kuKhKBU7c3) | 게시 실패와 채널 연결 업데이트를 별도 알림 범주로 제공한다. 결제 실패·카드 만료·보안 알림은 일반 구독 해제와 다른 필수 전달 경계를 가진다. | O-12 사건 종류와 수신 정책 분리, D-08 inline과 전역 알림 병행 |
| [Buffer All Channels](https://support.buffer.com/en-us/articles/how-to-use-the-all-channels-view-in-buffer-oqqE4Sdf3T) | 여러 채널의 Queue·Drafts·Approvals·Sent를 한곳에서 보고, 실패 게시물은 Queue에 남긴다. 채널 필터로 특정 고객·계정 맥락을 좁힌다. | P-06 실패 보존, O-01 통합 예외함, 계정·채널 필터 |

#### 33.6.2 차용, 변경, 비차용

| 구분 | v9.2 판정 |
|---|---|
| 차용 | 만료 임박, 만료, 권한 부족, 연결 해제, 일시 오류를 같은 빨간 오류로 합치지 않는다. 상태마다 다음 행동과 영향 범위를 표시한다. |
| 차용 | 문제 계정과 실패 게시물을 정상 목록 위로 올린다. 운영 홈과 작업 화면에서 같은 사건을 같은 상태어로 본다. |
| 차용 | 연결 복구와 실패 게시물 재시도를 분리한다. 복구 성공은 발행 성공이 아니며 자동 재발행하지 않는다. |
| 변경 | 이메일 하나에 의존하지 않는다. 작업 중 repair card, A-01 상태 센터, O-00 위험 레일, O-12 외부 수신을 심각도에 따라 조합한다. |
| 변경 | 실패 건을 단순 Queue 항목으로만 남기지 않고 `성공 채널`, `실패 채널`, `외부 결과 unknown`, `보존 콘텐츠`, `크레딧 상태`를 사건으로 연결한다. |
| 비차용 | 모든 만료와 실패를 즉시 외부 알림으로 보내지 않는다. 치명도, 영향 고객 수, 예약 기한, 반복 여부로 묶어 알람 피로를 줄인다. |

#### 33.6.3 상태별 알림·복구 매트릭스

| 상태 | 고객 화면 | 운영자 화면 | 외부 알림 | 복구 뒤 종료 |
|---|---|---|---|---|
| 만료 임박 | A-01·P-03 주의, 남은 기간과 영향 예약 | O-00·O-01 기한순 | 영향 예약이 있거나 임계값 이내일 때 | A-08 재인증, P-03 재검사 |
| 만료 | A-08 직접 복구, 정상 계정 유지 | O-01·O-02 영향 고객과 작업 | 발행 예정이 있으면 높은 우선순위 | 연결 확인 뒤 실패 건 목록으로 복귀 |
| 권한 부족 | 필요한 권한과 기능 영향 | provider·계정·최근 정상 시각 | 반복 또는 다수 고객 영향 시 | 권한 재승인, 기능별 재검사 |
| 발행 실패 | P-06 실패 채널만 repair | O-01·O-02 오류 묶음과 최근 성공 | Post Failures 수신 정책에 따름 | 재시도 성공, 초안 저장, 실패 유지 중 선택 |
| 결과 unknown | P-06 재발행 비활성, 상태 조회 | O-02 중복 방지와 provider 조회 | 장기 지속 또는 금전 위험 시 | 외부 결과 확정 뒤 성공·실패 분기 |
| 연결 해제 | A-01 재연결, 콘텐츠 보존 | O-00 위험과 영향 예약 | Channel Connection Update | 재연결 뒤 예약·실패 수동 확인 |

Nielsen의 system status visibility와 error recovery를 함께 적용한다. 알림은 상태를 알리는 데서 끝나지 않고 `보존된 것`, `실패 단위`, `다음 행동`, `돌아갈 화면`을 같은 카드에 둔다.

### 33.7 기존 화면별 근거 접합표

| 기존 화면 | 새로 확인된 근거 | 유지하는 설계 | prototype에서 검증할 것 |
|---|---|---|---|
| A-01 | Later 상태 분리, Buffer 문제 채널 상단 | 계정 전체 상태와 복구 CTA | 계정 0·5·50개에서 문제 계정 탐색시간 |
| A-03 | Buffer·Later·Hootsuite provider 세션 경계 | 외부 로그인 전 정확한 계정 확인 | 안내를 읽지 않고도 오계정 위험을 인식하는지 |
| A-05·A-06 | Buffer의 현재 활성 계정 의존 | callback 성공과 사용자 계정 승인 분리 | 오계정 후보 폐기 뒤 기존 예약 보존 인지 |
| C-02 | Stitch Fix 다중 이미지 비교 | 사례를 먼저 보여주고 자유입력 보조 | 390의 1열 대 이미지 전용 2열 완료시간 |
| C-01부터 C-05 | Spotify 초기 선택과 후속 학습 | 3개에서 5개 답 후보와 추천 근거 | 8단계 이탈, 첫 결과 시간, 추천 수락률 |
| C-06 | OpenAI 잔액·최근 사용·충전 설정 | 생성 전 비용·잔액·견적 시각 | 낮은 잔액에서 저장·축소·충전 선택 이해 |
| P-03 | Later expiring·expired·permissions | 계정·토큰·권한·정책 사전 검사 | 하나의 문제 채널이 정상 채널을 가리지 않는지 |
| P-06 | Buffer 복구 뒤 수동 재시도 | 실패 채널만 재시도, unknown 재발행 금지 | 사용자가 성공 채널을 중복 발행하지 않는지 |
| O-00·O-01 | Buffer 문제 항목 상단, 실패 외부 알림 | 미확인 위험 우선 | 정상 지표보다 사건을 먼저 발견하는지 |
| O-05·O-06 | OpenAI 잔액·최근 사용·최소·목표·월 상한 | 원장과 전후 잔액, 보류, 음수 | 금전 상태 stale·uncertain에서 중복 조정 방지 |
| O-12 | Buffer 알림 범주, Twilio 임계값 이벤트 | 사건별 수신과 전달 상태 분리 | 조용한 시간, 치명 우회, 테스트 실패 이해 |

### 33.8 390·1024와 데이터량 상태에 주는 영향

#### 1024 이상

- 계정 목록은 8열, 상태·영향 요약은 4열을 유지한다. 문제 계정을 상단으로 올리되 필터가 바뀌었다는 안내와 전체 계정 수를 함께 보여준다.
- C-02는 이미지 중심 사례만 2열 또는 3열 비교가 가능하다. 근거·비용·시간은 오른쪽 4열 요약 또는 상세 drawer로 점진 공개한다.
- O-05는 잔액 요약 4개 이상을 같은 카드 행에 억지로 넣지 않는다. 사용 가능·보류·최근 차감·환불 대기는 2열 또는 4열이되 마지막 원장 반영은 공통 메타로 분리한다.
- P-06은 채널별 결과 행을 유지하고 실패·unknown을 위로 정렬한다. 성공 행은 접을 수 있지만 수와 계정은 숨기지 않는다.

#### 390

- A-01 계정 행은 `상태`, `계정`, `영향`, `행동` 순서의 1열이다. handle과 외부 식별자 끝자리는 한 줄 말줄임과 전체 보기 행동을 갖는다.
- C-02 이미지 전용 2열은 최소 tap target 44px, 라벨 2줄, 이미지 동일 비율, 페이지 가로 overflow 0을 만족할 때만 실험한다. 설명이 열리면 1열로 전환한다.
- C-06 잔액은 숫자만 크게 두지 않고 `사용 가능`, `이번 예상`, `생성 뒤 예상`, `마지막 반영`을 세로로 읽게 한다.
- P-06과 A-08은 연결 복구 CTA와 발행 재시도 CTA를 동시에 primary로 두지 않는다. 먼저 연결을 복구하고 다음 화면에서 실패 건을 명시적으로 재시도한다.
- O-12 알림 규칙은 사건 종류별 accordion을 사용한다. 치명 알림의 수신 실패 백업은 접지 않는다.

#### 데이터량 0·정상·과다

| 영역 | 0 | 정상 | 과다 |
|---|---|---|---|
| 계정 | 첫 연결과 무연결 제작 | 플랫폼별 1개에서 5개 | 문제 우선, 검색, 상태 필터, 플랫폼 접기 |
| 온보딩 사례 | 인접 업계 사례와 직접 입력 | 3개에서 6개 | 추천 6개 우선, 필터, 더 보기. 무한 swipe 금지 |
| 잔액 원장 | 데이터 소스·마지막 점검·첫 충전 후보 | 최근 8개에서 20개 | 기간·유형 필터, 페이지, 내보내기 |
| 실패 게시물 | 최근 정상 확인과 알림 테스트 | 최근 실패 1개에서 8개 | 사건 묶음, 채널·계정 필터, 최신 50행, 페이지 |

### 33.9 벤치마크가 강화한 기존 우선순위

1. 계정 전환은 연결 버튼보다 `현재 어떤 계정으로 들어가 있는가` 확인이 먼저다.
2. OAuth callback 성공보다 `돌아온 실제 계정을 사용자가 승인했는가`가 연결 완료의 마지막 조건이다.
3. 온보딩 질문 수를 줄이는 것만이 답은 아니다. 비교 가능한 선택지를 보여주고, 첫 결과 뒤 행동으로 계속 학습해야 한다.
4. 크레딧은 잔액 숫자가 아니라 사용 가능·보류·최근 사용·갱신 시각·소진 방지 설정의 묶음이다.
5. 연결 복구와 실패 발행 재시도는 다른 행동이다. 복구 성공 뒤 자동 재발행하지 않는다.
6. 알림은 사건 종류와 심각도로 나눈다. 발행 실패, 연결 해제, 결제·보안 알림을 하나의 전역 토글로 뭉치지 않는다.

### 33.10 레드팀

#### 까다로운 다계정 운영자의 공격

공격: Buffer·Later·Hootsuite도 결국 사용자가 provider에서 로그아웃해야 한다. OpenClaw가 안내를 화면 안에 넣어도 잘못된 계정 연결 문제를 해결하지 못한다.

수정: 안내를 해결로 과장하지 않는다. A-03은 위험 감소 단계이고, 진짜 안전장치는 A-05의 실제 계정 readback과 A-06의 후보 보류다. readback을 신뢰할 데이터가 없으면 연결을 완료하지 않고 `확인 불가`로 남긴다.

#### 회의적 투자자의 공격

공격: Stitch Fix와 Spotify는 패션·음악 소비 제품이다. 마케팅 제작의 8단계 질문에 그대로 적용할 근거가 아니다.

수정: 숫자와 레이아웃을 복제하지 않는다. 두 사례에서 차용하는 것은 자유입력보다 선택을 먼저 보여주는 원리와 초기 입력 뒤 행동으로 계속 학습하는 구조뿐이다. C-02 2열은 별도 prototype 실험 후보이며 전체 질문 흐름 규칙으로 확정하지 않는다.

#### 까다로운 고객의 공격

공격: 잔액 경고와 자동 충전은 더 쓰게 만드는 다크패턴이 될 수 있다.

수정: 자동 충전은 기본 활성으로 확정하지 않는다. 최소선, 목표선, 월 상한, 즉시 발생 가능성, 끄기, 최근 충전 이력을 한 화면에서 보여준다. 충전하지 않아도 현재 작업을 저장하고 저비용 범위로 축소할 수 있다.

#### 장애 대응자의 공격

공격: 모든 만료와 실패를 위로 올리면 운영 홈이 경고로 가득 차고 실제 치명 장애를 못 찾는다.

수정: 같은 원인·provider·시간대의 실패는 사건으로 묶고 영향 고객 수, 예약 기한, 최근 정상 시각으로 정렬한다. 해결 확인과 오탐에는 근거를 요구하고, 단순 `확인함`을 해결로 표시하지 않는다.

### 33.11 셀프심문

질문: 이 보강 결론이 틀렸다면 가장 그럴듯한 이유는 무엇인가?

답: 가장 load-bearing한 가정은 동종 제품 도움말에서 확인한 복구 방식이 OpenClaw의 실제 provider callback과 데이터 모델에서도 구현 가능하다는 것이다. 공식 도움말은 사용자 행동을 설명하지만 후보 연결 보류, callback 뒤 계정 readback, 사건 묶음, 크레딧 원장의 기술 가능성을 증명하지 않는다.

수정: v9.2는 새 기능을 구현 완료로 승격하지 않는다. A-05·A-06은 보안 기술설계와 provider 샌드박스 검증 전 목표 UX다. O-05·O-06의 금전 상태와 O-00·O-01의 사건 묶음도 데이터 계약 확인 전 목표다. 공식 벤치마크는 화면 순서와 안전 원리를 지지할 뿐 시스템 가능성 증거가 아니다.

두 번째 질문: Stitch Fix의 2열 결과가 v9.1의 390 질문 화면 1열 계약과 충돌하지 않는가?

답: 전역 적용하면 충돌한다. Stitch Fix 연구 대상은 이미지 중심 yes/no 평가였고 C-01·C-03은 텍스트와 비용·근거가 긴 선택이다.

수정: 390 1열 계약은 유지한다. C-02의 이미지 전용 부분만 2열 실험 후보로 분리하고, prototype에서 실제 완료시간·오선택·되돌리기·줄바꿈을 관찰하기 전 DESIGN.md 규칙으로 승격하지 않는다.

### 33.12 품질 판정

#### Design review

이번 산출물은 Markdown 유저플로우 근거 보강이며 새 픽셀 화면을 만들지 않았다. 등록된 `design-review` Skill 호출은 현재 도구 목록에 없어 위조하지 않고 `/Users/sj/.claude/standards/design.md`의 rubric으로 정적 자체평가했다.

| 축 | 점수 | 근거 |
|---|---|---|
| 기존 계승 | A | v9.1 전체와 DESIGN.md v24.2, prototype v24, 현재 코드 경계를 유지 |
| 벤치마크 | A | WebSearch 5회와 공식 원문 확인, 네 필수 주제 모두 URL·사실·차용·변경·비차용 기록 |
| 흐름·복구 | A- | 계정·크레딧·발행 복구를 기존 화면과 종료점에 연결 |
| 반응형·overflow | B+ | 390·1024와 0·정상·과다 계약 보강, 신규 렌더 없음 |
| 실제 시각 검증 | 미검증 | v9.2는 prototype을 수정하지 않았고 픽셀 비교 범위가 아님 |

**Design Score: B+ (저충실 유저플로우 근거 리테이크, 실제 prototype 렌더 미검증)**

#### 문서 루브릭

| 항목 | 점수 | 근거 |
|---|---|---|
| 완결성 | 5/5 | v9.1 전량 유지, 필수 벤치마크 네 축과 적용 경계 추가 |
| 정밀성 | 5/5 | 공식 사실, 화면 ID, 상태, 차용·변경·비차용을 분리 |
| 근거 | 5/5 | 현재 구현·DESIGN·prototype·wiki와 공식 URL을 대조 |
| 추적성 | 5/5 | 근거를 A·C·P·O 화면과 prototype 검증 항목에 연결 |
| 출고 완성도 | 4/5 | 신규 렌더와 provider 샌드박스·금전 데이터 기술 검증은 범위 밖 |

**RUBRIC_SCORE: completeness=5/5 precision=5/5 evidence=5/5 traceability=5/5 professionalism=4/5 total=24/25**

**WEAKEST_LINE:** 공식 제품 UX는 화면 순서와 복구 원리를 지지하지만, 후보 연결 보류·사건 묶음·크레딧 원장의 OpenClaw 기술 가능성을 증명하지 않는다.

### 33.13 회귀 감사, 회수 필요, 다음 stage

#### 회귀 감사

- 원본 v9.1 최상위 장: 32개
- 개정 v9.2 최상위 장: 33개
- 원본 v9.1 줄 수: 1,566줄
- v9.2는 v9.1을 복제한 뒤 제목·상단 STAMP·목차와 33장만 추가했다.
- 유지한 기존 기능: 제작·편집·연결·발행·성과·무연결 체험 6흐름, happy·edge·empty·error·loading 경로, O-00부터 O-12, 질문 화법 점검표, D-08부터 D-10, 390·1024·overflow 계약, 기존 레드팀·셀프심문.
- 추가·변경한 것: 공식 WebSearch·WebFetch 근거, 네 주제별 차용·변경·비차용, 기존 화면 접합표, prototype 검증 후보.
- 삭제한 기존 절·화면·상태·인터랙션: 0개.
- 신규 디자인 토큰·컴포넌트·패턴: 0개. `DESIGN.md` v24.2 변경 불필요.

#### 회수 필요

- ⛔ 회수 필요: A-05·A-06의 callback 뒤 실제 계정 readback과 후보 보류가 provider별로 가능한지 샌드박스에서 검증해야 한다.
- ⛔ 회수 필요: C-02 이미지 중심 2열 후보는 390 prototype A/B 검증 전 채택할 수 없다.
- ⛔ 회수 필요: 고객 크레딧 최소선·목표선·월 상한, 자동 충전 기본값, 환불·잔액 조정 권한은 제품·법무·기술 합의가 필요하다.
- ⛔ 회수 필요: 발행 실패·연결 해제 외부 알림의 제공자, 비용, 치명 알림 백업 경로는 기술설계 전에 결정해야 한다.
- ⛔ 회수 필요: 사건 묶음과 알람 심각도는 실제 baseline·임계값 없이 확정할 수 없다.

#### 다음 stage 가능 여부

v9.2는 디자인 단계의 저충실 근거 문서로 회장 티키타카에 사용할 수 있다. prototype stage 진입 가능 여부는 v9.1과 동일하게 보류다. D-08부터 D-10, provider 계정 readback 가능성, 크레딧 정책, 알림 제공자·임계값, 등록 design-review B 이상이 종료 증거다.

### 33.14 v9.2 SOURCES / MODEL / SKILLS / STAMP

SOURCES:

- `/Users/sj/sj_code_master/openclaw-auto/docs/design-docs/user-flow-openclaw-service-v9.1-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/DESIGN.md` v24·v24.1·v24.2
- `/Users/sj/sj_code_master/openclaw-auto/docs/구현현황.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/v24-brief.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/openclaw-auto-marketing-agent-fidelity-v24-gpt-codex.html`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/home-desktop-1024.png`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/studio-desktop-1024.png`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/settings-desktop-1024.png`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/AccountManager.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/SocialConnectButton.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/lib/channel-capabilities.ts`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/ChannelTabs.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/studio/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/operator/customers/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/studio.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/marketing-hub-surface-map.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/reference/channel-status.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/decisions/004-social-connect-oauth-not-passwords.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/architecture/data-model.md`
- `/Users/sj/.claude/standards/design.md`
- `/Users/sj/.claude/standards/doc-review.md`
- `/Users/sj/.claude/standards/benchmarks.md`
- `/Users/sj/.claude/standards/artifact-stamp.md`
- [Buffer 채널 연결](https://support.buffer.com/en-us/articles/connecting-your-channels-to-buffer-HvWLgAJvL9)
- [Buffer Instagram 연결](https://support.buffer.com/en-us/articles/connecting-your-instagram-account-to-buffer-n9Ad6veXsu)
- [Buffer 신규 대시보드](https://support.buffer.com/en-us/articles/navigating-buffers-new-dashboard-layout-JsRJX4s6QQ)
- [Later 프로필 추가·제거·이전](https://help.later.com/hc/en-us/articles/360043244733-Add-Remove-Transfer-Social-Profiles-in-Later)
- [Hootsuite X 연결](https://help.hootsuite.com/hc/pt-br/articles/1260804308109-Connect-an-X-account)
- [Stitch Fix 스타일 선호 온보딩 UX 연구](https://multithreaded.stitchfix.com/blog/2016/11/30/us-design-capture-style-preferences-during-sign-up/)
- [Spotify Taste Onboarding](https://newsroom.spotify.com/2018-08-27/5-things-free-users-need-to-be-taking-advantage-of-on-spotify/)
- [OpenAI Credits 도움말](https://help.openai.com/en/articles/12642688-using-credits-for-flexible-usage-in-chatgpt-free-go-plus-pro-sora)
- [OpenAI 선불 결제 도움말](https://help.openai.com/en/articles/8264644-manage-your-chatgpt-subscription)
- [Twilio UsageTrigger](https://www.twilio.com/docs/usage/api/usage-trigger)
- [Later 연결 상태](https://help.later.com/hc/en-us/articles/1500003115942-Social-Profile-Connection-Statuses)
- [Buffer 연결 유지](https://support.buffer.com/en-us/articles/best-practices-for-keeping-your-social-channels-connected-UgsmoudZJj)
- [Buffer 채널 새로고침](https://support.buffer.com/en-us/articles/refreshing-a-channel-in-buffer-7oDS4jk7l1)
- [Buffer 이메일 알림](https://support.buffer.com/en-us/articles/enabling-email-notifications-in-buffer-kuKhKBU7c3)
- [Buffer All Channels](https://support.buffer.com/en-us/articles/how-to-use-the-all-channels-view-in-buffer-oqqE4Sdf3T)

MODEL: gpt-codex/gpt-5.6-sol

SKILLS_USED: 없음. 현재 설치된 스킬 중 기존 제품 유저플로우 문서의 공식 UX 근거만 보강하는 전용 매칭 스킬이 없었다. `/Users/sj/.claude/standards/design.md`와 `doc-review.md`는 스킬이 아니라 품질헌법으로 직접 읽고 적용했다.

SKILLS_SKIPPED: `imagegen`은 이미지 생성이 없는 Markdown 문서 리테이크라 스킵했다. `openclaw-creative-brief`는 자동 생성 에이전트용 창작 브리프라 제품 화면·복구 플로우 근거 보강과 범위가 다르다.

🏷 STAMP | line: openclaw-service | 생성: 2026-08-15 08:15 KST | model: gpt-codex/gpt-5.6-sol | agent: product-designer

skills: matching skill 없음, design.md·doc-review.md 직접 적용 | 근거: v9.1 + DESIGN.md v24.2 + prototype v24 + 현재 구현 + WebSearch 5회 + 공식 원문 15건

고민: 타사 화면 숫자와 UI를 복제하지 않고, 계정 readback·후속 학습·잔액 맥락·수동 재시도라는 안전 원리만 기존 OpenClaw 화면에 접합했다.

## 34. v9.3 확정 정책과 전체 화면·플로우 보강

### 34.1 개정 범위와 우선순위

v9.3은 v9.2의 33개 장, 고객 6개 흐름, 운영자 O-00부터 O-12, happy·edge·empty·error·loading 상태, 390·1024 레이아웃 계약을 전부 유지한다. 제품구조 결정서 §9.5에서 회장이 확정한 다음 다섯 항목을 화면과 막힘 없는 흐름으로 추가한다.

1. 자율 발행과 승인 후 발행을 테넌트 기본값, 채널별 override, 콘텐츠 유형별 override로 선택
2. 품질 게이트 반려분은 무과금이며 반려 이유와 차감 없음을 고객에게 명시
3. 가입 전 연락처를 받고 체험 크레딧을 지급
4. 결과물·소재·편집 지시서·사람이 읽을 수 있는 취향 프로파일·성과 이력을 일괄 내보내기
5. 운영자 화면에서 내부 테스트 테넌트와 실제 유저를 구분

34장은 위 확정값을 v9.2의 관련 분기보다 우선한다. 이 문서는 사용자에게 필요한 정보, 선택, 상태, 복구, 종료점을 확정한다. 체험 크레딧 수량·만료, 연락처 보관 기간, 콘텐츠 유형 분류, 발행 정책의 서버 적용 시점, 내보내기 보관 기간, 품질 반려 사유 코드는 제품·법무·기술설계에서 정한다.

### 34.2 실제 웹 검색 호출과 원문 증거

2026-08-15에 검색 도구를 실제로 여섯 번 호출했다. 네 필수 주제를 각각 분리했고, Hootsuite와 시그마인 공식 자료가 첫 검색에서 충분히 나오지 않아 두 번 추가 검색했다. 모든 호출은 성공했다. 실패 메시지는 0건이다.

| 실제 호출 | 검색 주제 | 실제 결과에서 읽은 문장 | 공식 URL | 우리 화면에 차용·변경한 것 |
|---:|---|---|---|---|
| 1 | Buffer·Later 계정 연결 | “Before connecting a channel to Buffer, log in to the correct social account in another browser tab.” | [Buffer 채널 연결](https://support.buffer.com/en-us/articles/connecting-your-channels-to-buffer-HvWLgAJvL9) | A-03에서 provider 세션을 먼저 확인한다. 우리 앱이 외부 로그아웃을 대신했다고 말하지 않는다. |
| 1 | Buffer 다계정 | “Buffer connects the Instagram account that’s currently active in your browser.” | [Buffer Instagram 연결](https://support.buffer.com/en-us/articles/connecting-your-instagram-account-to-buffer-n9Ad6veXsu) | OAuth 성공과 의도한 계정 확인을 분리한다. A-05 readback 승인 전 기본 계정을 바꾸지 않는다. |
| 6 | Hootsuite 계정 연결 | “Em uma aba ou janela separada do navegador, faça logout do X.” | [Hootsuite X 연결](https://help.hootsuite.com/hc/pt-br/articles/1260804308109-Connect-an-X-account) | 제공자별 로그아웃 안내를 A-03에 둔다. 모든 제공자에 공통 계정 선택기가 있다고 가정하지 않는다. |
| 2 | 온보딩 질문 | “Seeing more than one image at a time helped users make decisions.” | [Stitch Fix 온보딩 연구](https://multithreaded.stitchfix.com/blog/2016/11/30/us-design-capture-style-preferences-during-sign-up/) | C-02 이미지 사례는 비교 가능한 묶음으로 보여준다. 긴 설명 카드까지 2열로 일반화하지 않는다. |
| 2 | 온보딩 질문 | “Answering a few quick questions helps us tailor your experience and recommend the right next steps.” | [Buffer Home](https://support.buffer.com/en-us/articles/using-buffer-home-WXHZkDiPYb) | 질문 답이 바로 다음 행동과 추천을 바꾸게 한다. 제품과 무관한 리드 정보는 제작 질문에 넣지 않는다. |
| 3 | 크레딧 잔액 | “This shows credits balance and recent usage.” | [OpenAI Credits](https://help-lb.openai.com/en/articles/12642688-using-credits-for-flexible-usage-in-chatgpt-freegopluspro-sora) | 잔액만 떼어 보이지 않고 최근 사용, 보류, 갱신 시각, 이번 예상 차감을 함께 보여준다. |
| 3 | 소진 경고 | “A UsageTrigger is a webhook that notifies your application of usage thresholds.” | [Twilio UsageTrigger](https://www.twilio.com/docs/usage/api/usage-trigger) | O-12에 임계값, 현재값, 발생 시각, 전달 상태를 분리한다. 알림 수신을 잔액 사실로 오해하지 않는다. |
| 4 | 토큰 만료 | “To keep your accounts secure, your connection to Later will sometimes expire and require re-authentication.” | [Later 연결 만료](https://help.later.com/hc/en-us/articles/360042869414-Why-Is-My-Connection-Expired) | 만료를 사용자 실수로 몰지 않는다. A-08에서 원인 후보, 보존 항목, 재인증을 함께 보여준다. |
| 4 | 발행 실패 복구 | “No, Buffer will not automatically retry your failed posts if the connection fails.” | [Buffer 채널 새로고침](https://support.buffer.com/en-us/articles/refreshing-a-channel-in-buffer-7oDS4jk7l1) | 연결 복구와 실패 발행 재시도를 분리한다. 자율 발행 모드에서도 unknown과 기존 실패 건은 자동 재발행하지 않는다. |
| 4 | 실패·연결 알림 | “We'll send an email if a post in your queue fails to be published.” | [Buffer 알림](https://support.buffer.com/en-us/articles/enabling-email-notifications-in-buffer-kuKhKBU7c3) | 발행 실패와 채널 연결 해제를 별도 사건으로 보낸다. 치명도·기한·중복 묶음은 OpenClaw 방식으로 추가한다. |
| 5 | 국내 연락처 기반 체험 | “완성 영상은 입력하신 이메일로 보내드려요.” | [시그마인](https://sigmine.ai/) | 가입 폼 전체보다 연락처 확인과 첫 결과 경험을 먼저 둔다. 마케팅 수신 동의는 결과 전달과 분리한다. |
| 5 | 국내 체험 크레딧 | “무료 크레딧 받고 직접 써보기.” | [시그마인](https://sigmine.ai/) | 상시 무료 등급을 만들지 않고, 확인된 연락처에 체험 크레딧을 지급하는 일회성 흐름으로 변경한다. |

검색 결과의 문장을 그대로 UI 카피로 복제하지 않는다. 차용한 것은 외부 세션의 정직한 경계, 비교 가능한 선택, 잔액의 맥락, 사건별 알림과 명시적 복구, 연락처와 첫 가치 교환 구조다. 타사의 요금 수치, 브랜드 표현, 레이아웃, 무료 제공량은 차용하지 않는다.

### 34.3 신규·확장 화면 목록

| ID | 화면 | 목적 | 기존 접합점 | 다음 화면 |
|---|---|---|---|---|
| M-01 | 발행 방식 설정 | 테넌트 기본, 채널별, 유형별 발행 모드 선택 | Settings 확장 | M-02, V-01, P-03 |
| M-02 | 적용 결과 확인 | 어떤 범위에 어떤 모드가 실제 적용되는지 확인 | 기존 설정 요약 확장 | M-01 또는 이전 작업 |
| V-01 | 승인 대기함 | 승인 후 발행 결과를 기한·채널·계정별로 검수 | 기존 Queue 확장 | V-02 |
| V-02 | 승인 상세 | 마스터·파생·캡션·계정·시각·품질 근거 승인 | P-04, P-05, E-01, V-01 |
| Q-01 | 품질 게이트 반려 안내 | 반려 이유, 보존된 것, 무과금, 다음 행동 안내 | C-07과 P-01 사이 | C-05, E-01, C-07, O-10 |
| TR-00 | 가입 전 체험 진입 | 연락처 제공과 체험 가치·제한을 한 결정으로 설명 | 공개 랜딩 | TR-01 |
| TR-01 | 연락처 입력·동의 | 결과 전달 연락처와 필수·선택 동의 분리 | 신규 공개 화면 | TR-02 |
| TR-02 | 연락처 확인·크레딧 지급 | 확인 완료, 지급 상태, 체험 범위 표시 | 신규 공개 화면 | TR-03 |
| TR-03 | 체험 작업공간 | SNS 연결 없이 C-01부터 결과·편집까지 경험 | G-00, T-01 확장 | Q-01, T-02, TR-04 |
| TR-04 | 결과 보존 가입 | 결과·남은 크레딧·선택 이력을 정식 계정으로 이어가기 | T-02 확장 | 기존 가입·Home |
| X-01 | 데이터 내보내기 센터 | 내 소유 데이터 범주와 수량 확인 | Settings 확장 | X-02 |
| X-02 | 내보내기 범위 확인 | 범주·기간·형식·제외 항목·예상 크기 확인 | 신규 | X-03 또는 X-01 |
| X-03 | 준비·다운로드 | 진행 상태, 부분 실패, 파일 목록, 만료 안내 | 신규 | X-04 또는 종료 |
| X-04 | 내보내기 이력 | 과거 요청, 만료, 재생성, 누락 항목 확인 | 신규 | X-02 또는 종료 |

운영자 O-03·O-04·O-09는 신규 화면을 만들지 않고 `내부 테스트` 라벨, 필터, 감사 이력으로 확장한다. P-03·P-04·P-06은 현재 작업에 적용된 발행 모드와 그 출처를 반복한다.

### 34.4 테넌트 발행 방식 설정과 두 모드

#### 34.4.1 적용 우선순위

사용자가 보는 우선순위는 `콘텐츠 유형별 override > 채널별 override > 테넌트 기본값`이다. 이 순서는 화면 해석 계약이며 저장 스키마를 확정하지 않는다.

M-01의 각 행은 범위, 현재 모드, 상속 출처, 다음 발행부터 적용 여부, 영향받는 대기 건수를 보여준다. 한 행의 예시는 `Instagram · 영상 · 자율 발행 · 테넌트 기본에서 상속`이다. 모드 두 가지는 다음처럼 설명한다.

| 모드 | 화면 설명 | 품질 통과 뒤 | 실패·unknown |
|---|---|---|---|
| 자율 발행 | `품질 기준을 통과한 결과를 선택한 시각과 계정으로 자동 발행합니다.` | P-03 사전 점검 후 예약 또는 발행 | 자동 재발행 금지. P-06과 A-08에서 명시적 복구 |
| 승인 후 발행 | `품질 기준을 통과한 결과를 승인 대기함에 보냅니다. 확인 전에는 발행하지 않습니다.` | V-01·V-02 승인 뒤 P-04 또는 P-05 | 승인 대기로 보존. 자동 전환·자동 승인 금지 |

신규 테넌트의 화면 추천은 `승인 후 발행`이다. 첫 채널·첫 유형의 결과를 확인한 뒤 사용자가 자율 발행으로 바꿀 수 있다. 추천은 제품 기본값을 확정하는 기술 계약이 아니라, 위험이 큰 첫 사용에서 브랜드 손실을 줄이는 UX 제안이다.

#### 34.4.2 M-01 Happy 경로

`Settings` -> `M-01 테넌트 기본 선택` -> `채널별 예외 선택` -> `유형별 예외 선택` -> `M-02 적용 결과 미리보기` -> `변경 확인` -> `원래 작업으로 복귀`

자율 발행으로 바꿀 때 M-02는 다음을 한 화면에 보여준다.

1. 자동 발행되는 채널과 콘텐츠 유형
2. 사용되는 계정과 예약 시각 결정 위치
3. 품질 게이트 반려 시 무과금·미발행
4. 토큰 만료, 정책 거부, 결과 unknown 시 자동 재발행 금지
5. 현재 승인 대기·예약 건 중 새 정책이 적용되는 범위

`자율 발행 켜기`는 영향 목록을 읽은 뒤에만 활성화한다. 채널별·유형별 예외가 하나도 없으면 빈 표를 보여주지 않고 `현재 모든 범위가 테넌트 기본값을 따릅니다`와 `예외 추가`를 제공한다.

#### 34.4.3 자율 발행 Happy 경로

`C-07 생성` -> `품질 게이트 통과` -> `P-01·P-02 파생·텍스트 준비` -> `P-03 계정·정책·현재 모드 확인` -> `예약이면 P-04` -> `P-05 자동 실행` -> `P-06 결과`

- C-07과 P-03에 `자율 발행 · Instagram 영상 override`처럼 실제 적용 모드와 출처를 표시한다.
- 사용자는 플랫폼 요청 전까지 작업 단위 `이번만 승인 후 발행으로 전환` 또는 `이번 발행 중지`를 선택할 수 있다.
- 품질 통과는 발행 성공이 아니다. 계정·토큰·정책·시간·중복 검사를 P-03에서 다시 통과해야 한다.
- 일부 채널만 차단되면 정상 채널은 설정에 따라 계속하고 차단 채널은 P-06에 보존한다. 사용자가 `모두 성공해야 발행`을 별도 선택하지 않았다면 조용히 전체를 취소하지 않는다.
- 외부 결과가 unknown이면 해당 채널은 상태 조회만 허용한다. 자율 발행 모드라는 이유로 재전송하지 않는다.

#### 34.4.4 승인 후 발행 Happy 경로

`C-07 생성` -> `품질 게이트 통과` -> `P-01·P-02 준비` -> `V-01 승인 대기` -> `V-02 상세 검수` -> `승인하고 예약 P-04` 또는 `승인하고 지금 발행 P-05` -> `P-06 결과`

V-01 기본 정렬은 발행 예정이 가까운 순서, 품질 통과 시각이 오래된 순서다. 행은 결과 썸네일, 채널·계정, 유형, 예정 시각, 품질 통과, 비용, 변경된 revision, 필요한 행동을 표시한다.

V-02의 주요 행동은 한 번에 하나다.

- `승인하고 예약`: 예약 시각이 있거나 시각 확인이 필요한 경우
- `승인하고 지금 발행`: 지금 발행이 선택된 경우
- `수정 요청`: E-01로 이동하고 현재 승인안 보존
- `이번 결과 사용 안 함`: 사유를 선택하고 결과를 미채택으로 보존. 품질 게이트 반려와 다른 사건이므로 자동 무과금으로 표현하지 않는다.

승인 대기 기한이 지나도 자율 발행으로 바뀌지 않는다. 예약 시각이 지나면 `승인 필요 · 예약 시각 지남`으로 남고 새 시각 선택 또는 지금 발행을 요구한다.

#### 34.4.5 발행 모드 Edge·Empty·Error·Loading

| 상태 | 화면 | 복구·종료 |
|---|---|---|
| 범위 충돌 | M-02에 테넌트·채널·유형 규칙과 최종 적용값 표시 | 예외 제거, 범위 수정, 최종값 유지 |
| 정책 변경 중 대기 건 존재 | 영향 건수를 모드별로 분리 | 기존 정책 유지 또는 다음 작업부터 적용. 일괄 소급은 별도 확인 |
| 승인자 부재 | V-01에 `승인 가능한 사람이 없습니다` | 권한 해결, 초안 보존, 자율 발행으로 몰래 전환 금지 |
| 승인 대기 0건 | V-01 empty에 마지막 확인 시각과 새 제작 CTA | C-01 또는 종료 |
| 설정 조회 실패 | M-01 마지막 확인값과 갱신 실패 표시 | 재조회. 확인 불가 상태에서 자율 발행 활성화 금지 |
| 설정 저장 timeout | M-02 `적용 여부 확인 중` | 현재값 재조회. 중복 저장 금지 |
| 승인 저장 실패 | V-02 입력·검수 위치 보존 | 다시 승인, 초안 저장, 지원 요청 |
| 대기 건 과다 | V-01 상위 위험 20건, 필터·페이지 | 채널·유형·기한·계정 필터, 선택 일괄 승인 전 영향 확인 |
| 모드 로딩 | 현재 적용 모드를 지우지 않고 `갱신 중` 표시 | 읽기는 허용, 저장만 잠금 |

### 34.5 품질 게이트 반려는 무과금

#### 34.5.1 Q-01 정보 위계와 확정 카피

Q-01의 첫 문장은 결과 실패보다 금전 사실과 보존을 먼저 말한다.

**제목:** `품질 기준을 통과하지 못해 결과를 전달하지 않았어요`

**핵심 안내:** `이번 시도는 품질 게이트에서 반려되어 크레딧을 사용하지 않았습니다.`

**보존 안내:** `선택한 방향, 자료, 스타일, 채널 설정은 그대로 보존했습니다.`

**이유 영역:**

- 통과하지 못한 기준의 사용자 언어 이름
- 발견된 위치 또는 구간
- 왜 전달할 수 없는지
- 자동으로 바꿀 수 있는 것과 다시 선택해야 하는 것
- 검수 시각과 기준 버전

내부 모델 오류명, 점수 원문, 공급자 프롬프트는 고객에게 그대로 노출하지 않는다. 이유가 권리·안전과 관련돼 제한 미리보기도 위험하면 결과 이미지를 보여주지 않고 안전한 설명만 제공한다.

#### 34.5.2 잔액과 원장 표시

| 원장 상태 | 고객 문구 | 행동 |
|---|---|---|
| 차감 없음 확인 | `차감 0크레딧 · 현재 사용 가능 잔액은 원장 확인값을 표시` | 같은 방향 재시도, 선택 수정 |
| 잔액 동기화 중 | `이번 시도는 차감되지 않았습니다. 현재 잔액을 확인하고 있어요.` | 잔액 새로고침, 제작 선택 수정 |
| 원장 조회 실패 | `차감 없음은 확인했습니다. 잔액 표시는 잠시 불러오지 못했습니다.` | 원장 재조회, 지원 요청 |
| 판정과 원장 충돌 | `차감 상태를 확인 중입니다. 같은 작업을 다시 실행하지 마세요.` | O-11 지원 사건 자동 연결, 상태 조회만 |

무과금은 사용자 선택으로 버린 결과, 발행 실패, 연결 실패의 비용 정책을 자동으로 바꾸지 않는다. Q-01은 시스템 품질 게이트가 사용자에게 전달하기 전에 반려한 사건에만 사용한다.

#### 34.5.3 Q-01 Happy·Edge·Empty·Error·Loading

`C-07 품질 검사` -> `Q-01 반려 이유·차감 0 확인` -> `같은 방향 자동 보정`, `C-05 선택 변경`, `E-01 지시 수정`, `저장 후 종료`, `지원 요청` 중 하나

| 상태 | 표현 | 복구 |
|---|---|---|
| 이유 1개 | 해당 기준과 위치를 펼쳐 표시 | 권장 수정 1개와 직접 수정 |
| 이유 여러 개 | 전달을 막은 상위 3개, 나머지 접기 | 한 번에 고칠 묶음 제안, 전체 이유 보기 |
| 이유 데이터 없음 | `반려됐지만 상세 이유를 불러오지 못했습니다` | 차감 0과 선택 보존을 먼저 확인, 이유 재조회·지원 |
| 자동 보정 재반려 | 같은 이유 반복 횟수와 바뀐 점 표시 | 자동 반복 중단, 선택 변경·지원 |
| 검사 timeout | 반려로 단정하지 않고 `검수 결과 확인 중` | 상태 조회, 작업 보존, 중복 생성 금지 |
| 품질 서비스 오류 | `검수를 완료하지 못해 결과를 전달하지 않았습니다. 크레딧은 사용하지 않았습니다.` | 검수 재시도, 저장 후 종료 |
| loading | 완료한 검사와 현재 검사, 예상이 아닌 실제 진행 단계 | 백그라운드 전환, 안전한 취소 |

### 34.6 가입 전 연락처 기반 체험

#### 34.6.1 체험 원칙

- 상시 무료 등급이 아니라 확인된 연락처에 체험 크레딧을 지급한다.
- 기본 연락처는 시그마인 사례와 결과 전달 목적에 맞춘 이메일이다. 전화번호·메신저를 추가 수단으로 만들려면 별도 제품 결정이 필요하다.
- 결과 전달과 체험 운영에 필요한 필수 동의와 광고성 정보 수신 선택 동의를 분리한다.
- 가입 전에도 연락처 수집 목적, 보관 기간, 삭제·문의 경로를 입력 전에 보여준다.
- SNS 연결은 체험의 선행 조건이 아니다. TR-03은 기존 T-01 무연결 제작을 재사용한다.
- 체험 크레딧 수량, 만료, 1인당 지급 횟수는 실제 정책값을 읽는다. 화면에 임의 숫자를 쓰지 않는다.

#### 34.6.2 Happy 경로

`TR-00 연락처 남기고 체험 크레딧 받기` -> `TR-01 이메일·필수 동의` -> `TR-02 확인 링크 또는 코드` -> `지급 완료` -> `TR-03 C-01부터 제작` -> `Q-01 또는 T-02 결과` -> `TR-04 결과 보존 가입` -> `기존 Home`

| 화면 | 핵심 정보 | 주요 행동 | 신뢰 장치 |
|---|---|---|---|
| TR-00 | 체험으로 가능한 것, 불가능한 것, 지급 조건 | 연락처 남기기 | 카드 등록 요구 여부, SNS 무연결, 결과 보존 조건 명시 |
| TR-01 | 이메일, 필수 동의, 선택 마케팅 동의 | 확인 메일 받기 | 선택 동의 미체크여도 체험 가능 |
| TR-02 | 마스킹 연락처, 확인 상태, 지급 상태 | 링크 열기 또는 코드 확인 | 재전송 간격과 연락처 수정 |
| TR-03 | 사용 가능 체험 잔액, 이번 예상, 남은 범위 | C-01부터 제작 | 가입 모달 자동 노출 금지, 품질 반려 무과금 |
| TR-04 | 결과, 남은 크레딧, 이어지는 데이터 | 계정 만들고 그대로 이어가기 또는 결과 받기 | 가입을 미뤄도 안내된 범위의 결과 전달 보장 |

가입 완료 뒤 결과물, 선택 이력, 남은 유효 체험 크레딧은 같은 작업공간에 이어진다. 가입을 취소하면 TR-03으로 돌아가며 이미 만든 결과를 즉시 삭제하지 않는다. 보존 기간이 끝나기 전 남은 기간과 내보내기 또는 가입 행동을 보여준다.

#### 34.6.3 Edge·Empty·Error·Loading

| 상태 | 표현 | 복구·종료 |
|---|---|---|
| 이미 체험한 연락처 | 남은 체험과 기존 결과가 있으면 이어가기 | TR-03 또는 TR-04, 중복 지급 약속 금지 |
| 확인 메일 미도착 | 마스킹 주소와 재전송 가능 시각 | 재전송, 이메일 수정, 지원 |
| 확인 링크 만료 | 입력과 동의 보존 | 새 링크 발송 |
| 지급 정책 조회 실패 | 크레딧 수량을 숨기고 `지급 조건 확인 중` | 재조회. 확인 전 제작 시작 금지 |
| 지급 timeout | 성공·실패 단정 금지 | 지급 상태 조회, 중복 지급 요청 금지 |
| 체험 잔액 0 | 만든 결과와 선택 보존 | 가입, 충전 정책 보기, 저장 종료 |
| 체험 결과 0개 | 빈 저장 목록 대신 첫 제작 CTA | C-01 |
| 품질 반려 | Q-01에서 차감 0, 체험 잔액 유지 | 같은 방향 보정 또는 선택 변경 |
| 연락처 형식 오류 | 입력값 보존, 구체 오류 | 수정 후 다시 보내기 |
| 연락처 서비스 장애 | 입력값을 브라우저에 안전 보존 | 다시 시도, 복사, 나중에 받기 |
| loading | 확인·지급·작업공간 준비를 별도 단계로 표시 | 중복 클릭 잠금, 다른 안내 읽기 허용 |

### 34.7 사용자 소유 데이터 내보내기

#### 34.7.1 X-01 범주와 사람이 읽을 수 있는 형태

| 범주 | 화면에서 보여줄 수량·범위 | 다운로드 안의 사람이 읽을 수 있는 항목 |
|---|---|---|
| 결과물 | 마스터·채널 파생·revision·게시용 파일 수 | 원본 파일, 미리보기 인덱스, 파일명·버전·생성일 목록 |
| 소재 | 업로드·생성·참고 자료 수, 권리 상태 | 원본 폴더, 출처·권리·사용 이력 목록 |
| 편집 지시서 | 결과별 지시서·revision 수 | 결과별 Markdown 지시서, 컷·자막·효과·순서 표 |
| 취향 프로파일 | 현재 축, 근거 사건 수, 마지막 갱신 | `취향-프로파일.md`: 선호·비선호, 축과 가중치, 근거, 불확실성, 수정 이력 |
| 성과 이력 | 기간·채널·계정·게시물 수 | CSV와 읽기용 요약: 선택·revision·게시 결과·지표·기간·표본·외부 변수 |

압축 파일 최상단에 `README`와 전체 목록을 둔다. 기계 호환용 JSON이 포함돼도 사람이 읽는 Markdown·CSV·미리보기 인덱스를 대체하지 않는다. 소셜 토큰, API 키, 서버 키, 다른 테넌트 데이터, 식별 가능한 운영 루프 집계는 내보내지 않는다.

#### 34.7.2 Happy 경로

`Settings` -> `X-01 데이터 내보내기` -> `범주·기간 선택` -> `X-02 수량·형식·제외 항목 확인` -> `내보내기 준비` -> `X-03 완료·파일 목록` -> `다운로드` -> `X-04 이력`

- X-01은 다섯 범주를 기본 선택하되 각 범주의 수량과 예상 크기를 보여준다. 크기를 계산하지 못하면 가짜 값을 쓰지 않는다.
- X-02는 권리 제한, 삭제된 원본, 외부 제공자에서 아직 동기화하지 못한 성과를 `제외` 또는 `확인 필요`로 분리한다.
- X-03은 전체 성공, 부분 성공, 실패를 구분한다. 부분 성공 파일은 내려받을 수 있고 누락 목록만 다시 준비할 수 있다.
- 다운로드 링크가 만료되면 데이터가 삭제됐다고 표현하지 않는다. X-04에서 같은 범위로 새 파일을 준비한다.
- 탈퇴 흐름에서는 계정 삭제 확인 전에 최신 내보내기를 먼저 제안하되 내보내기를 강제하지 않는다.

#### 34.7.3 Edge·Empty·Error·Loading

| 상태 | 표현 | 복구·종료 |
|---|---|---|
| 결과물 0개 | 다른 범주 수량과 `결과물 없음` 이유 | 소재·취향·성과만 내보내기 |
| 소재 권리 불명 | 해당 파일명·출처·권리 상태 | 포함 여부 확인, 권리 메타와 함께 내보내기, 지원 |
| 성과 동기화 지연 | 마지막 동기화 시각과 누락 채널 | 마지막 저장값으로 내보내기 또는 동기화 후 재요청 |
| 매우 큰 내보내기 | 범주·기간별 크기 상위 항목 | 기간 나누기, 범주 나누기, 백그라운드 준비 |
| 일부 파일 손상 | 성공 파일과 손상 목록 분리 | 손상 항목만 재준비, 지원 연결 |
| 준비 timeout | 결과 미확정 | 상태 조회, 중복 요청 금지 |
| 다운로드 만료 | 파일 만료와 원본 데이터 보존을 분리 | 같은 범위 다시 준비 |
| 내보내기 이력 0개 | 첫 내보내기 설명 | X-01 |
| loading | 범주별 스캔, 패키징, 검증 단계 | 안전하게 나가기, 알림 받기 |

### 34.8 운영자 화면의 내부 테스트 구분

#### 34.8.1 표시와 집계 원칙

- `내부 테스트`는 운영자 전용 라벨이다. 고객 셸과 고객용 다운로드에는 노출하지 않는다.
- O-03 테넌트 행은 고객명 옆에 텍스트 배지 `내부 테스트`를 표시한다. 색만으로 구분하지 않는다.
- O-04 개요는 현재 분류, 변경 시각, 변경 주체, 사유를 보여준다.
- O-09 운영 루프의 기본 필터는 `실사용자만`이다. `내부 테스트만`, `모두 비교`를 선택할 수 있다.
- O-07 원가와 O-08 막힘은 내부 테스트를 숨기지 않고 기본값과 필터 상태를 명시한다. 실제 비용은 내부 테스트도 발생하므로 원가 총계에서 조용히 제외하지 않는다.
- 벤치마크·추천 근거로 승격되는 집계는 내부 테스트 제외값을 기본으로 한다. 비교 화면에는 두 집단 표본을 병렬로 표시할 수 있다.

#### 34.8.2 Happy 경로

`O-03 테넌트 선택` -> `O-04 개요` -> `분류 변경` -> `현재 영향 확인` -> `사유 입력` -> `변경 확인` -> `O-09 집계 재계산 상태 확인`

분류 변경 전 영향 목록은 운영 루프 추천 근거, 고객 지표, 내부 proof 보고, 원가 집계 중 바뀌는 것과 바뀌지 않는 것을 구분한다. 과거 원시 사건을 삭제하거나 이동했다고 말하지 않는다. 집계 관점만 분리한다.

#### 34.8.3 Edge·Empty·Error·Loading

| 상태 | 표현 | 복구 |
|---|---|---|
| 내부 테스트 0개 | O-03 필터 empty와 현재 전체 테넌트 수 | 필터 초기화 |
| 실사용자 0개 | O-09에 `실사용자 표본 0` | 내부 테스트 참고값 보기. 추천 근거로 승격 금지 |
| 분류 변경 직후 | 기존 집계와 `재계산 중` 배지 | 마지막 계산 시각 유지, 완료 뒤 교체 |
| 잘못 분류 | 감사 이력에서 이전값 확인 | 영향 재확인 뒤 되돌리기 |
| 일괄 변경 | 대상 테넌트 이름·수·현재 분류 미리보기 | 개별 제외, 확인 후 실행 |
| 저장 timeout | 분류 미확정 | 현재값 재조회, 중복 변경 금지 |
| 집계 실패 | 마지막 정상 집계와 실패 시각 | 다시 계산, 원시 사건 확인 |
| 과다 테넌트 | O-03 검색·필터·페이지, 배지는 행에 유지 | 내부/실사용자/상태 조합 필터 |

### 34.9 반응형·밀도·overflow 계약

#### 1024 이상

| 화면군 | 주축 | 열수 | 정렬·밀도 |
|---|---|---|---|
| M-01·M-02 | column 안 12열 | 정책 목록 8 + 적용 요약 4 | 테넌트·채널·유형 시작선 일치, 요약 sticky 가능 |
| V-01 | column | 필터 12 + 목록 12 | 행 최대 20개, 기한·채널·유형 필터 |
| V-02 | row | 결과·근거 8 + 승인 행동 4 | 행동 레일은 본문을 가리지 않음 |
| Q-01 | column | 반려 이유 8 + 잔액·행동 4 | 금전 사실과 보존을 접힘선 위에 배치 |
| TR-00부터 TR-04 | column | 본문 7 + 체험 요약 5 | 연락처 한 결정, 선택 마케팅 동의는 별도 행 |
| X-01·X-02 | column | 범주 8 + 수량·크기 4 | 다섯 범주를 한 표로, 세부는 행 펼침 |
| X-03·X-04 | column | 상태·파일 8 + 요약 4 | 부분 실패를 상단에, 성공 파일 목록 보존 |
| O-03·O-04·O-09 | 기존 계약 유지 | 기존 8+4 또는 12 | `내부 테스트`는 고객명·필터와 같은 열 순서 |

#### 390

- 모든 신규 화면은 column 1열이다.
- M-01은 `테넌트 기본 -> 채널 예외 -> 유형 예외 -> 적용 결과` 순서다. 정책 표를 가로 압축하지 않는다.
- V-02는 `결과 -> 계정·시각 -> 품질 근거 -> 비용 -> 승인 행동` 순서다.
- Q-01은 `무과금 -> 보존 -> 이유 -> 다음 행동` 순서다.
- TR-01은 이메일, 필수 동의, 선택 동의, CTA 순서다. 동의 문구를 한 줄에 이어붙이지 않는다.
- X-01은 범주별 수량·크기를 카드 행으로 reflow한다. 파일명과 경로는 `overflow-wrap:anywhere`와 복사를 제공한다.
- O-03의 내부 테스트 배지는 고객명과 같은 줄을 우선하되 2줄이 되면 배지 전체를 다음 줄로 보낸다.
- sticky CTA는 마지막 콘텐츠를 가리지 않게 16px 안전 여백을 둔다.

#### 데이터량 0·정상·과다

| 데이터 | 0 | 정상 | 과다 |
|---|---|---|---|
| 정책 예외 | 기본값과 예외 추가 | 채널·유형 1개에서 8개 | 검색, 채널·유형 묶음, 최신 변경 우선 |
| 승인 대기 | 마지막 확인과 새 제작 | 1개에서 20개 | 기한·채널·유형 필터, 페이지, 영향 확인 후 일괄 행동 |
| 품질 반려 이유 | 이유 조회 실패와 무과금 확인 | 1개에서 3개 | 상위 3개, 범주 묶음, 전체 이유 접기 |
| 체험 결과 | 첫 제작 CTA | 1개에서 8개 | 최근 8개, 기간·상태 필터, 가입·내보내기 |
| 내보내기 파일 | 범주 없음 안내 | 5개 범주와 최근 요청 1개에서 8개 | 범주·기간별 분할, 이력 페이지 |
| 테넌트 | 필터 초기화 | 1개에서 20개 | 검색, 내부 테스트·상태 필터, 페이지 |

spacing은 DESIGN.md의 `4, 8, 12, 16, 24, 32, 48`만 사용한다. 신규 토큰은 0개다. 화면 간 좌우 시작선은 기존 v24의 224px sidebar와 main inset을 따른다.

### 34.10 상태·추적성·dead-end 감사

| 확정 요구 | 화면 | happy | edge | empty | error | loading | 종료점 |
|---|---|---:|---:|---:|---:|---:|---|
| 자율 발행 | M-01·M-02, P-03부터 P-06 | 충족 | 충족 | 충족 | 충족 | 충족 | P-06, 초안 보존, 모드 변경 |
| 승인 후 발행 | V-01·V-02, P-04부터 P-06 | 충족 | 충족 | 충족 | 충족 | 충족 | P-06, 수정, 미채택 보존 |
| 채널별·유형별 설정 | M-01·M-02 | 충족 | 충족 | 충족 | 충족 | 충족 | 저장 확인 또는 변경 없음 |
| 품질 반려 무과금 | Q-01, O-05·O-11 | 충족 | 충족 | 충족 | 충족 | 충족 | 재시도, 선택 변경, 저장 종료, 지원 |
| 연락처 기반 가입 전 체험 | TR-00부터 TR-04 | 충족 | 충족 | 충족 | 충족 | 충족 | 가입, 결과 전달, 저장 종료 |
| 데이터 일괄 내보내기 | X-01부터 X-04 | 충족 | 충족 | 충족 | 충족 | 충족 | 다운로드, 부분 다운로드, 재준비 |
| 내부 테스트 구분 | O-03·O-04·O-09 | 충족 | 충족 | 충족 | 충족 | 충족 | 분류 확인, 되돌리기, 필터 종료 |

dead-end 감사 결과는 0건이다. 어떤 설정·검수·체험·내보내기·운영 분기에서도 사용자는 이전 화면, 입력 보존, 저장 종료, 상태 재조회, 지원 연결 중 최소 하나를 가진다. 승인 대기, 품질 검사 timeout, 내보내기 timeout, 내부 테스트 재집계는 성공·실패를 추정하지 않고 `확인 중`으로 종료할 수 있다.

### 34.11 회수 필요와 다음 stage 가능 여부

- ⛔ 회수 필요: 체험 크레딧 수량·만료·중복 지급 기준, 연락처 확인 수단·보관 기간·삭제 경로는 제품·법무·기술 합의가 필요하다.
- ⛔ 회수 필요: 발행 정책의 콘텐츠 유형 분류, 기존 예약에 대한 적용 시점, 일괄 변경 권한은 기술설계 전에 확정해야 한다.
- ⛔ 회수 필요: 품질 게이트 반려 사유 분류와 고객용 번역, 무과금 원장 확인 기준, 반복 반려 상한은 studio 계약과 맞춰야 한다.
- ⛔ 회수 필요: 내보내기 최대 크기, 생성 파일 보관 기간, 다운로드 인증, 손상 파일 재준비 정책은 기술설계가 필요하다.
- ⛔ 회수 필요: 내부 테스트 분류 변경 권한과 운영 루프 재계산 시점은 감사 요구와 함께 합의해야 한다.
- ⛔ 회수 필요: v9.3 신규 화면은 prototype 390·1024에서 실제 렌더·overflow·줄바꿈·클릭을 검증하지 않았다.

현재 v9.3은 디자인 단계의 저충실 흐름 정본 후보로 회장 티키타카와 prototype 입력에 사용할 수 있다. prototype 루프는 시작 가능하지만 eng-design 진입은 불가하다. 종료 증거는 회장 디자인 승인, DESIGN.md v24.3 정합, 핵심 화면 390·1024 prototype, 등록 design-review B 이상, 위 정책 gap의 회수 또는 명시적 상류 이관이다.

### 34.12 레드팀과 셀프심문

#### 레드팀 1: 까다로운 고객

공격: 자율 발행을 켰다가 브랜드에 맞지 않는 게시물이 나가면 품질 게이트가 있다는 설명은 변명이 되지 않는다.

수정: 신규 테넌트 화면 추천은 승인 후 발행으로 두고, 자율 발행 전 M-02에서 채널·유형·계정·시각·실패 안전장치를 확인한다. 작업 단위 중지와 이번만 승인 전환을 열고, 품질 통과와 provider 발행 성공을 분리한다. unknown과 기존 실패 건은 자율 모드에서도 자동 재발행하지 않는다.

#### 레드팀 2: 개인정보에 민감한 체험 사용자

공격: 무료 크레딧을 미끼로 연락처와 광고 동의를 묶으면 리드 수집 다크패턴이다.

수정: 결과 전달·체험 운영 필수 동의와 마케팅 수신 선택 동의를 분리한다. 선택 동의를 하지 않아도 같은 체험을 제공하고, 입력 전에 목적·보관·삭제 경로를 보여준다.

#### 레드팀 3: 이탈하려는 고객

공격: ZIP 파일 하나를 주면서 취향 프로파일과 편집 지시서는 내부 식별자 덩어리라 다른 도구에서 쓸 수 없다.

수정: X-01은 Markdown·CSV·미리보기 인덱스를 필수로 정의한다. 취향은 축·가중치·근거·불확실성을 사람 문장으로, 편집 지시서는 결과별 컷·자막·효과·순서 표로 제공한다. JSON은 보조일 뿐 사람용 문서를 대체하지 않는다.

#### 레드팀 4: 회의적 데이터 담당자

공격: 내부 테스트 배지를 운영자가 임의로 바꾸면 성과가 좋은 표본만 실제 유저로 남기는 데이터 세탁이 가능하다.

수정: O-04에서 변경 주체·시각·사유와 전후값을 감사 이력으로 남기고, O-09는 현재 분류뿐 아니라 기간 내 분류 변경을 표시한다. 원시 사건은 삭제하지 않고 집계 관점만 바꾼다.

#### 셀프심문

질문: 이 결론이 틀렸다면 가장 그럴듯한 이유는 무엇인가?

답: 가장 load-bearing한 가정은 사용자가 테넌트·채널·콘텐츠 유형의 세 층 정책을 이해하고 실제 적용 모드를 예측할 수 있다는 것이다. override가 늘어나면 자율 발행을 껐다고 생각한 영상이 특정 채널에서 계속 나갈 수 있다.

수정: M-01의 규칙 편집보다 M-02의 최종 적용 결과를 승인 대상으로 둔다. 모든 제작·검수·발행 화면에서 `현재 모드 + 적용 출처`를 반복하고, 정책 변경 시 영향받는 대기 건을 먼저 보여준다. 설정 저장 성공 토스트만으로 종료하지 않는다.

두 번째 질문: 품질 반려 무과금이 공급자 원가를 무한히 떠안는 악용 경로가 되지 않는가?

답: 가능하다. 무과금은 고객 신뢰 원칙이지만 반복 자동 보정이 무한하면 우리 원가가 폭증한다.

수정: Q-01은 반복 횟수와 바뀐 점을 보여주고 같은 이유의 자동 반복을 중단한다. 반복 상한과 속도 제한 값은 화면에서 임의 확정하지 않고 운영 루프·기술설계 회수 항목으로 올렸다. 사용자에게는 무과금 원칙을 유지하되 다음 선택 변경이나 지원 경로로 전환한다.

### 34.13 품질 판정

#### Design review

| 축 | 점수 | 근거 |
|---|---|---|
| 기존 계승 | A | v9.2의 33개 장과 v24.2 셸·토큰·운영자 흐름 전량 유지 |
| 흐름·복구 | A- | 신규 14개 화면, 두 발행 모드, 5상태, 종료점과 보존 규칙 정의 |
| 벤치마크 | A | 실제 검색 6회, Buffer·Later·Hootsuite·Stitch Fix·OpenAI·Twilio·시그마인 공식 원문 확인 |
| 신뢰·윤리 | A | 품질 반려 무과금, 마케팅 동의 분리, 데이터 이동성, 내부 테스트 감사 이력 |
| 반응형·overflow | B+ | 390·1024 주축·열수·0·정상·과다 계약, 신규 렌더 없음 |
| 실제 시각 검증 | 미검증 | Markdown 흐름 리테이크이며 prototype·픽셀 대조는 다음 루프 |

**Design Score: B+ (저충실 유저플로우 정적 review, prototype 렌더 미검증)**

등록된 `design-review` 스킬은 현재 도구 목록에 없어 실제 호출로 위조하지 않았다. `/Users/sj/.claude/standards/design.md`의 동일 rubric으로 정적 자체평가했다.

#### 문서 루브릭

| 항목 | 점수 | 근거 |
|---|---|---|
| 완결성 | 5/5 | v9.2 전량 유지, 신규 확정 5건에 화면·상태·복구·종료점 추가 |
| 정밀성 | 5/5 | ID, 정보 순서, 카피, 우선순위, 금전·보존·오류 경계 명시 |
| 근거 | 5/5 | 실제 구현·DESIGN·prototype·wiki·제품결정과 실제 웹 검색 원문 대조 |
| 추적성 | 5/5 | 제품구조 §9.5 다섯 요구를 M·V·Q·TR·X·O 화면에 1:1 매핑 |
| 전문성 | 4/5 | client-ready 저충실 문서지만 신규 prototype과 기술 가능성은 미검증 |

**RUBRIC_SCORE: completeness=5/5 precision=5/5 evidence=5/5 traceability=5/5 professionalism=4/5 total=24/25**

**WEAKEST_LINE:** 체험 크레딧 수량·만료, 발행 정책 적용 시점, 품질 반려 원장, 내보내기 보관, 내부 테스트 재집계는 사용자 흐름은 닫혔지만 기술·법무 계약이 아직 없다.

### 34.14 회귀 감사

- 원본 v9.2 최상위 장: 34개
- 개정 v9.3 최상위 장: 35개
- 원본 v9.2 줄 수·크기: 1,929줄·151,205B
- 개정 v9.3 줄 수·크기: 2,403줄·194048B
- v9.3은 v9.2 전체를 복제한 뒤 상단 메타데이터와 34장을 확장했다.
- 유지한 기존 기능: 제작·편집·연결·발행·성과·무연결 체험 6흐름, O-00부터 O-12, 질문 화법, 계정 readback, 다계정, capability 탭, 390·1024·overflow, 기존 레드팀·셀프심문.
- 추가·변경한 것: 실제 웹 검색 증거, M-01·M-02, V-01·V-02, Q-01, TR-00부터 TR-04, X-01부터 X-04, O-03·O-04·O-09 내부 테스트 확장.
- 삭제한 기존 절·화면·상태·인터랙션: 0개.
- 신규 color·typography·spacing·radius 토큰: 0개.
- 신규 컴포넌트·상태·금지 패턴은 DESIGN.md v24.3에 동시 반영한다.

### 34.15 v9.3 SOURCES / MODEL / SKILLS / STAMP

SOURCES:

- `/Users/sj/sj_code_master/openclaw-auto/docs/design-docs/user-flow-openclaw-service-v9.2-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/제품구조-결정-2026-08-15.md` §9.5
- `/Users/sj/sj_code_master/openclaw-auto/docs/prd-openclaw-service-v8.1.1-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/구현현황.md`
- `/Users/sj/sj_code_master/openclaw-auto/DESIGN.md` v24·v24.1·v24.2·v24.3
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/v24-brief.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/openclaw-auto-marketing-agent-fidelity-v24-gpt-codex.html`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/AccountManager.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/components/channel/SocialConnectButton.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/studio/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/operator/customers/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/architecture/two-service-boundary.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/studio.md`
- `/Users/sj/.claude/standards/design.md`
- `/Users/sj/.claude/standards/doc-review.md`
- `/Users/sj/.claude/standards/benchmarks.md`
- `/Users/sj/.claude/standards/artifact-stamp.md`
- [Buffer 채널 연결](https://support.buffer.com/en-us/articles/connecting-your-channels-to-buffer-HvWLgAJvL9)
- [Buffer Instagram 연결](https://support.buffer.com/en-us/articles/connecting-your-instagram-account-to-buffer-n9Ad6veXsu)
- [Hootsuite X 연결](https://help.hootsuite.com/hc/pt-br/articles/1260804308109-Connect-an-X-account)
- [Stitch Fix 온보딩 연구](https://multithreaded.stitchfix.com/blog/2016/11/30/us-design-capture-style-preferences-during-sign-up/)
- [Buffer Home](https://support.buffer.com/en-us/articles/using-buffer-home-WXHZkDiPYb)
- [OpenAI Credits](https://help-lb.openai.com/en/articles/12642688-using-credits-for-flexible-usage-in-chatgpt-freegopluspro-sora)
- [Twilio UsageTrigger](https://www.twilio.com/docs/usage/api/usage-trigger)
- [Later 연결 만료](https://help.later.com/hc/en-us/articles/360042869414-Why-Is-My-Connection-Expired)
- [Buffer 채널 새로고침](https://support.buffer.com/en-us/articles/refreshing-a-channel-in-buffer-7oDS4jk7l1)
- [Buffer 알림](https://support.buffer.com/en-us/articles/enabling-email-notifications-in-buffer-kuKhKBU7c3)
- [시그마인](https://sigmine.ai/)

MODEL: gpt-codex/gpt-5.6-sol

SKILLS_USED: 없음. 현재 설치된 스킬 중 기존 제품의 저충실 화면·상태·유저플로우를 개정하는 전용 매칭 스킬이 없었다. `design.md`, `doc-review.md`, `benchmarks.md`, `artifact-stamp.md`는 스킬이 아니라 품질헌법으로 직접 읽고 적용했다.

SKILLS_SKIPPED: `imagegen`은 이미지 생성이 없는 Markdown 문서 과제라 스킵했다. `openclaw-creative-brief`는 자동 생성 에이전트용 브리프이며 제품 화면과 복구 플로우 설계와 범위가 다르다.

🏷 STAMP | line: openclaw-service | 생성: 2026-08-15 08:29 KST | model: gpt-codex/gpt-5.6-sol | agent: product-designer

skills: matching skill 없음, design.md·doc-review.md 직접 적용 | 근거: v9.2 + 제품구조 §9.5 + DESIGN.md v24.2 + prototype v24 + 현재 구현 + 실제 WebSearch 6회 + 공식 출처 11개

고민: 자동화의 편의가 브랜드 통제를 침범하지 않게 적용 모드를 모든 작업에 반복하고, 무과금·체험·내보내기·내부 테스트를 신뢰 장치로 연결했다.

## 35. v9.4 시장·요청 조립·3후보·다국어 품질·운영 예외 보강

### 35.1 적용 범위와 우선순위

v9.4는 v9.3의 35개 최상위 장, 고객 6개 흐름, 운영자 O-00부터 O-12, happy·edge·empty·error·loading 상태, 390·1024 레이아웃 계약을 전부 유지한다. 이 장은 OpenClaw PRD v8.2.1, 제품구조 결정서 §9.6의 회장 확정 5건, §9.7 요청 조립 4층, Studio PRD v1.2.1과 레드팀 제품구상의 최신 결론을 화면 계약으로 만든 증분이다.

충돌 시 우선순위는 다음과 같다.

1. `docs/제품구조-결정-2026-08-15.md` §9.6과 §9.7
2. `docs/prd-openclaw-service-v8.2.1-gpt-codex.md`
3. `studio/docs/prd-studio-service-v1.2.1-gpt-codex.md`
4. 이 문서 35장
5. 이 문서 0장부터 34장

이번 장이 확정하는 것은 화면 순서, 사용자가 보는 정보, 선택과 복구 경로다. 생성 견적 산식, 크레딧 원장, 품질 판정 가중치, 출력 언어 프로필 저장 구조, 발행 정책의 API와 DB는 확정하지 않는다.

### 35.2 기존 구현 정독 결과와 계승 범위

#### 확인한 구현

| 표면 | 현재 확인된 것 | v9.4 계승 방식 |
|---|---|---|
| Studio 제작 | 7개 채널 미리보기, 4개 직접 발행 채널, 생성·편집·저장·발행·예약, 초안·이력 | 제작 결과와 채널 탭을 없애지 않고 C-00부터 C-08을 앞단에 연결 |
| 계정 | 다계정 선택과 연결 상태, 채널 capability | 요청 조립과 발행 전 readback에서 선택 계정·채널을 그대로 노출 |
| 공통 UI | Button, Card, EmptyState, Field, Stack, Section과 DESIGN 토큰 | 신규 토큰 없이 기존 컴포넌트 조합으로 확장 |
| 운영자 | 고객·결제·작업·채널·이벤트·진단·지원 표면 | O-00 홈에서 예외 6개만 먼저 보이고 상세 화면은 기존 O-01부터 O-12를 재사용 |
| prototype v24 | 1024 데스크톱 3영역, 390 모바일 단일 열, 비용 견적·채널 미리보기·발행 준비·재시도 | 셸·헤더·카드 밀도·단일 열 전환을 유지하고 요청 조립과 후보 선택을 교체 입력으로 추가 |

#### 유지한 기존 기능

- 제작·편집·연결·발행·성과·SNS 무연결 제작 체험의 6개 고객 흐름
- Studio의 7채널 미리보기와 4채널 직접 발행
- 초안 저장, 생성 이력, 예약, 다계정 선택, capability 탭
- 테넌트별 발행 모드, 품질 게이트 반려 무과금, 가입 전 체험, 데이터 내보내기, 내부 테스트 구분
- O-00부터 O-12 운영자 상세 화면과 증거 우선 복구
- 390·1024 반응형, 8pt 간격, 44px 터치 영역, 0·정상·과다 데이터 계약

#### 추가하거나 의미를 좁힌 것

- C-00 시장·출력 언어가 모든 제작 요청의 첫 화면
- 사용자가 답해야 하는 질문을 C-00, C-01, C-03의 최대 3개로 제한
- C-04에 요청 조립 4층을 독립 행으로 표시
- C-05는 항상 저해상도 후보 3개를 비교하는 화면
- C-06은 선택한 1개만 고해상도로 만드는 최종 견적 확인
- F-01·F-02를 다국어 품질 피드백 경로로 추가
- O-00의 첫 화면은 잔액·실패·큐·토큰 만료·고객 막힘·환불 근거만 노출

현재 배포 URL의 전체 실경로는 이 작업에서 제공되지 않았다. 코드와 prototype 렌더는 직접 확인했지만 실제 배포 화면의 클릭 E2E는 미검증이다.

### 35.3 벤치마크 실조사와 적용 판단

#### 지정 검색 1: AI content generation onboarding flow UX best practice 2026

- [AI-generated onboarding flows](https://www.uxperiment.io/article/ai-generated-onboarding-flows): 첫 가치까지의 시간을 줄이고, 그 시점에 필요한 정보만 묻고, 권한 요청 전에 이유를 설명하는 원칙을 확인했다.
- [Rethinking the UI of GenUI](https://arxiv.org/abs/2606.13843): 구조화 입력은 의도를 명확히 하지만 진입 장벽을 높일 수 있고, 폭넓은 저충실 탐색은 가능성을 빨리 보여주는 장점이 있다는 비교 연구를 확인했다.

차용: 시장·언어를 추천값과 함께 먼저 묻고, 이후 질문을 2개만 더 허용한다. 저해상도 3안으로 탐색 폭을 먼저 보여준다.

변경: 연구의 저충실 산출물을 최종 결과로 두지 않는다. OpenClaw는 3안을 비교한 뒤 선택한 1안만 실사용 고해상도로 올린다.

#### 지정 검색 2: progressive disclosure onboarding SaaS reduce decision fatigue

- [Nielsen Norman Group, Make It Easier for Users to Make Decisions](https://www.nngroup.com/reports/make-decisions/): 선택지 표현 방식과 추천이 의사결정 난도를 바꾸며, 핵심 비교 정보는 결정 순간에 보여야 한다는 근거를 확인했다.
- [Userpilot progressive disclosure examples](https://userpilot.com/blog/progressive-disclosure-examples/): 현재 과업에 필요한 정보만 먼저 보이면 인지 부하를 낮출 수 있다는 사례를 확인했다.
- [SaaS onboarding flow guide](https://productquant.dev/blog/saas-user-onboarding-flow/): 첫 실행은 하나의 활성화 가치에 집중하고 세부 설정은 점진적으로 여는 패턴을 확인했다.

차용: 추천안 1개를 목록 맨 앞에 고정하고 근거·예상 비용 범위·예상 시간을 같은 카드 안에 둔다. 4층의 세부 출처와 가중치는 펼침 영역으로 보낸다.

변경: 비용·시간·브랜드 충돌처럼 선택을 바꾸는 정보는 숨기지 않는다. 점진적 공개를 정보 은폐에 쓰지 않는다.

#### 지정 검색 3: credit based pricing UI cost estimate before generation

- [Shape of AI, Cost Estimates](https://www.shapeof.ai/patterns/cost-estimates): 비용 견적을 실행 버튼 가까이에 두고, 고정 숫자가 불가능하면 범위로 표시하며, 파라미터 변경 시 견적을 갱신하고 비용 동인을 설명하는 패턴을 확인했다.
- [Ibbaka, credit-based pricing for AI agents](https://www.ibbaka.com/ibbaka-market-blog/a-guide-to-the-design-of-credit-based-pricing-for-ai-agents): 크레딧 가격은 투명성과 예측 가능성이 핵심이며 사용 전 비용과 잔액을 이해할 수 있어야 한다는 원칙을 확인했다.
- [Adobe Firefly generative credits FAQ](https://helpx.adobe.com/creative-cloud/apps/generative-ai/generative-credits-faq.html): 예측 가능한 생성은 실행 전에 고정 비용을 표시하고, 변동 비용은 `Credits vary`처럼 불확실성을 명시하는 실제 사례를 확인했다.

차용: C-04, C-06, C-07의 주행동 바로 위에 잔액, 이번 단계 비용 범위, 총 예상 범위, 소요 시간, 변동 사유를 표시한다.

변경: 미선택 후보의 고해상도 비용은 현재 차감액에 합산하지 않는다. 후보마다 `추후 고해상도 변환 시 추가 과금`을 별도 표시하고 사용자가 다시 견적을 승인하게 한다.

#### 벤치마크 적용에서 의도적으로 제외한 것

- 가입 장벽을 낮춘다는 이유로 시장·출력 언어를 시스템이 조용히 추론하지 않는다. 추천값을 보여주되 사용자가 확인하거나 `건너뛰고 추천값 사용`을 선택해야 요청 파라미터가 확정된다.
- 진행률을 높이기 위한 가짜 긴급성, 가짜 절약액, 숨겨진 크레딧 차감은 사용하지 않는다.
- 성과 수치를 생성물의 언어 품질에 대한 확정적 인과로 표현하지 않는다.

### 35.4 v9.4 핵심 흐름 원칙

1. **시장 먼저:** 새 제작 요청은 C-00에서 타깃 시장과 출력 언어를 가장 먼저 확정한다.
2. **출력 언어는 명시적 파라미터:** UI 언어와 생성물 언어를 분리한다. 건너뛰면 추천값이 요청에 명시적으로 기록되고 C-04에서 다시 보인다.
3. **질문 최대 3개:** C-00 시장·언어, C-01 목표·대상, C-03 스타일·우선순위 외에는 사용자에게 질문형 입력을 요구하지 않는다.
4. **모든 질문 건너뛰기 가능:** 건너뛰면 추천값을 적용하고, 어떤 값이 적용됐는지 즉시 readback한다.
5. **추천안 먼저:** 추천 카드 1개를 맨 앞에 두고 근거, 예상 비용 범위, 예상 소요 시간을 같은 시야에 표시한다.
6. **요청 조립 4층:** C-04에서 시장, 공통 마케팅 지식, 개인 취향, 브랜드 제약을 각각 독립 행으로 확인한다.
7. **저비용 탐색 후 선택 집중:** C-05에서 저해상도 3안, C-06에서 선택 1안의 고해상도 견적, C-07에서 선택본만 렌더한다.
8. **발행은 승인 기본:** P-03·P-04의 기본 모드는 승인 후 발행이며 자율 발행은 명시적 옵션이다.
9. **이해 못 하는 언어도 판정 가능:** F-01·F-02에서 성과 신호와 사용자의 자기 피드백을 함께 기록한다.
10. **운영 홈은 예외만:** O-00에는 잔액·실패·큐·토큰 만료·고객 막힘·환불 근거만 둔다. BI는 비범위다.

### 35.5 질문 예산과 추천 계약

#### 질문 1: C-00 시장·출력 언어

상단 `질문 1/3`을 표시한다.

- 필드 1: 타깃 시장. 한국, 일본, 영어권, 태국, 직접 입력
- 필드 2: 출력 언어. 시장 선택에 따른 추천값을 먼저 표시
- 추천 카드: `추천: 일본 시장 + 일본어 출력`
- 근거: 선택한 캠페인 목표, 기존 계정의 주요 팔로어 지역, 브랜드 지원 언어 중 확인된 근거만 표시
- 비용: 이 화면에서는 요청 전체의 초기 범위만 표시
- 시간: 저해상도 3안까지와 선택본 고해상도까지를 분리 표시
- 보조 행동: `건너뛰고 추천값 사용`
- 안전장치: UI 언어를 출력 언어로 자동 복사하지 않는다.

#### 질문 2: C-01 목표·대상

상단 `질문 2/3`을 표시한다.

- 필드: 이번 콘텐츠 목표 1개와 핵심 대상 1개
- 추천 카드: 과거 성과와 현재 캠페인에서 근거가 있는 1개 조합
- 근거: 최근 게시물 성과, 캠페인 메모, 선택 시장의 마케팅 공통 지식
- 비용·시간: 목표에 따른 채널 수와 미디어 유형이 견적에 주는 영향을 범위로 표시
- 보조 행동: `건너뛰고 추천값 사용`

#### 질문 3: C-03 스타일·우선순위

상단 `질문 3/3`을 표시한다.

- 필드: 스타일 방향 1개, 최우선 기준 1개
- 추천 카드: 개인 취향과 브랜드 제약을 동시에 만족하는 1개 방향
- 근거: 사용자가 승인한 과거 선택, 브랜드 금지 패턴, 선택 시장의 채널 관행
- 비용·시간: 고해상도 미디어 유형에 따른 범위
- 보조 행동: `건너뛰고 추천값 사용`

#### 질문이 아닌 단계

- C-02는 마케팅 공통 지식의 근거 보기다.
- C-04는 요청 조립 검토다.
- C-05는 후보 비교와 선택이다.
- C-06은 비용·시간 재확인이다.
- C-07은 생성 진행이다.
- C-08은 결과와 다음 행동이다.

따라서 화면 수가 3개를 넘더라도 사용자의 대답을 요구하는 질문은 3개를 넘지 않는다.

### 35.6 요청 조립 4층의 입력과 가시성

| 층 | 어디서 들어오는가 | 사용자가 어디서 보는가 | 수정 권한 | 생성 뒤 피드백 귀착점 |
|---|---|---|---|---|
| 1. 타깃 시장 | C-00 시장·출력 언어, 요청 단위 | 전 화면 상단 MarketLanguageBadge, C-04 첫 행, C-05·C-08 결과 메타, P-03 발행 확인 | 요청마다 변경 가능 | F-01 시장 적합성 피드백 |
| 2. 마케팅 공통 지식 | 선택 시장·목표·채널을 키로 Studio가 제공하는 근거 묶음 | C-02 근거 보기, C-04 둘째 행의 출처·갱신일, C-05 후보별 적용 이유 | 원문 직접 편집 불가, 적용 제외·오류 신고 가능 | F-01 `시장 관행과 어긋남` 신고 |
| 3. 개인 취향 | 기존 승인·거절·편집·직접 선택, C-03 현재 요청 선택 | C-03 추천 근거, C-04 셋째 행, C-05 후보별 `취향 반영` 칩 | 요청에서 끄기·강도 변경 가능 | F-01 만족·불만족, 다음 요청 반영 여부 승인 |
| 4. 브랜드 제약 | 브랜드 설정·금지어·톤·필수 표기·법적 제한 | C-04 넷째 행, 충돌 시 상단 경고, C-05·C-08 제약 통과 상태 | 권한 있는 사용자가 브랜드 설정에서 수정 | F-01 위반 신고, Q-01 품질 게이트 |

#### C-04 RequestLayerSummary 표시 순서

1. `시장과 출력 언어`: 값, 추천 여부, 마지막 변경자
2. `마케팅 공통 지식`: 적용된 근거 수, 상위 근거 2개, 전체 보기
3. `내 취향`: 반영 강도, 사용한 승인 이력 수, 이번 요청에서 끄기
4. `브랜드 제약`: 통과·주의·충돌, 필수 표기와 금지 패턴

네 행은 하나의 프롬프트 문자열로 합치지 않는다. 사용자는 각 층의 출처와 영향 범위를 분리해서 이해할 수 있어야 한다.

#### 4층 상태 계약

| 상태 | 시장 | 공통 지식 | 개인 취향 | 브랜드 제약 | 다음 행동 |
|---|---|---|---|---|---|
| happy | 명시됨 | 출처·갱신일 있음 | 승인 이력 있음 | 통과 | 저해상도 3안 견적 확인 |
| empty | 추천값 대기 | 근거 없음 | 이력 없음 | 설정 없음 | 추천값·기본 공통 지식·무취향·안전 기본값을 각각 표시하고 계속 가능 |
| error | 지원 불가 조합 | 조회 실패 | 이력 조회 실패 | 충돌 | 오류 난 층만 재시도·제외·수정, 다른 층 입력 보존 |
| loading | 시장 추천 계산 | 근거 불러오는 중 | 취향 불러오는 중 | 제약 검사 중 | 행별 skeleton, 전체 화면 차단 금지 |

### 35.7 v9.4 현재 화면 목록

#### 고객 제작·품질 핵심 화면

| ID | 화면 | 역할 | 주행동 | 종료점 |
|---|---|---|---|---|
| G-00 | 시작 홈 | 기존 작업·새 제작 진입 | 새 제작 시작 | C-00 |
| C-00 | 시장·출력 언어 | 질문 1/3, 요청 시장과 출력 언어 확정 | 추천값으로 계속 또는 직접 선택 | C-01 |
| C-01 | 목표·대상 | 질문 2/3 | 추천 목표·대상 적용 | C-02 |
| C-02 | 마케팅 공통 지식 | 적용 근거와 출처 검토 | 적용 계속 또는 특정 근거 제외 | C-03 |
| C-03 | 스타일·우선순위 | 질문 3/3 | 추천 스타일 적용 | C-04 |
| C-04 | 요청 조립 검토 | 4층, 비용 범위, 시간, 잔액 확인 | 저해상도 3안 만들기 | C-05 |
| C-05 | 저해상도 후보 3개 | 같은 조건의 3안 비교 | 1개 선택 | C-06 |
| C-06 | 선택본 고해상도 확인 | 선택 1안 견적·시간·추가 과금 고지 | 선택본 고해상도 만들기 | C-07 |
| C-07 | 선택본 렌더 진행 | 선택본 1개만 고해상도 렌더 | 완료 결과 보기 | C-08 |
| C-08 | 고해상도 결과 | 결과 편집·저장·발행 준비·피드백 | 저장, 편집, 발행 준비, 언어 평가 | E-01·P-03·F-01 |
| F-01 | 다국어 품질 피드백 | 이해 여부, 자기 피드백, 시장 적합성 기록 | 피드백 저장 | F-02 |
| F-02 | 품질 신호 요약 | 성과 신호와 사용자 피드백을 분리해서 보기 | 다음 요청 반영 승인 | C-00·R-01 |

#### 계승 화면

| 묶음 | 화면 ID | v9.4 의미 |
|---|---|---|
| 편집 | E-01·E-02·E-03 | 선택 고해상도 결과를 편집하고 버전을 보존 |
| 계정 | A-01부터 A-08 | 연결·재인증·다계정·권한 복구 |
| 발행 | P-01부터 P-06, M-01·M-02, V-01·V-02, Q-01 | 승인 후 발행 기본, 자율 발행 옵션, 품질 반려 무과금 |
| 성과 | R-01부터 R-03 | 결과·비교·다음 추천, 인과 과장 금지 |
| 무연결 체험 | T-01부터 T-03, TR-00부터 TR-04 | 연결 전 제작과 연락처 기반 체험 |
| 데이터 이동 | X-01부터 X-04 | 범위 확인, 내보내기, 완료·부분 실패 |
| 운영자 | O-00부터 O-12 | 예외 6개 홈과 기존 상세 복구 |

### 35.8 happy path

```text
G-00 새 제작
  -> C-00 타깃 시장과 출력 언어 확인
  -> C-01 목표와 대상 확인
  -> C-02 마케팅 공통 지식 근거 검토
  -> C-03 스타일과 우선순위 확인
  -> C-04 요청 조립 4층, 추천 근거, 저해상도 비용 범위와 시간 확인
  -> C-05 저해상도 후보 3개 비교
  -> 후보 1개 선택
  -> C-06 선택본 고해상도 비용 범위와 시간 재확인
  -> C-07 선택본 1개만 고해상도 렌더
  -> C-08 결과 확인·편집·저장
  -> P-03 승인 후 발행 검토
  -> P-04 사용자 승인
  -> P-05 발행·예약 진행
  -> P-06 결과·증거
  -> F-01 언어 이해 여부와 자기 피드백
  -> F-02 성과 신호와 피드백 요약
  -> 다음 요청 반영 여부 승인
  -> C-00 또는 R-01
```

happy path의 모든 전진 행동에는 이전 단계로 돌아가는 편집 링크가 있다. 생성 전에는 입력을 보존하고, 생성 뒤에는 새 버전을 만든다. 어떤 화면도 브라우저 뒤로가기만을 유일한 복구 수단으로 쓰지 않는다.

### 35.9 저해상도 3안에서 선택본 고해상도까지

#### C-05 CandidatePreviewGrid

- 후보 수는 항상 3개다.
- 세 후보는 같은 시장, 출력 언어, 목표, 브랜드 제약 아래에서 방향만 다르게 생성한다.
- 각 카드에는 저해상도 미리보기, 방향 이름, 핵심 차이 2개, 추천 근거, 브랜드 제약 상태, 예상 선택본 고해상도 비용 범위와 시간을 표시한다.
- 추천안 1개는 첫 카드에 배치하지만 자동 선택하지 않는다.
- 카드 선택은 단일 선택이다. 선택 후 주행동 `이 후보로 고해상도 확인`이 활성화된다.
- 미선택 카드 하단에는 `나중에 이 후보를 고해상도로 만들면 추가 과금됩니다`를 상시 표시한다.
- 세 후보를 모두 고해상도로 만드는 묶음 행동은 제공하지 않는다.

#### C-06 HighResolutionUpgradeReview

정보 순서는 다음과 같다.

1. 선택한 후보 썸네일과 변경 불가 조건
2. 예상 비용 범위와 잔액
3. 예상 소요 시간
4. 비용 변동 요인
5. 품질 게이트 실패 시 무과금 원칙
6. 미선택 후보 2개는 현재 과금 대상이 아니며 추후 각자 별도 견적·승인이 필요하다는 고지
7. 주행동 `선택본 고해상도 만들기`

#### 미선택 후보의 추후 고해상도

```text
C-08 또는 생성 이력
  -> 미선택 후보 보기
  -> 후보 1개 선택
  -> 별도 비용 범위·잔액·시간 확인
  -> 사용자 명시 승인
  -> 새 고해상도 작업
```

기존 결제 승인을 재사용하지 않는다. 과거 선택본과 같은 요청이라도 새로운 비용이므로 매번 별도 견적을 보여준다.

#### 비용 카피 계약

- 고정 가능한 값: `예상 18크레딧`
- 변동 가능한 값: `예상 18~26크레딧`
- 변동 사유: `영상 길이와 출력 해상도에 따라 달라집니다`
- 시간: `저해상도 3안 약 1~2분`, `선택본 고해상도 약 4~7분`
- 잔액 부족: `현재 잔액으로는 최대 예상치까지 실행할 수 없습니다`

숫자는 예시 표현 형식이며 실제 단가를 확정하지 않는다. 기술설계는 견적 응답의 근거 시각, 만료, 변동 상한과 실제 차감 reconciliation을 합의해야 한다.

### 35.10 전체 화면 상태 분기표

아래 표는 v9.4에서 살아 있는 모든 화면의 happy, empty, error, loading 경로를 다시 고정한다. `empty`는 데이터가 없을 때의 정상 상태다. `error`는 원인과 복구 행동을 함께 보여준다. `loading`은 이전 입력과 마지막 정상값을 지우지 않는다.

#### 시작·제작·편집

| ID | happy | empty | error | loading | 복구·종료 |
|---|---|---|---|---|---|
| G-00 | 최근 작업과 새 제작 진입 | 최근 작업 없음, C-00 CTA | 홈 조회 실패, 재시도·지원 | 최근 작업 skeleton, 새 제작 CTA 유지 | C-00 또는 최근 작업 |
| C-00 | 시장·출력 언어 확정 | 추천값 카드와 직접 입력 | 지원 조합 조회 실패, 안전 추천·재시도 | 추천 계산 skeleton, 수동 선택 가능 | C-01 또는 G-00 |
| C-01 | 목표·대상 확정 | 추천 목표·대상 | 근거 조회 실패, 직접 선택 | 추천 근거 skeleton, 선택 가능 | C-02 또는 C-00 |
| C-02 | 공통 지식 근거 확인 | 근거 없음 고지, 기본 원칙 적용 | 근거 일부 실패, 제외·재시도 | 출처 행별 skeleton | C-03 또는 C-01 |
| C-03 | 스타일·우선순위 확정 | 안전 추천 스타일 | 취향 조회 실패, 무취향 진행 | 추천 카드 skeleton | C-04 또는 C-02 |
| C-04 | 4층·비용·시간·잔액 확인 | 각 층 기본값을 분리 표시 | 충돌 층만 차단, 입력 보존 | 4개 행 개별 skeleton | C-05 또는 오류 층 편집 |
| C-05 | 저해상도 3안 비교 | 후보 없음, 무과금 재생성 | 1~2개 실패는 부분 결과와 재시도, 3개 실패는 무과금 | 카드별 진행률과 취소 | C-06 또는 C-04 |
| C-06 | 선택본 고해상도 견적 승인 | 선택 없음, C-05로 복귀 | 견적 만료·잔액 부족, 새 견적·충전 | 견적 계산 skeleton, 실행 비활성 | C-07 또는 C-05 |
| C-07 | 선택본 1개 렌더 진행 | 작업 없음, C-05로 복귀 | 품질 반려 무과금·재시도, 시스템 실패 증거 | 단계·예상 남은 시간·취소 규칙 | C-08 또는 C-06 |
| C-08 | 고해상도 결과·다음 행동 | 결과 없음, 이력·재시도 | 일부 자산 실패, 성공 자산 보존 | 결과 슬롯별 skeleton | E-01·P-03·F-01 |
| E-01 | 편집기와 저장된 원본 | 편집 대상 없음, C-08 | 자산 로드 실패, 원본 유지 | 캔버스 skeleton, 메타 유지 | E-02·C-08 |
| E-02 | 편집 변경 미리보기 | 변경 없음, 저장 비활성 | 미리보기 실패, 편집값 보존 | 변경 영역만 loading | E-03·E-01 |
| E-03 | 새 버전 저장 완료 | 저장할 변경 없음 | 저장 실패, 로컬 변경 보존 | 저장 중 중복 제출 차단 | C-08·P-03 |

#### 계정 연결과 계정 전환

| ID | happy | empty | error | loading | 복구·종료 |
|---|---|---|---|---|---|
| A-01 | 연결 가능한 채널 목록 | 지원 채널 없음 고지 | 목록 조회 실패·재시도 | 채널 카드 skeleton | A-02·T-01 |
| A-02 | 권한 설명과 연결 시작 | 선택 채널 없음, A-01 | OAuth 시작 실패·재시도 | 연결 창 준비 | A-03·A-01 |
| A-03 | 외부 권한 승인 | 승인할 권한 없음 | 거절·취소·콜백 오류를 분리 | 외부 응답 대기·취소 | A-04·A-02 |
| A-04 | 연결 계정 readback | 계정 없음, 다시 연결 | 계정 목록 실패·재인증 | 계정 목록 skeleton | A-05·A-02 |
| A-05 | 게시 대상 계정 선택 | 선택 가능한 계정 없음 | 권한 부족·재연결 | 계정 전환 중 기존 선택 유지 | A-06·A-04 |
| A-06 | capability 확인 | 기능 정보 없음, 안전 제한 | 정책 조회 실패·수동 발행 안내 | 채널별 capability skeleton | A-07·A-05 |
| A-07 | 연결 완료 | 완료할 연결 없음 | 저장 실패·재시도 | 연결 저장 중 | G-00·P-01 |
| A-08 | 만료 토큰 복구 | 만료 없음 | 재인증 실패·지원 | 재인증 상태·기존 작업 보존 | A-04·O-06 |

#### 발행·승인·품질

| ID | happy | empty | error | loading | 복구·종료 |
|---|---|---|---|---|---|
| P-01 | 채널·계정·권한 준비 | 연결 채널 없음, A-01 | capability 실패·수동 발행 | 준비 상태 skeleton | P-02·A-01 |
| P-02 | 채널별 결과 미리보기 | 미리보기 없음, C-08 | 일부 채널 실패, 성공 채널 유지 | 탭별 skeleton | P-03·E-01 |
| P-03 | 승인 후 발행 검토 | 발행 대상 없음 | 시장·언어·브랜드·권한 충돌 | 검토 항목별 계산 | P-04·P-02 |
| P-04 | 사용자 명시 승인 | 승인 대상 없음 | 승인 저장 실패, 발행 금지 | 승인 처리 중 중복 차단 | P-05·P-03 |
| P-05 | 발행·예약 진행 | 작업 없음 | 채널별 실패와 재시도 | 채널별 진행률·취소 가능 여부 | P-06·O-02 |
| P-06 | 결과·증거·게시물 링크 | 발행 결과 없음 | 증거 일부 누락, 검증 대기 | 증거 수집 중 | R-01·F-01·P-03 |
| M-01 | 발행 모드 요약 | 설정 없음, 승인 후 발행 기본 | 정책 조회 실패, 기본값 유지 | 정책 skeleton | M-02·P-03 |
| M-02 | 자율 발행 옵션 확인 | 옵션 미설정, 기본 유지 | 권한·브랜드 충돌 시 저장 차단 | 영향 범위 계산 | P-03·M-01 |
| V-01 | 승인 요청함 | 대기 승인 없음 | 목록 실패·재시도 | 승인 행 skeleton | V-02·G-00 |
| V-02 | 승인 상세·행동 | 대상 소실, 목록으로 | 만료·충돌·권한 오류 | 최신 상태 확인 | P-04·V-01 |
| Q-01 | 품질 게이트 결과 | 검사 없음 | 반려 이유·무과금·수정 경로 | 검사 항목별 진행 | C-03·E-01·P-03 |

#### 성과·다국어 피드백

| ID | happy | empty | error | loading | 복구·종료 |
|---|---|---|---|---|---|
| R-01 | 게시물별 핵심 성과 | 데이터 없음, 기간·연결 안내 | 채널별 수집 실패 | 마지막 수집 시각과 skeleton | R-02·A-08 |
| R-02 | 시장·언어·채널 비교 | 비교 표본 부족 | 일부 지표 실패·성공값 유지 | 비교 행별 skeleton | R-03·F-01 |
| R-03 | 다음 요청 추천 | 추천 근거 부족, 수동 시작 | 추천 생성 실패·C-00 | 추천 계산 중 | C-00·R-01 |
| F-01 | 이해 여부와 자기 피드백 저장 | 피드백 없음, 3개 선택지 | 저장 실패, 입력 보존 | 제출 중 중복 차단 | F-02·C-08 |
| F-02 | 성과와 피드백 분리 요약 | 성과·피드백 부족 상태 명시 | 일부 신호 실패, 단정 금지 | 신호별 skeleton | C-00·R-01·F-01 |

#### SNS 무연결 제작 체험·내보내기

| ID | happy | empty | error | loading | 복구·종료 |
|---|---|---|---|---|---|
| T-01 | 연결 없이 제작 시작 | 체험 가능 항목 없음 | 자격 조회 실패·연락처 경로 | 체험 범위 loading | T-02·TR-00 |
| T-02 | 제한된 제작 결과 | 결과 없음, 재시도 | 생성 실패·무과금 | 진행률·예상 시간 | T-03·C-00 |
| T-03 | 저장·연결·가입 선택 | 보존할 결과 없음 | 저장 실패·다운로드 | 보존 처리 중 | A-01·TR-00·G-00 |
| TR-00 | 체험 소개 | 제공 체험 없음 고지 | 정책 조회 실패·연락처 | 정책 loading | TR-01·G-00 |
| TR-01 | 연락처 입력 | 값 없음·예시 도움말 | 형식·중복·동의 오류 | 검증 중 | TR-02·TR-00 |
| TR-02 | 연락처 확인 | 확인 대상 없음 | 만료·오입력·재전송 | 확인 대기·남은 시간 | TR-03·TR-01 |
| TR-03 | 체험 생성 | 요청 없음, C-00 | 생성 실패·무과금·지원 | 단계·예상 시간 | TR-04·C-00 |
| TR-04 | 체험 결과·가입 전환 | 결과 없음·재시도 | 보존 실패·다운로드 | 결과 준비 중 | G-00·A-01·C-00 |
| X-01 | 내보낼 범위 선택 | 내보낼 데이터 없음 | 범위 조회 실패·재시도 | 항목 수 계산 | X-02·설정 |
| X-02 | 형식·비용·기간 확인 | 선택 범위 없음 | 견적·권한 오류 | 파일 크기 계산 | X-03·X-01 |
| X-03 | 내보내기 진행 | 작업 없음 | 부분 실패·성공 파일 보존 | 항목별 진행률 | X-04·O-02 |
| X-04 | 완료·다운로드 | 결과 없음·다시 요청 | 링크 만료·재생성 | 서명 링크 생성 중 | 설정·X-01 |

#### 운영자 O-00부터 O-12

| ID | happy | empty | error | loading | 복구·종료 |
|---|---|---|---|---|---|
| O-00 | 예외 6개와 우선순위 | `지금 처리할 예외 없음` | 일부 카드 실패, 나머지 유지 | 카드별 skeleton | O-01·O-02·O-05·O-06·O-08·O-10 |
| O-01 | 잔액 위험 고객 | 위험 고객 없음 | 원장 조회 실패·재시도 | 잔액 행 skeleton | 고객 상세·O-00 |
| O-02 | 실패 작업 큐 | 실패 없음 | 큐 조회 실패·진단 | 실패 행 skeleton | O-07·O-10 |
| O-03 | 고객·테넌트 검색 | 결과 없음 | 검색 실패·조건 완화 | 검색 결과 skeleton | O-04·O-00 |
| O-04 | 고객 상세 | 고객 없음 | 일부 패널 실패·증거 보존 | 패널별 skeleton | O-08·O-10·O-11 |
| O-05 | 대기 큐 | 지연 없음 | 큐 상태 실패·진단 | 큐 행 skeleton | O-02·O-07 |
| O-06 | 토큰 만료 | 만료 없음 | 갱신 실패·고객 연락 | 만료 행 skeleton | A-08·O-08 |
| O-07 | 작업 진단 | 이상 없음 | 진단 실패·원본 로그 링크 | 단계별 skeleton | O-02·O-10 |
| O-08 | 고객 막힘 | 막힌 고객 없음 | 사유 조회 실패·지원 | 막힘 행 skeleton | O-04·O-10·O-11 |
| O-09 | 이벤트·감사 이력 | 이벤트 없음 | 수집 지연·마지막 정상값 | 이벤트 skeleton | O-04·O-00 |
| O-10 | 환불 근거 | 환불 후보 없음 | 원장·작업 증거 불일치 | 증거 묶음 skeleton | O-11·O-04 |
| O-11 | 지원 조치 | 열림 요청 없음 | 전송 실패·초안 보존 | 처리 중 중복 차단 | O-12·O-08 |
| O-12 | 조치 결과·감사 | 결과 없음 | 감사 기록 실패, 조치 완료 주장 금지 | 증거 저장 중 | O-00·O-04 |

### 35.11 주요 edge·error 분기

| 발생 조건 | 사용자에게 보이는 것 | 자동 처리 | 사용자 선택 | 종료점 |
|---|---|---|---|---|
| 시장과 출력 언어 불일치 | `영어권 시장에 일본어 출력으로 진행합니다` readback | 자동 수정 없음 | 유지·추천 조합 적용 | C-01 |
| 미지원 출력 언어 | 보장 범위와 제한 | 안전 모델 확인 | 계속·지원 언어 변경 | C-01·C-00 |
| 한국어·영어 외 언어 | 품질 보장 수준과 평가 계획 | 성과·피드백 루프 준비 | 계속·언어 변경 | C-01 |
| 공통 지식 없음 | 출처 없음과 기본 원칙 적용 | 안전 기본값 | 계속·시장 변경 | C-03·C-00 |
| 개인 취향 없음 | `처음이라 취향 이력이 없습니다` | 무취향 추천 | 계속 | C-04 |
| 브랜드 제약 없음 | 안전 기본값과 설정 유도 | 위험 표현 기본 차단 | 계속·설정 | C-05·브랜드 설정 |
| 브랜드 제약 충돌 | 충돌 항목과 영향 후보 | 생성 차단 | 입력 수정·권한 있는 설정 변경 | C-03·브랜드 설정 |
| 질문 건너뛰기 | 적용한 추천값 readback | 추천값을 요청에 명시 | 되돌려 수정·계속 | 다음 단계 |
| 후보 1개 실패 | 성공 2개와 실패 카드 | 실패 카드 무과금 | 해당 카드만 재시도·전체 다시 만들기 | C-05 |
| 후보 2개 실패 | 성공 1개, 비교 부족 경고 | 실패 2개 무과금 | 2개 재시도·1개 선택 | C-05·C-06 |
| 후보 3개 실패 | 실패 원인과 무과금 | 안전 재시도 가능 여부 확인 | 다시 만들기·지원 | C-04·O-02 |
| 후보 미선택 | 주행동 비활성, 선택 안내 | 없음 | 카드 1개 선택 | C-06 |
| 견적 만료 | 이전 견적과 새 견적 차이 | 재계산 | 새 견적 승인·취소 | C-06·C-05 |
| 잔액이 예상 상한 미만 | 부족액과 상한 | 실행 차단 | 충전·저비용 옵션·취소 | C-06 |
| 고해상도 품질 반려 | 이유·반복 횟수·무과금 | 상한 내 수정 재시도 | 입력 수정·지원 | Q-01·C-03 |
| 미선택 후보 고해상도 요청 | 별도 과금·별도 시간 | 새 견적 | 승인·취소 | C-07·C-08 |
| 승인 전 발행 시도 | 승인 필요 상태 | 발행 차단 | 승인 요청·초안 유지 | P-04·C-08 |
| 자율 발행 선택 | 영향 채널·계정·취소 규칙 | 정책 검증 | 명시 확인·기본 유지 | M-02·P-03 |
| 토큰 만료 | 영향 채널과 예약 | 해당 발행 보류 | 재인증·다른 채널·초안 | A-08·P-03 |
| 사용자가 출력 언어를 모름 | `언어 자체는 판단 어려움` 선택 | 성과와 다른 피드백을 분리 | 시각·브랜드·목표 피드백 | F-02 |
| 성과 표본 부족 | 표본 수와 기간 | 결론 생성 안 함 | 기간 늘리기·정성 피드백 | F-01·R-01 |
| 사용자 피드백과 성과 충돌 | 두 신호를 병렬 표시 | 자동 취향 변경 없음 | 다음 요청 반영·보류 | F-02·C-00 |
| 운영 카드 일부 실패 | 실패 카드에 마지막 정상 시각 | 정상 카드 갱신 | 카드 재시도·상세 | O-00 |

### 35.12 발행 정책 흐름

#### 기본값

새 테넌트, 새 채널, 새 캠페인의 기본 모드는 모두 `승인 후 발행`이다.

```text
C-08 결과 저장
  -> P-01 계정·채널 준비
  -> P-02 채널별 미리보기
  -> P-03 발행 대상·시장·출력 언어·브랜드 제약 readback
  -> P-04 사용자 승인
  -> P-05 발행 또는 예약
  -> P-06 결과·증거
```

#### 자율 발행 옵션

자율 발행은 M-02에서 별도로 켠다.

- 적용 대상: 테넌트 전체가 아니라 선택 채널·계정·콘텐츠 유형 범위
- 켜기 전 정보: 영향 범위, 금지 콘텐츠, 실패 시 보류, 토큰 만료, 중지 방법
- 예외: 브랜드 충돌, 품질 게이트 주의·반려, 미지원 capability, 처음 쓰는 출력 언어는 항상 승인으로 회귀
- 되돌리기: M-01에서 즉시 `승인 후 발행`으로 변경
- 감사: 적용 시각, 변경자, 적용 대상, 실제 발행 결과를 보존

자율 발행이 켜져도 C-08에서 현재 적용 모드를 보여준다. 사용자가 모르는 상태에서 자동으로 발행하지 않는다.

### 35.13 다국어 품질 판정 루프

#### 화면 경로

```text
C-08 결과
  -> `이 결과의 언어 품질 남기기`
  -> F-01 이해 여부와 자기 피드백
  -> F-02 성과 신호·피드백 요약
  -> `다음 요청에 반영` 또는 `이번만 기록`
  -> C-00 새 요청 또는 R-01 성과
```

P-06 발행 결과와 R-02 시장·언어 비교에도 F-01 진입 링크를 둔다. 따라서 사용자는 생성 직후, 발행 직후, 성과 확인 뒤 어느 시점에서도 피드백을 남길 수 있다.

#### F-01 입력

질문 A: `이 출력 언어를 직접 이해하나요?`

- `잘 이해함`
- `일부 이해함`
- `판단 어려움`

질문 B: `이번 결과가 목표에 얼마나 맞았나요?`

- 목표 적합 5단계
- 브랜드 느낌 적합 5단계
- 시장 적합 5단계 또는 `판단 어려움`

질문 C: `다음에 바꾸고 싶은 점이 있나요?`

- 자연스러운 표현
- 금기·문화 맥락
- 브랜드 톤
- CTA
- 시각 스타일
- 기타 짧은 메모

F-01은 피드백 화면이므로 제작 질문 3개 상한에 포함하지 않는다. 한 화면에서 단계적으로 펼치며, `판단 어려움`을 정당한 답으로 취급한다.

#### 이해하지 못하는 언어의 판정

사용자가 `판단 어려움`을 고르면 다음을 분리한다.

1. 사용자가 직접 판단 가능한 목표·브랜드·시각 만족도
2. 게시 뒤 관찰 가능한 도달·반응·클릭·부정 피드백·신고·수정 요청
3. 언어 자연스러움처럼 아직 판단할 수 없는 항목

F-02는 1과 2를 근거로 다음 추천을 제시하되 3을 `검증되지 않음`으로 남긴다. 높은 클릭률을 자연스러운 언어의 증거로 치환하지 않는다. 낮은 성과도 언어 문제로 단정하지 않는다.

#### 선호 학습 안전장치

- 사용자의 승인·거절·편집 이력을 개인 취향 층의 후보 근거로 쓸 수 있다.
- F-01 피드백은 `다음 요청에 반영`을 사용자가 승인해야 지속 선호에 들어간다.
- 출력 언어별 선호 프로필을 분리한다. 일본어 결과의 편집을 한국어 선호에 자동 전파하지 않는다.
- 시장 공통 지식의 오류 신고는 개인 취향으로 흡수하지 않고 별도 검토 대상으로 보낸다.
- 브랜드 제약 위반 신고는 Q-01 품질 게이트와 브랜드 설정 검토로 보낸다.

### 35.14 1인 운영자 홈

#### O-00 정보 구조

O-00의 첫 화면에는 다음 6개 카드만 둔다.

| 순서 | 카드 | 보여주는 최소 정보 | 상세 경로 | 주행동 |
|---|---|---|---|---|
| 1 | 잔액 | 위험 고객 수, 최저 잔액, 마지막 원장 시각 | O-01 | 고객 확인 |
| 2 | 실패 | 실패 작업 수, 가장 오래된 실패, 환불 후보 표시 | O-02 | 실패 진단 |
| 3 | 큐 | 대기 수, 최장 대기 시간, 지연 기준 | O-05 | 큐 보기 |
| 4 | 토큰 만료 | 만료·임박 채널 수, 영향 예약 수 | O-06 | 재인증 요청 |
| 5 | 고객 막힘 | 진행 불가 고객 수, 대표 원인 | O-08 | 막힘 풀기 |
| 6 | 환불 근거 | 근거 완비·누락 건수, 검토 대기 | O-10 | 증거 검토 |

카드 우선순위는 `고객 피해 가능성 > 금전 위험 > 시간 지연`이다. 정상 상태에서는 큰 KPI 숫자 대신 `처리할 예외 없음`과 마지막 점검 시각을 보여준다.

#### 비범위

- 매출 추세, 코호트, 퍼널, 캠페인 성과 비교
- 고객 세그먼트, 예측, 이상치 탐지, 장기 추세
- 채널별 성과 BI, 마케팅 ROI, 실험 분석
- 운영자용 커스텀 리포트 빌더

필요한 단일 고객·작업·원장 증거는 기존 O-03·O-04·O-07·O-09·O-11·O-12에서 본다. O-00에 BI를 추가하지 않는다.

### 35.15 레이아웃·밀도·overflow 계약

#### 토큰 계승

- 브랜드 형용사: 정직한, 통제 가능한, 증거 중심
- color: 기존 `bg`, `surface`, `surface-2`, `border`, `text`, `muted`, `subtle`, `accent`, `success`, `warning`, `danger`
- typography: 12, 13, 15, 17, 20, 24
- spacing: 4, 8, 12, 16, 24, 32, 48
- radius: 8, 12, full
- motion: 120ms, 200ms, 320ms
- 최소 터치 영역: 44px

신규 color·typography·spacing·radius 토큰은 0개다.

#### 주축·순서·열수

| 화면 | 1024 | 390 | 주축·순서 |
|---|---|---|---|
| C-00·C-01·C-03 | 12열 중 본문 7열, 추천 5열 | 1열 | desktop flex-row, mobile flex-column. 제목→추천→입력→비용·시간→행동 |
| C-02 | 출처 목록 8열, 적용 요약 4열 | 1열 | desktop row, mobile column. 요약이 목록보다 먼저 |
| C-04 | 4층 요약 8열, 견적 rail 4열 | 1열 | desktop row, mobile column. 시장→공통 지식→취향→브랜드→견적 |
| C-05 | 후보 3열 고정 | 1열 카드 3개 | desktop grid 3 columns, mobile flex-column. 추천 후보가 첫 카드 |
| C-06 | 선택본 7열, 견적 rail 5열 | 1열 | desktop row, mobile column. 썸네일→비용→추가 과금 안내→행동 |
| C-08 | 결과 8열, 다음 행동 4열 | 1열 | desktop row, mobile column. 결과→상태→편집·발행·피드백 |
| F-01·F-02 | 입력·신호 8열, 설명 4열 | 1열 | desktop row, mobile column. 판단 가능 신호가 먼저 |
| O-00 | 3열 × 2행 | 1열 6카드 | desktop grid 3 columns, mobile flex-column. 우선순위 순 |

#### 간격과 정렬

- 페이지 padding: 1024에서 32, 390에서 16
- 섹션 gap: 32
- 카드 내부 padding: 24, 모바일 16
- 카드 간 gap: 16
- 라벨과 값 gap: 8
- 칩 간 gap: 8
- 후보 썸네일과 설명 gap: 16
- 모든 카드 제목, 입력 시작, 하단 행동 rail은 같은 수직 정렬선을 공유한다.

#### 데이터 0·정상·과다

| 영역 | 0 | 정상 | 과다 |
|---|---|---|---|
| 시장 | 추천값과 직접 입력 | 최근 4개 + 검색 | 최근 4개 고정, 나머지 검색·가상 목록 |
| 공통 지식 | 출처 없음 고지 | 상위 2개 + 전체 보기 | 상위 2개 고정, 나머지 접힘·가상 목록 |
| 취향 | 이력 없음 | 핵심 칩 3개 | 상위 3개 + `외 N개`, 전체 보기 |
| 브랜드 제약 | 안전 기본값 | 필수·금지 각 3개 이하 | 충돌 우선, 나머지 접힘·검색 |
| 후보 | 0이면 무과금 재시도 | 항상 3개 | 3개를 넘기지 않음 |
| 운영 예외 | 정상 empty | 카드당 상위 3건 | 총수+상위 3건, 상세에서 pagination |

#### 긴 텍스트·이미지·목록

- 시장명·언어명·계정명은 한 줄 말줄임과 전체값 tooltip·접근성 이름을 제공한다.
- 추천 근거는 기본 2줄, `근거 전체 보기`로 확장한다.
- URL과 해시·작업 ID는 `overflow-wrap:anywhere`를 적용한다.
- 사용자 메모는 3줄까지 보이고 전체 보기는 별도 펼침이다.
- 후보 이미지 비율은 고정하고 `object-fit:cover`를 쓴다. 원본 전체 보기를 별도 제공한다.
- 390에서 버튼 두 개 이상은 수직 8 간격으로 쌓고, 비용·시간 고지를 버튼보다 위에 둔다.
- 카드 내부 이중 스크롤을 금지한다. 긴 목록은 페이지 scroll 또는 명시적 pagination을 쓴다.

### 35.16 DESIGN.md 개정 근거

이번 과제는 유저플로우 정본을 먼저 만드는 작업이다. 아래 신규 패턴은 다음 DESIGN.md 최신화에서 컴포넌트 인벤토리와 상태 계약으로 반영해야 한다.

| 제안 컴포넌트 | 쓰는 화면 | 필수 상태 | 기존 토큰·패턴 계승 |
|---|---|---|---|
| MarketLanguageSelector | C-00 | 추천·직접 선택·미지원·loading | Field, Card, Badge |
| MarketLanguageBadge | C-01부터 P-06 | 확정·추천값·주의 | Badge |
| QuestionBudgetIndicator | C-00·C-01·C-03 | 1/3·2/3·3/3 | progress semantics, 기존 typography |
| RecommendationCard | C-00·C-01·C-03 | 근거 있음·근거 부족·loading | Card, status color |
| MarketingKnowledgeEvidence | C-02 | 0·정상·과다·부분 실패 | Section, EmptyState |
| RequestLayerSummary | C-04 | 4층 happy·empty·error·loading | Card, Stack, StatusBadge |
| CandidatePreviewGrid | C-05 | 3성공·부분 실패·전부 실패·loading | Card, radio semantics |
| HighResolutionUpgradeReview | C-06 | 견적·만료·잔액 부족·loading | Confirm rail, warning token |
| LanguageQualityFeedback | F-01 | 이해·일부 이해·판단 어려움·저장 오류 | Field, segmented control |
| PerformanceSignalSummary | F-02 | 충분·부족·충돌·loading | DataState, EvidenceLink |
| OperatorExceptionHome | O-00 | 6카드 happy·empty·partial error·loading | 기존 operator card |

#### 금지 패턴 추가 근거

- UI 언어를 출력 언어로 묵시적으로 사용
- 3개를 넘는 질문형 온보딩
- 추천 근거·비용·시간을 다른 화면에 숨김
- 미선택 후보까지 자동 고해상도 생성·차감
- 추가 과금 고지를 실행 뒤에 표시
- 승인 설정을 건드리지 않았는데 자율 발행
- 성과 지표만으로 모르는 언어의 품질을 확정
- O-00에 BI·추세·퍼널·예측을 추가

DESIGN.md 현행과 이 문서의 토큰은 정합한다. 다만 위 컴포넌트 인벤토리와 금지 패턴은 아직 DESIGN.md에 반영되지 않았으므로 다음 디자인 루프의 필수 작업이다.

### 35.17 요구 추적성

| 요구 | 입력 화면 | 확인 화면 | 결과·복구 화면 | 검증 포인트 |
|---|---|---|---|---|
| 시장 먼저, 출력 언어 파라미터 | C-00 | C-04, 전 화면 badge | C-08·P-03·F-01 | G-00 새 제작 다음이 C-00인지 |
| 요청 조립 4층 | C-00·C-02·C-03·브랜드 설정 | C-04 4개 행 | C-05·C-08·F-01 | 네 층의 출처·수정 권한이 분리되는지 |
| 저해상도 3개, 선택 1개 고해상도 | C-04 | C-05·C-06 | C-07·C-08 | 고해상도 job이 선택 ID 하나만 받는지 |
| 미선택 추가 과금 | C-05 카드 하단 | C-06 고지 | 생성 이력 별도 견적 | 기존 승인 재사용이 없는지 |
| 질문 3개 상한·건너뛰기 | C-00·C-01·C-03 | QuestionBudgetIndicator | C-04 추천값 readback | 질문형 화면이 3개 이하인지 |
| 추천 1개·근거·비용·시간 | C-00·C-01·C-03 | C-04·C-06 | 견적 만료 복구 | 같은 시야에 있는지 |
| 승인 후 발행 기본 | M-01 기본값 | P-03·P-04 | P-05·P-06 | 승인 없이 발행되지 않는지 |
| 자율 발행 옵션 | M-02 | C-08·P-03 mode badge | 예외 시 승인 회귀 | 명시적 범위·감사·중지가 있는지 |
| 다국어 품질 루프 | F-01 | F-02 | C-00·R-01 | 이해 못 함을 정당한 상태로 다루는지 |
| 운영자 예외 6개 | O-00 | O-01·O-02·O-05·O-06·O-08·O-10 | O-12 | BI가 첫 화면에 없는지 |

### 35.18 dead-end 감사

| 막힘 | 뒤로 | 대체 경로 | 보존 | dead-end |
|---|---|---|---|---|
| 시장 추천 실패 | G-00 | 직접 선택 | 새 요청 초안 | 0 |
| 출력 언어 미지원 | C-00 | 지원 언어·제한 동의 | 목표 입력 없음 | 0 |
| 공통 지식 실패 | C-01 | 안전 기본값·특정 근거 제외 | 시장·목표 | 0 |
| 브랜드 충돌 | C-03 | 입력 수정·브랜드 설정 | 3질문 답변 | 0 |
| 저해상도 일부 실패 | C-04 | 실패 카드만 재시도·성공안 선택 | 성공 후보 | 0 |
| 저해상도 전부 실패 | C-04 | 무과금 재시도·지원 | 요청 4층 | 0 |
| 후보 미선택 | C-05 유지 | 추천 후보 선택 | 세 후보 | 0 |
| 견적 만료 | C-05 | 새 견적 | 선택 후보 | 0 |
| 잔액 부족 | C-06 | 충전·저비용 옵션·초안 | 요청·후보 | 0 |
| 고해상도 실패 | C-06 | 무과금 재시도·입력 수정 | 선택·비용 승인 기록 | 0 |
| 승인 거절 | P-03 | 편집·초안 | 결과 | 0 |
| 토큰 만료 | P-03 | 재인증·다른 채널·초안 | 결과·예약 | 0 |
| 성과 없음 | P-06 | 즉시 피드백·기간 변경 | 결과·발행 증거 | 0 |
| 언어 판단 어려움 | F-01 | 판단 가능한 항목만 저장 | 결과·시장·언어 | 0 |
| 피드백 저장 실패 | F-01 | 재시도·메모 복사 | 입력 로컬 보존 | 0 |
| 운영자 카드 실패 | O-00 | 카드 재시도·상세 직접 진입 | 다른 카드 | 0 |

dead-end 합계: 0.

### 35.19 회수 필요

#### ⛔ 회수 필요: 기술설계에서 합의할 것

1. 출력 언어 보장 등급과 미지원 조합을 내려주는 계약
2. 4층 요청 조립에서 각 층의 버전·출처·적용 제외를 전달하는 계약
3. 저해상도 3안과 선택본 고해상도 간 식별자·계보·재실행 계약
4. 견적 범위, 견적 만료, 실제 차감, 품질 반려 무과금의 원장 정합
5. 언어별 개인 취향의 격리와 사용자 승인 뒤 학습 반영
6. 성과 신호와 자기 피드백의 표본 부족·충돌 상태 계약
7. 운영자 6개 예외 카드의 집계 기준과 실제 증거 상세 연결

이 항목은 API·DB·비용 정책이므로 디자인 단계에서 임의 확정하지 않는다.

#### ⛔ 회수 필요: plan reopen 후보

- 한국어·영어 외 출력 언어의 약속 수준이 PRD의 지원 정책과 판매 카피에서 같은지 확인해야 한다.
- `시장`의 단위가 국가, 언어권, 채널 지역 중 무엇인지 제품 계약으로 고정되지 않으면 추천 근거와 성과 비교가 흔들린다.
- 사용자 자기 피드백을 개인 취향에 반영하는 보존 기간·삭제 권리·설명 의무가 미정이다.
- 환불 근거의 최소 증거 묶음과 자동·수동 환불 권한이 미정이다.

### 35.20 레드팀

#### 까다로운 고객의 공격

`질문을 세 개만 물어도 결국 확인 화면이 많아 번거롭다. 추천이 내 취향을 빙자해 시스템 편한 값으로 밀어붙일 수 있다. 후보는 저해상도라 실제 품질을 비교하기 어렵고, 나중에 다른 후보를 고해상도로 만들 때 돈을 또 내야 한다. 모르는 언어 성과가 낮아도 왜 낮은지 알 수 없다.`

수정 반영:

- 질문 화면과 검토 화면을 구분하고, 모든 질문에 건너뛰기와 적용값 readback을 넣었다.
- 추천 카드에 근거·비용·시간을 같은 시야에 두고 자동 선택을 금지했다.
- C-05 후보 카드마다 고해상도 예상 범위와 추가 과금 고지를 상시 표시했다.
- F-02는 성과와 언어 품질을 인과로 합치지 않고, 판단 불가 상태를 남긴다.

#### 경쟁자의 공격

`3안 생성과 4층 근거를 보여주면 첫 결과가 늦어진다. 자율 발행을 기본으로 하는 경쟁 서비스보다 자동화 체감이 약하다.`

수정 반영:

- 구조화 입력을 3질문으로 제한하고 추천값으로 건너뛰게 해 입력 비용을 통제했다.
- 저해상도 3안은 빠른 탐색에 집중하고 고비용 고해상도는 선택 1안으로 제한했다.
- 승인 후 발행은 속도보다 브랜드·금전·채널 위험을 통제하는 포지셔닝이다. 자율 발행은 범위가 명시된 옵션으로 남겼다.

#### 회의적 투자자의 공격

`운영 홈에서 BI를 빼면 사업 상태를 모른다. 다국어 품질을 자기 피드백과 성과로 보는 것은 정확도가 낮다.`

수정 반영:

- O-00은 1인 운영자의 당일 장애 복구 화면으로 범위를 고정했다. 사업 BI는 이 제품 범위가 아니다.
- 다국어 품질은 정확하다고 가장하지 않는다. 이해 여부, 자기 피드백, 성과 신호, 미검증 항목을 분리해 오판 비용을 낮춘다.

### 35.21 셀프심문

**질문: 이 결론이 틀렸다면 가장 그럴듯한 이유는 무엇인가?**

가장 load-bearing한 가정은 `저해상도 3안이 선택 피로를 줄이면서도 고해상도 선택을 할 만큼 차이를 전달한다`는 것이다. 저해상도 때문에 텍스트·질감·세부 완성도를 판정하지 못하면 사용자는 추천안에 기대거나 고해상도를 여러 개 구매하게 되고, 비용 통제라는 설계 목적이 무너진다.

**자답:** 후보는 방향 비교에 필요한 구도, 카피 구조, 색감, 핵심 메시지 차이를 보여줘야 한다. 텍스트 가독성이나 디테일이 선택 기준인 유형은 정지 썸네일만으로 부족하다.

**수정:** C-05에 후보별 핵심 차이 2개, 적용 근거, 브랜드 제약 상태, 고해상도 예상 범위를 넣었다. 다음 prototype은 이미지·영상·텍스트형 각각에서 3안의 차이가 390과 1024에서 실제로 구분되는지 검증해야 한다. 구분되지 않으면 후보 렌더 사양 또는 비교 정보가 plan gap이며, 고해상도 다중 생성으로 우회하지 않는다.

두 번째 load-bearing 가정은 `사용자가 이해 못 하는 언어도 성과와 자기 피드백으로 충분히 개선할 수 있다`는 것이다.

**자답:** 성과는 품질의 대리 신호이고 사용자는 언어 자연스러움을 직접 평가할 수 없다. 잘못 학습할 위험이 있다.

**수정:** F-02에서 판단 가능한 것과 불가능한 것을 분리하고, 지속 취향 반영은 사용자 승인 뒤에만 일어나게 했다. 언어 자연스러움은 별도 검증이 없으면 `미검증`으로 남긴다.

### 35.22 품질 판정

#### Design review

| 축 | 점수 | 근거 |
|---|---|---|
| 기존 계승 | A | v9.3 전체 복제, 기존 6흐름·O-00부터 O-12·v24 셸·토큰 보존 |
| 흐름·복구 | A | 전체 화면 happy·empty·error·loading과 주요 edge·dead-end 0 정의 |
| 선택 피로 | A- | 3질문 상한·건너뛰기·추천 1개·근거·비용·시간을 화면 계약으로 고정 |
| 비용 투명성 | A | 저해상도 3안, 선택본만 고해상도, 미선택 추가 과금과 새 승인 명시 |
| 다국어 신뢰 | A- | 성과·자기 피드백·미검증을 분리, 실제 언어 품질 모델은 미검증 |
| 반응형·overflow | B+ | 390·1024 주축·열수·8pt·0·정상·과다 계약, v9.4 신규 렌더 없음 |
| 실제 시각 검증 | 미검증 | v24 렌더는 확인했으나 v9.4 prototype·픽셀 대조는 다음 루프 |

**Design Score: B+ (저충실 유저플로우 정적 review, v9.4 prototype 렌더 미검증)**

등록된 `design-review` 스킬은 현재 도구 목록에 없어 실제 호출로 위조하지 않았다. `/Users/sj/.claude/standards/design.md`의 rubric으로 정적 자체평가했다.

#### 문서 루브릭

| 항목 | 점수 | 근거 |
|---|---|---|
| 완결성 | 5/5 | 필수 7건, 전체 화면 목록, 모든 화면 4상태, edge·복구·종료점 포함 |
| 정밀성 | 5/5 | 화면 ID, 정보 순서, 질문 예산, 4층 입력·가시성, 과금 시점 명시 |
| 근거 | 5/5 | 기존 구현·PRD·결정·DESIGN·prototype·wiki와 실제 WebSearch 3회 이상 대조 |
| 추적성 | 5/5 | 요구별 입력·확인·결과·검증 포인트 연결 |
| 전문성 | 4/5 | client-ready 저충실 문서지만 v9.4 시각물·기술 계약은 미검증 |

**RUBRIC_SCORE: completeness=5/5 precision=5/5 evidence=5/5 traceability=5/5 professionalism=4/5 total=24/25**

**WEAKEST_LINE:** 시장 단위, 출력 언어 보장 등급, 견적·차감·환불 원장, 언어별 선호 학습의 기술·법무 계약은 아직 합의되지 않았다.

### 35.23 다음 stage 가능 여부

- 디자인 내부 prototype 루프: 가능. C-00, C-04, C-05, C-06, C-08, F-01, F-02, O-00을 390·1024 허브로 연결해야 한다.
- DESIGN.md 최신화: 필요. 35.16의 신규 컴포넌트와 금지 패턴을 현행 DESIGN.md에 반영한 뒤 prototype과 정합 확인해야 한다.
- 디자인 게이트 승인: 아직 불가. v9.4 prototype 렌더, 0·정상·과다 상태, 390·1024 overflow, 등록 design-review B 이상이 미검증이다.
- 기술설계 진입: 아직 불가. 디자인 승인과 35.19의 비싼 결정 합의가 먼저다.
- 코드·QA·배포: 시작하지 않았다.

### 35.24 회귀 감사

- 자기검사 기준: C-00의 타깃 시장 입력, C-04의 타깃 시장 readback, C-05의 타깃 시장 후보 조건, C-08의 타깃 시장 결과 메타, F-01의 타깃 시장 적합성 피드백이 각각 화면 맥락으로 존재한다.
- 필수 용어 직접 검사: `rg` 기준 출력 언어 30회, 타깃 시장 12회, 저해상도 27회, 요청 조립 4층 11회, 추가 과금 10회다. 제목·표·화면 설명·상태·복구 문맥에 반복되어 한 줄 언급으로 끝나지 않았다.
- 원본 v9.3 최상위 장: 35개
- 개정 v9.4 최상위 장: 36개
- 원본 v9.3 줄 수·크기: 2,403줄·194,048B
- 개정 v9.4 줄 수·크기: 3,212줄·252,454B
- v9.4는 v9.3 전체를 복제한 뒤 상단 메타데이터와 35장을 확장했다.
- 유지한 기존 기능: 6개 고객 흐름, O-00부터 O-12, M·V·Q·TR·X 화면, 질문 화법, 계정 readback, 다계정, capability 탭, 390·1024·overflow, 레드팀·셀프심문.
- 추가·변경한 것: C-00, C-01부터 C-08의 최신 의미, F-01·F-02, 요청 조립 4층, 저해상도 3안, 선택본 단일 고해상도, 미선택 추가 과금, 승인 후 발행 기본, 운영 예외 6개 홈.
- 삭제한 기존 절·화면·상태·인터랙션: 0개.
- 신규 color·typography·spacing·radius 토큰: 0개.
- 신규 컴포넌트 후보: 11개. DESIGN.md 반영은 다음 디자인 루프의 필수 작업.

### 35.25 v9.4 SOURCES / MODEL / SKILLS / STAMP

SOURCES:

- `/Users/sj/sj_code_master/openclaw-auto/docs/design-docs/user-flow-openclaw-service-v9.3-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prd-openclaw-service-v8.2.1-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/제품구조-결정-2026-08-15.md` §9.6·§9.7
- `/Users/sj/sj_code_master/openclaw-auto/studio/docs/prd-studio-service-v1.2.1-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/레드팀-제품구상-2026-08-15-gpt-codex.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/구현현황.md`
- `/Users/sj/sj_code_master/openclaw-auto/DESIGN.md`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/openclaw-auto-marketing-agent-fidelity-v24-gpt-codex.html`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/studio-desktop-1024.png`
- `/Users/sj/sj_code_master/openclaw-auto/docs/prototype/qa-v24/studio-mobile-390.png`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/studio/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/dashboard/src/app/operator/page.tsx`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/architecture/two-service-boundary.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/marketing-hub-surface-map.md`
- `/Users/sj/sj_code_master/openclaw-auto/wiki/product/studio.md`
- `/Users/sj/.claude/standards/design.md`
- `/Users/sj/.claude/standards/doc-review.md`
- `/Users/sj/.claude/standards/benchmarks.md`
- `/Users/sj/.claude/standards/artifact-stamp.md`
- [AI-generated onboarding flows](https://www.uxperiment.io/article/ai-generated-onboarding-flows)
- [Rethinking the UI of GenUI](https://arxiv.org/abs/2606.13843)
- [Nielsen Norman Group, Make It Easier for Users to Make Decisions](https://www.nngroup.com/reports/make-decisions/)
- [Userpilot progressive disclosure examples](https://userpilot.com/blog/progressive-disclosure-examples/)
- [SaaS onboarding flow guide](https://productquant.dev/blog/saas-user-onboarding-flow/)
- [Shape of AI, Cost Estimates](https://www.shapeof.ai/patterns/cost-estimates)
- [Ibbaka, credit-based pricing for AI agents](https://www.ibbaka.com/ibbaka-market-blog/a-guide-to-the-design-of-credit-based-pricing-for-ai-agents)
- [Adobe Firefly generative credits FAQ](https://helpx.adobe.com/creative-cloud/apps/generative-ai/generative-credits-faq.html)

MODEL: gpt-codex/gpt-5.6-sol

SKILLS_USED: 없음. 현재 설치된 스킬 중 기존 제품의 저충실 화면·상태·유저플로우를 개정하는 전용 매칭 스킬이 없었다. `design.md`, `doc-review.md`, `benchmarks.md`, `artifact-stamp.md`는 스킬이 아니라 품질헌법으로 직접 읽고 적용했다.

SKILLS_SKIPPED: `imagegen`은 이미지 생성이 없는 Markdown 문서 과제라 스킵했다. `openclaw-creative-brief`는 자동 생성 에이전트용 브리프이며 제품 화면·상태·복구 플로우와 범위가 다르다. `pipeline`은 부모 컨트롤러가 단계 상태와 승인 게이트를 소유하므로 워커 산출물 작성에 호출하지 않았다.

🏷 STAMP | line: openclaw-service | 생성: 2026-08-15 20:22 KST | model: gpt-codex/gpt-5.6-sol | agent: product-designer

skills: matching skill 없음, design.md·doc-review.md·benchmarks.md·artifact-stamp.md 직접 적용 | 근거: v9.3 + PRD v8.2.1 + 제품구조 §9.6·§9.7 + Studio PRD v1.2.1 + 레드팀 + DESIGN.md + prototype v24 + 현재 구현 + 실제 WebSearch 3회 이상

고민: 사용자의 답을 줄이면서도 시장·언어·비용·브랜드 통제를 숨기지 않고, 탐색 비용과 최종 품질 비용을 선택 전후로 분리했다.
