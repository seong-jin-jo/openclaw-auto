# openclaw-auto 구현현황

> 이 문서는 2026-08-10 디자인 시스템 build에서 최초 생성됐다. 기존 현황 문서가 없었으므로 실제 코드·테스트·빌드 결과를 기준으로 작성했다.

## 2026-08-28 v63 성과실 실제 화면

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| 성과 판정 | 최근 30일 표본 수, 5건 판정 문턱, 플랫폼 집중, 핵심 4지표, 보조 6지표를 v63 순서로 표시 | `data-perf-verdict`, 표본 경계 0건과 5건 계약 테스트 통과. 390, 768, 1024, 1440 실캡처에서 섹션 순서 유지 | 로컬 앱 반영, 미배포 |
| 무엇이 통했나 | 상위 글의 실제 본문, 조회, 좋아요, 답글을 유지하고 첫 카드에서 성과 기반 제안 API를 호출 | 정상 제안 호출과 실패 뒤 재시도 계약 통과. 제안 큐 기존 API와 출처 근거 보존 | 로컬 앱 반영, 미배포 |
| 달린 반응 | 댓글 수와 원문 링크는 실제 수집값만 표시. 댓글 본문 읽기와 답글은 준비 중이라고 명시하고 입력창과 전송 단추를 만들지 않음 | 댓글 본문과 답글 조작 0개를 컴포넌트 테스트와 네 폭 Playwright에서 확인. 401과 콘솔 오류 0건 | 댓글 백엔드 미구현, 화면은 정직한 준비 상태 |
| 반응형 | 성과 원장 표를 1024 미만에서 카드형 행으로 바꾸고 플랫폼 필터는 모바일 탐색 안내와 함께 제한된 가로 스크롤 사용 | 성과실 본문 `scrollWidth=clientWidth` 4/4. 전체 문서 가로 넘침 0 4/4 | 로컬 앱 반영, 미배포 |

**기존 구현 확인:** `PerformanceRoom.tsx`에는 성과 집계, 플랫폼 필터, 제안 조회와 큐 인계,
상위 글, 반응 수, 원문 링크, 원장 표가 이미 있었다. 이를 재작성하지 않고 v63의 방 상단,
판정 순서, 첫 글 반복 제작 행동, 댓글 준비 상태, 모바일 원장 표현을 추가했다. 기존
`/api/suggestions`, `/api/suggestions/enqueue`, `/api/metrics` 연결은 유지했다.

**프로토타입 대조:** v63의 판정, 무엇이 통했나, 성과 제안, 달린 반응, 원장 순서를 그대로
유지했다. 댓글 본문과 답글 조작은 백엔드 계약이 없어 구현하지 않았고, 회장 확정 요구대로
준비 상태를 표시했다. 이는 프로토타입보다 기능을 덜 보이게 만든 것이 아니라 존재하지 않는
기능을 있는 것처럼 보이지 않기 위한 확정 차이다.

**검증:** `npm run test` 150파일, 1,213건 통과, 6건 조건부 스킵, 실패 0. `npx tsc
--noEmit` 성공. 고정 커밋 `3b74b799` 격리 worktree의 `npm run build -- --webpack`
성공, 정적 페이지 171개. `design-lint.sh dashboard/src` 토큰 위반 0. Playwright 실서버
캡처는 네 폭 모두 본문 넘침 0, 섹션 순서 정상, 인증 401 0, 콘솔 오류 0을 기록했다.

**레드팀:** 까다로운 고객은 댓글 수가 보이면 답글도 된다고 오해할 수 있다. 답글 입력과
전송 단추를 렌더하지 않고, 댓글 본문 읽기와 답글 보내기가 함께 준비 중임을 한 문장으로
표시했다. Playwright가 두 조작이 다시 생기면 실패하도록 고정했다.

**셀프심문:** 이 구현이 틀렸다면 가장 그럴듯한 이유는 빈 실데이터 캡처만 보고 상위 글과
원장 반응형을 놓치는 것이다. 이를 줄이기 위해 표본 5건, 상위 글, 원문 링크 유무, 제안 성공과
실패를 컴포넌트 계약으로 따로 검증하고 실서버에서는 네 폭 레이아웃과 준비 상태를 관찰했다.

STAMP | line: osmu-fe5 | 생성: 2026-08-28 04:37 KST | model: gpt-5.6 | agent: code-builder | skill: qa | 고민: 실제 댓글 계약이 없는 상태에서도 성과 학습 흐름을 끊지 않고 정직한 다음 행동을 남겼다.

SKILLS_USED: `qa`, 실제 앱 네 폭 렌더, 반응형 관측, 인증과 콘솔 오류 검증. SKILLS_SKIPPED: 이미지 생성은 승인 HTML을 코드로 이식하는 과제라 대상 아님.

SOURCES: `docs/prototype/openclaw-auto-4room-v63.html` | `docs/requests/회장-확정-요구사항-대장.md` R185 | `docs/audit/osmu-v62-api-gap-audit-v1-gpt-codex.md` | https://support.google.com/youtube/answer/9002587 | https://support.buffer.com/en-us/articles/using-insights-in-buffer-x4gLauQU5a

MODEL: gpt-5.6 / code-builder

## 2026-08-28 v63 백엔드 4차 갭 계약 재검증

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| 성과 0건 방향 | 기존 브랜드 가이드와 `viral_signals`를 사용해 가설 3개, 근거, 표본 0/5 판정, 미검증 라벨을 반환 | 실제 DB에 브랜드 맥락과 시장 신호를 넣고 `POST /api/suggestions` HTTP 200, 가설 3개, `verified:false`, `sampleThresholdMet:false` 관찰 | 로컬 반영, 미배포 |
| 장면 순서 변경 | 기존 `drafts.payload.editor_handoff` 안의 장면 ID 전체 집합과 revision을 검사하는 전용 operation 유지 | `reorder_scenes` HTTP 200, 순서 `scene-b`, `scene-a`, revision 1을 실제 DB와 대조 | 로컬 반영, 미배포 |
| 문장 삭제·복원 | 삭제는 문장 ID를 유지한 채 `visible:false` 및 `deleted_at`을 저장하고 복원은 같은 ID를 재활성화. revision 경합 차단 | 삭제 200, 복원 200, 낡은 revision 409. DB history의 `reorder_scenes`, `delete_line`, `restore_line` 3건 일치 | 로컬 반영, 미배포 |
| 제안 출처 보존 | 기존 queue에 `performance_suggestion` 출처를 보존. 누락된 customer proxy allowlist를 복구하고 폐기 token 401 회귀 테스트 추가 | 수정 전 고객 token 403을 재현. 수정 후 HTTP 201과 DB `sourceContext` 일치, queue 정리 200 | 로컬 반영, 미배포 |

**기존 구현 확인:** 가설 3개와 제안 큐 인계는 `d892a5f7`, 편집 장면·문장
계약은 `854d6c6a`에 이미 존재했다. 이 회차에서 재창조하지 않고 실제 고객 경로의
proxy 누락 1건만 수정했다. 기존 queue file lock, DB mirror, draft JSONB, revision 충돌
차단, Studio 인계, 성과실 UI 호출은 유지했다. 신규 table과 column은 없다.

**검증:** `npm run test` 148 files, 1,204 passed, 6 skipped, 0 failed. `npx tsc --noEmit`
성공. 격리 worktree의 `npm run build -- --webpack` 성공, 정적 페이지 171개. `design-lint.sh
dashboard/src` 위반 0. 실제 HTTP 8경로와 DB 대조 전문은
`/tmp/osmu-build4-live.t2f5Hf/`에 보존했다. 임시 데이터 DB 잔여는 0이다.

**레드팀:** 목 route의 단위·mock 통합 테스트만 보면 계약이 끝난 것처럼 보이지만,
실제 proxy는 고객 인증을 403으로 거절했다. 같은 토큰으로 proxy, route handler, RLS,
file queue, DB mirror를 모두 통과시켜 이 허상을 제거했다.

**셀프심문:** 이 판정이 틀렸다면 가장 그럴듯한 이유는 개발 토큰이 운영 인증 경계를
대리하지 못한다는 것이다. 이를 줄이기 위해 production 분기에서 유효 `osmu_` 토큰을
해석하는 proxy 테스트와 실제 DB 토큰 호출을 둘 다 통과시켰다. production 배포 후
재관찰은 QA 소유로 남긴다.

🏷 STAMP | line: osmu-build4 | 생성: 2026-08-28 02:42 KST | model: gpt-5.6 | agent: code-builder | skill: 없음 | 고민: 이미 있는 네 계약을 재창조하지 않고 실제 고객 경계의 403을 종료증거로 찾았다.

SKILLS_USED: 없음. SKILLS_SKIPPED: 매칭되는 설치 코드 구현 스킬 없음. `dev.md`, `benchmarks.md` 품질 계약 적용.

SOURCES: `docs/audit/osmu-v62-api-gap-audit-v1-gpt-codex.md` | `docs/prototype/openclaw-auto-4room-v63.html` | `docs/requests/회장-확정-요구사항-대장.md` | `dashboard/db/schema.sql` | https://nextjs.org/docs/app/api-reference/file-conventions/route | https://www.postgresql.org/docs/current/functions-json.html

MODEL: gpt-5.6 / code-builder

## 2026-08-28 v63 실제 화면 3차

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| 네 방 사이드바 | 생성실, 편집실, 발행실, 성과실의 순번, 연결선, 현재 위치를 사이드바 첫 영역에 배치하고 채널 목록은 그 아래 유지. 1024 폭에서도 네 방 이름을 표시 | `docs/prototype/qa-fe3/sidebar-4room-1440.png`, `sidebar-4room-1024.png` 원본 확인. 브라우저 링크 4개와 좁은 폭 이름 4개 가시성 통과 | 로컬 앱 반영, 미배포 |
| 생성실과 편집실 | 생성실과 편집실에 `data-room-top`을 추가하고 오른쪽을 대화창으로 구성. Studio API 후보 선택과 `StudioCommandPanel` 명령 handler를 그대로 사용 | 실제 브라우저 `POST /api/studio/v1/generations` 201, A, B, C 선택 3개. 생성과 편집 계약 테스트 10건 통과 | 로컬 앱 반영, 미배포 |
| 발행실 | 대화형 오른쪽 패널, 7개 미리보기 동시 노출, 각 칸 머리의 발행 체크와 계정 선택, 미리보기 내부 표시 이름, 제목, 캡션, 해시태그, 첫 댓글 편집. 발행 이력과 옛 기능 단추 목록 제거 | `docs/prototype/qa-fe3/publish-room-1440.png` 원본 확인. 미리보기 7개, 지원 채널 체크 4개, 계정 선택 4개, 중지 0개, 콘솔 오류 0 | 로컬 앱 반영, 실제 SNS 게시 미실행 |
| 실행 계약 | `초안으로 저장`, `검토 요청`, `Publish (3)`, `날짜 잡기` 문구와 기존 API를 연결. 검토 요청은 queue 생성 뒤 기존 request-review를 호출하며 실패 시 후속 호출을 막음 | 검토 정상 1건과 거절 1건, 발행 성공·부분 실패·전체 실패·첫 댓글 계약 포함 발행실 14건 통과 | 로컬 앱 반영, 미배포 |
| 기존 기능 보존 | Studio API 후보 3장, 선택 뒤 편집실 인계, 챗봇 명령 라우팅, 미리보기 입력, 발행 진행률, permalink, partial 복구 가드, 내레이션 폴백 배너를 유지 | 전체 Vitest 148파일, 1,200건 통과, 6건 skipped. Studio API 201과 후보 3장 실제 관찰 | 로컬 앱 반영, 미배포 |

**검증:** `npm run test` 148파일, 1,200 passed, 6 skipped, 0 failed. `npx tsc --noEmit` 성공.
`npm run build` 성공, 정적 페이지 171개 생성. NFT 추적 범위 경고 1건은 기존 설정 경고이며 빌드는
성공했다. `design-lint.sh dashboard/src` 토큰 위반 0. 재기동한 `http://localhost:3456`에서
최종 브라우저 스모크를 다시 실행해 Studio 201, 후보 3장, 인증 401 0, 콘솔 오류 0을 관찰했다.

**레드팀:** 회의적인 QA 입장에서 DOM에 7개가 있다는 수치만으로는 R164를 통과시킬 수 없다고
판정했다. 실제 전체 페이지 캡처에서 세 번째 카드가 가로로 밀리는 것을 발견해 3열 그리드로
수선한 뒤 7개를 다시 원본으로 확인했다.

**셀프심문:** 이 구현이 틀렸다면 가장 그럴듯한 이유는 v63 구조 교체가 2차 API 연결을 끊는 것이다.
전체 회귀 테스트와 실제 Studio 생성 201을 함께 통과시키고, 내레이션 배너 누락 1건도 새 화면에
복원해 이 가정을 반증했다.

🏷 STAMP | line: osmu-fe3 | 생성: 2026-08-28 01:56 KST | model: gpt-5.6 | agent: code-builder | skill: 없음 | 고민: 기능 연결을 유지하면서 R164의 7칸과 R177의 대화형 선택 구조를 실제 화면에 동시에 맞췄다.

SKILLS_USED: 없음. SKILLS_SKIPPED: 매칭되는 설치 코드 구현 스킬 없음. `dev.md`, `benchmarks.md` 품질 계약 적용.

SOURCES: `docs/prototype/openclaw-auto-4room-v63.html` | `docs/prototype/openclaw-auto-4room-v64.html` | `DESIGN.md` | `dashboard/src/components/home/PerformanceRoom.tsx` | https://www.intercom.com/help/en/articles/6612589-set-up-and-customize-the-messenger | https://linear.app/docs/default-team-pages

MODEL: gpt-5.6 / code-builder

## 2026-08-28 v63 생성실·편집실·발행실 화면 2차

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| 발행실 | 텍스트, 영상, 카드뉴스 갈래별 세로 묶음과 묶음 안 가로 미리보기 7칸을 구현. 제목, 표시 이름, 캡션, 해시태그, 첫 댓글을 미리보기 카드 안에서 편집. 발행 선택과 Save, Publish, 예약은 오른쪽 발행 담당 대화창으로 이동 | `publish-room-1440.png`. 브라우저에서 7개 미리보기, 첫 댓글 지원 입력 4개, 중지 버튼 0개, 콘솔 오류 0 관찰 | 로컬 앱 반영, 미배포 |
| 생성실 | 첫 손님 학습 정보 미수집 항목을 표시하고 대화창에서 주제, 목적, 대상, 소재 권리를 받음. 독립 Studio bearer와 작업 공간을 사용해 학습 정보 7층 생성 API를 실제 호출하고 A, B, C 후보 3장을 표시 | `create-room-candidates-1440.png`. `POST /api/studio/v1/generations` HTTP 201과 후보 3장 직접 관찰 | 로컬 앱 반영, production identity adapter 미연결 |
| 편집실 | 왼쪽 목차, 현재 장면, 비율·배경·목소리·속도·자막 조작, 대사 줄 단위 편집을 구현. Studio 담당 패널은 편집 인계, 준비 완료, OpenClaw 큐 명령을 실제 API로 실행 | `edit-room-openclaw-handoff-1440.png`. 대시보드 tenant와 Studio workspace를 분리한 브라우저에서 생성 201, 편집 인계 201, 준비 200, 큐 인계 201, 콘솔 오류 0 관찰 | 로컬 앱 반영, 미배포 |
| 발행 capability | `/api/publish/first-comment-capabilities` 응답을 화면 진실원으로 사용. Threads, X, Instagram, Facebook만 첫 댓글 입력을 표시하고 Shorts, Reels, TikTok은 응답 사유를 표시. 발행 요청에도 지원 채널 첫 댓글만 전달 | 정상 전달과 미지원 거절을 포함한 집중 32건 통과. 브라우저 첫 댓글 입력 4개 관찰 | 로컬 앱 반영, 실제 SNS 게시 미실행 |
| 챗봇 명령 라우팅 | `POST /api/studio/commands`가 편집 인계, 장면 순서, 문장 삭제·복원, 준비 완료, OpenClaw 큐 명령을 기존 handler로 라우팅하고 실제 target을 응답 | `handoff_to_editor` 201, `reorder_scenes`·`delete_line`·`restore_line`·`mark_ready` 200, `enqueue_openclaw` 201 전문을 `/tmp/osmu-build3-live.nLKDsI`에 보존 | 로컬 앱 반영, 미배포 |
| 후보 단일 계약 | 기존 Studio v1 생성 응답의 한 job 안에 A, B, C 후보 3장을 유지하고 화면 client가 `job_id`를 각 후보의 `generation_id`로 결합. 별도 후보 API를 재창조하지 않음 | `POST /api/studio/v1/generations` 201 전문에서 A, B, C와 각 candidate ID 관찰. 생성실 브라우저에서도 후보 3장 관찰 | 로컬 앱 반영, 미배포 |
| 편집·큐 영속 계약 | text, image, video, card, audio handoff를 기존 draft JSONB에 저장. 장면과 문장 변경은 revision 경합 방지. ready 결과는 기존 queue에 Studio 출처와 멱등키로 인계 | revision 충돌 409 통합 테스트, 큐 최초 201과 같은 revision 재호출 200 `reused:true` 실앱 응답 관찰 | 신규 table·column 없음, 미배포 |

### 기존 화면 보존과 이동

| 기존 구현 | 이번 처리 |
|---|---|
| 글감 입력, 기존 OSMU 생성, 이미지·영상 생성 | 로직을 보존하고 발행 담당 대화창으로 이동 |
| 드래프트 Save, 발행 이력 불러오기, 예약, 계정 선택 | 상태와 API 호출을 보존하고 발행 담당 대화창에 배치 |
| 플랫폼별 발행 진행률, permalink, partial 복구 가드 | 기존 로직 보존. 발행 중지 단추만 백엔드 미지원 계약에 맞춰 제거 |
| `PlatformPreview`의 로고, 캐러셀, 영상 레일 | 좌표와 구조를 재작성하지 않고 그대로 재사용. 카드 내부 편집 필드만 확장 |
| 별도 편집 드로어 | 발행 미리보기 카드 제자리 편집으로 이동 |
| `PerformanceRoom.tsx` 성과실 | 변경 없음 |

**검증:** 전체 `npm run test` 148 files, 1,193 passed, 6 skipped, 0 failed. `npx tsc --noEmit` 성공. `npm run build` 성공, 정적 페이지 171개 생성. `design-lint.sh dashboard/src` 토큰 위반 0. Chrome 1440 폭에서 생성실, 편집실, 발행실을 직접 렌더하고 버튼과 입력을 조작했다. Studio 생성 201, 편집 인계 201, 준비 200, OpenClaw 큐 인계 201, 첫 댓글 입력 4개, 발행 중지 버튼 0개, 콘솔 오류 0을 관찰했다.

**새 테이블 회수 없음:** 편집 인계와 장면·문장 변경은 기존 `drafts.payload.editor_handoff`에 저장하고, OpenClaw 인계는 기존 queue payload의 `sourceContext`를 사용한다. 대시보드 tenant와 Studio workspace는 개발 런타임의 별도 session key로 분리해 인증 경계를 섞지 않았다.

🏷 STAMP | line: osmu-build3-fe2 | 생성: 2026-08-28 01:16 KST | model: gpt-5.6 | agent: code-builder | skill: qa | 고민: 두 인증 경계의 token과 workspace ID를 모두 분리하면서 후보 출처가 OpenClaw queue까지 보존되게 했다.

SKILLS_USED: `qa`, 로컬 Chrome 렌더·클릭·API 응답·콘솔 오류 검증. SKILLS_SKIPPED: 이미지 생성 스킬은 기존 프로토타입과 코드 UI 구현 과제라 대상 아님.

SOURCES: `docs/prototype/openclaw-auto-4room-v63.html` | `DESIGN.md` | `docs/requests/회장-확정-요구사항-대장.md` R157, R158, R164, R173, R177, R191, R197, R204, R205 | `dashboard/src/app/studio/page.tsx` 기존 구현 | https://support.buffer.com/en-us/articles/what-is-buffers-api-GtIYIQilz5 | https://help.descript.com/hc/en-us/articles/10165362727821

MODEL: gpt-5.6 / code-builder

## 2026-08-28 Studio v1 인증 헤더 충돌 해소

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| 독립 인증 경계 | 현재 존재하는 `/api/studio/v1/generations`, 생성 조회, 재생성 경로만 대시보드 Proxy 인증에서 분리하고 Studio Route Handler가 자체 bearer를 검증. 기존 `/api/studio/text`, `/api/studio/drafts`와 tenant allowlist는 변경 없음 | `STUDIO-AUTH-01~04` 포함 집중 70건 통과. 잘못된 Studio bearer는 Studio 오류 `TOKEN_INVALID` 401, 미등록 v1 경로는 대시보드 401 | 로컬 앱 반영, 미배포 |
| Studio 실제 앱 호출 | 생성, 학습정보 거절, 조회, 무료 및 추가 재생성을 `localhost:3456` 한 프로세스에 호출 | 201 후보 3장, 422 `learning_context.u3.purpose`, 200, 201, 409 전문을 `/private/tmp/studio-api-live-final.GqNVsz/`에 보존 | 로컬 앱 반영, 미배포 |
| 대시보드 회귀 | 같은 프로세스에서 운영자 인증과 tenant 선택 계약을 유지 | `GET /api/queue` 200, `POST /api/suggestions` 200과 아이디어 3건 관찰 | 기존 동작 유지, 미배포 |

**검증:** 전체 Vitest 144 files, 1,170 passed, 6 skipped, 0 failed. 기존 1,166건에 인증 경계 4건이 추가됐다. 집중 70건 통과. `npx tsc --noEmit` exit 0. 커밋 `1eb0e848` 전용 worktree에서 `npm run build -- --webpack` 성공, 정적 페이지 169/169, Studio v1 route 3개 manifest 포함. 공유 트리는 동시 Studio 작업 중이라 기본 Turbopack build 시점에 타 작업의 일시적 타입 오류 2건을 관찰했으며 이 인증 커밋 빌드와 분리했다. `design-lint.sh dashboard/src` 위반 0. 최종 `http://localhost:3456/api/health` 200과 listener 생존을 확인했다.

**고른 방법:** 버전이 분리된 Studio v1 네임스페이스만 대시보드 Proxy에서 Route Handler로 통과시켰다. Studio가 독립 회원·권한 장부를 가진다는 제품 경계를 지키면서 기존 대시보드 보호 목록과 tenant 스코프를 약화시키지 않기 위해서다.

🏷 STAMP | line: studio | 생성: 2026-08-28 00:24 KST | model: gpt-5.6 | agent: code-builder

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. `dev.md`, `benchmarks.md` 적용.

SOURCES: `pipeline-state.studio.md` approved_artifacts 4종 | `dashboard/src/proxy.ts` | `dashboard/src/lib/studio/generation/identity.ts` | https://nextjs.org/docs/app/getting-started/proxy | https://nextjs.org/docs/app/guides/authentication

MODEL: gpt-5.6 / code-builder

## 2026-08-27 Studio 생성 API build

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| 생성 입력 | 학습 정보 7층의 S0, S1, U2, U3, X4, R6을 필수로 검사하고 L5는 빈 초기값을 허용. 소재 권리와 요청 시점 `platform_spec`를 받되 provider 인증값은 거절 | 정상 요청 HTTP 201, U3 누락 HTTP 422 응답 전문 관찰 | 로컬 build, 미배포 |
| 후보 3장 | A 문제 우선, B 근거 우선, C 과정 우선의 구조화된 스토리보드를 형식, 근거, 비용 견적 상태, 채널 준비본과 함께 반환 | HTTP 201 응답에서 서로 다른 A/B/C와 추적 ID 관찰 | 로컬 build, 실제 미디어 provider 생성은 미구현 |
| R27 재생성 | U2 timezone의 회원 기준 하루 1회 무료. 경합 요청은 한 건만 소비하고 두 번째부터 과금 승인 요구 | 첫 재생성 HTTP 201, 두 번째 HTTP 409와 reset 시각 관찰. 경합 통합 테스트 통과 | 메모리 장부만 구현, 미배포 |
| 회원 및 작업 공간 | production adapter는 안전하게 거절하고 명시적 development identity adapter로만 실행. 로그인과 결제 provider 자체는 domain에서 분리 | Bearer 누락 401, 잘못된 workspace 403, 권한 있는 workspace 정상 경로 통과 | production 장부는 신규 DB 합의 전까지 미구현 |
| 단독 손님 내려받기 | 범위 외로 보류 | 만료 링크, 원본, 채널 준비본 영속성은 신규 DB 장부와 object storage signing adapter가 필요 | 미구현, 미배포 |

**검증:** Studio 단위·통합 15건 통과. 전체 Vitest 144 files, 1,166 passed, 6 skipped, 0 failed. TypeScript 통과. webpack production build 성공, Studio 신규 route 3개가 manifest에 포함. `design-lint.sh dashboard/src` 토큰 위반 0. 실제 로컬 HTTP는 생성 201, 필수 입력 거절 422, 무료 재생성 201, 추가 재생성 409, 생성 결과 조회 200을 관찰했다.

**⏹ 회수 필요:** R104 회원·작업 공간 권한, 생성 job·후보, idempotency, R27 일일 사용량을 production에서 유지하려면 신규 DB table 설계 합의가 필요하다. 현재 메모리 runtime은 단일 process 로컬 수직 검증용이며 production 준비 상태가 아니다.

**⏹ 회수 필요:** R105 만료 다운로드를 구현하려면 output ledger, object key, 만료 시각을 저장할 table과 service-neutral signing adapter 계약 합의가 필요하다.

🏷 STAMP | line: studio | 생성: 2026-08-27 23:10 KST | model: gpt-5.6 | agent: code-builder

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. `dev.md`, `benchmarks.md` 적용.

SOURCES: `docs/prototype/openclaw-auto-4room-v63.html` | 회장 확정 요구사항 R27, R37, R71, R104, R105, R132, R133 | 학습정보 7층 계약 | [IETF Idempotency-Key draft](https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-idempotency-key-header), 재시도의 중복 실행을 막는 요청 key 계약 참고 | [AWS S3 presigned URL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/using-presigned-url.html), 기간 제한 접근과 bearer 취급 경계 참고

MODEL: gpt-5.6 / code-builder

## 2026-08-27 v63 백엔드 build 2차

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| 첫 댓글 발행 | `POST /api/publish`의 `first_comment` 계약을 기존 발행 경로에 연결. Threads, X, Instagram, Facebook은 지원하고 Shorts, Reels, TikTok은 발행 전에 명시적으로 거절. 결과는 기존 `published_posts.provider_meta`에 기록 | capability 조회와 TikTok 거절 요청을 실제 HTTP 200, 400으로 관찰. X 본문 발행 후 답글 계약 통합 테스트 통과. 실제 SNS 계정 발행은 미검증 | 미배포 |
| 발행실 복귀 컨텍스트 | `GET /api/queue?returnTo=inbox|calendar`를 정식 입력으로 받고 레거시 `source`도 보존. 모든 post에 `publishContext`와 Studio `returnUrl`을 반환 | inbox 요청 HTTP 200 응답에서 queue ID, sourceRoute, returnUrl 관찰 | 백엔드 계약 구현, 브라우저 클릭은 미검증 |
| 검토 요청 | `POST /api/queue/[postId]/request-review` 신설. 기존 draft를 한 번에 승인 인박스 요청 상태로 만들고 중복 요청은 같은 상태를 재사용 | 로컬 queue post 대상 HTTP 200, `reviewRequest.status=requested`, `inboxUrl=/inbox` 관찰. 승인 완료 또는 draft가 아닌 입력의 409 거절 테스트 통과 | 미배포 |
| 통합 발행 상태 | `GET /api/publish?draft_id=...`가 Threads, X, Instagram, Facebook, Shorts, Reels, TikTok의 상태를 한 응답으로 집계. 기존 `published_posts`만 읽고 stop 미지원 이유를 계약에 표시 | 로컬 draft 대상 HTTP 200, 전체 `not_started`, 7개 target과 합계 관찰. published, failed, processing 혼합 상태 통합 테스트 통과 | 조회만 구현, stop 미구현, 미배포 |
| 제안 표본 판정 | 제안 응답에 `sampleAssessment {count, threshold, thresholdMet}`를 추가하고 5건 미만은 검증된 제안으로 표시하지 않음 | 성과 표본 0건 tenant 요청 HTTP 200, 제안 3개 모두 `0/5`, `thresholdMet:false`, `verified:false` 관찰 | 미배포 |

**검증:** `npm run test` 142 files, 1,146 passed, 6 skipped, 0 failed. `npx tsc --noEmit` 성공. `npm run build` 성공, 168개 정적 페이지 생성. `design-lint.sh dashboard/src` 토큰 위반 0. 실제 curl 6개 계약을 관찰했고 검증용 queue row는 삭제해 파일과 DB 잔여 0을 확인했다. 신규 DB table과 column은 없다.

**남은 검증:** 지원 플랫폼의 실제 첫 댓글 발행은 외부 SNS에 콘텐츠를 만드는 작업이라 실행하지 않았다. 기존 Threads 연결 계정은 새 `threads_manage_replies` scope를 받기 위해 재연결이 필요할 수 있다. QA에서 inbox와 calendar 클릭 복귀, 연결된 테스트 계정의 첫 댓글 1회, 혼합 발행 상태 화면을 확인해야 한다.

🏷 STAMP | line: osmu | 생성: 2026-08-27 22:10 KST | model: gpt-5.6 | agent: code-builder

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. `dev.md`, `benchmarks.md` 적용.

SOURCES: API 갭 감사 | R56, R68, R98, R185 | Next.js Route Handlers | X Create Post | Threads Posts and Replies | Instagram Comment Moderation

MODEL: gpt-5.6 / code-builder

## 2026-08-27 v63 백엔드 build

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| 성과 0건 제안 | `POST /api/suggestions`가 브랜드 맥락과 시장 신호로 가설 3개를 반환. 모두 `가설 · 우리 검증 기록 아님`, `verified:false`, 근거 ID와 표본수를 포함 | 로컬 Postgres 성과 표본 0건 tenant에 실제 요청, HTTP 200과 3개 반환 관찰 | 미배포 |
| 제안 큐 인계 | `POST /api/suggestions/enqueue` 신설. 기존 `queue.json` 잠금과 `queue_posts` mirror를 재사용하고 suggestion ID, 근거, 면책 라벨을 보존 | 최초 HTTP 201, 동일 제안 재요청 HTTP 200 `reused:true`, DB에 동일 suggestion ID 1건 관찰 | 미배포 |
| inbox·calendar 복귀 | `GET /api/queue?source=inbox|calendar`가 발행실 `returnUrl`, queue ID, draft ID를 포함한 `publishContext`를 반환 | 두 source 모두 HTTP 200. 미지 source는 HTTP 400 관찰 | 백엔드 계약만 구현, 프론트 클릭 연결은 미검증 |
| 댓글과 첫 댓글 | 이번 build 범위에서 미구현 | 댓글 본문, 나중 처리, 좋아요, 수정 이력을 남기려면 신규 `engagement_items` 스키마 합의가 필요. provider read-through와 첫 댓글은 스키마 없이 가능하지만 이번 범위에서 미검증 | 미구현, 미배포 |

**검증:** `npm run test` 139 files, 1,133 passed, 6 skipped, 0 failed. `npm run build` 성공. `design-lint.sh dashboard/src` 토큰 위반 0. 신규 DB table과 column은 없다.

🏷 STAMP | line: osmu | 생성: 2026-08-27 18:30 KST | model: gpt-5.6 | agent: code-builder

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. `dev.md`, `benchmarks.md` 적용.

SOURCES: `docs/prototype/openclaw-auto-4room-v63.html` | R56·R68·R98·R185 | Next.js Route Handlers | YouTube Comments API | Meta Threads API

MODEL: gpt-5.6 / code-builder

## 2026-08-27 v62 네 방 백엔드 대조

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| API 전수 감사 | 상위 family 98개와 하위 route 파일 163개를 v62 네 방의 핵심 동작 46개와 대조. 있음 14개, 부족 21개, 없음 11개 | `docs/audit/osmu-v62-api-gap-audit-v1-gpt-codex.md` | 문서만 갱신, 소스 미수정 |
| 댓글과 답글 | replies 숫자 집계만 있음. 댓글 본문 읽기, 답글, 나중 처리, 좋아요는 미구현 | `dashboard/src/app/api/metrics/route.ts`, API 전수 검색 | 미구현, 미배포 |
| 성과 제안 인계 | suggestion과 queue API는 각각 있으나 제안에서 생성 큐로 가는 연결은 미구현 | `dashboard/src/app/api/suggestions/route.ts`, `dashboard/src/app/api/queue/add/route.ts` | 미구현, 미배포 |
| 성과 0건 제안 | 현행 API는 빈 ideas와 재시도 안내를 반환해 R68을 충족하지 못함 | `dashboard/src/app/api/suggestions/route.ts` | 미구현, 미배포 |
| DB 선택 | 댓글, 제안 lifecycle, 학습 후보의 무스키마안과 신규 테이블안을 분리해 제시 | `docs/audit/osmu-v62-db-options-v1-gpt-codex.md` | 미확정 |

**게이트:** `pipeline-state.osmu.md`에서 design 미승인, build `artifacts_ok: false`, override 만료를 확인했다.
이번 세션은 소스 코드와 DB 스키마를 수정하지 않았다. build와 실제 요청 증거는 미검증이다.

**다음 상태:** v62 design과 eng-design을 승인한 뒤 기존 queue를 재사용하는 제안 인계부터 구현한다.
댓글은 승인된 선택지에 따라 provider read-through 또는 `engagement_items` migration으로 진행한다.

🏷 STAMP | line: osmu | 생성: 2026-08-27 20:40 KST | model: gpt-5.6 | agent: code-builder

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. `dev.md`, `benchmarks.md`, `doc-review.md` 적용.

SOURCES: v62 prototype | API route 163개 | 요구사항 대장 | DB schema | 공식 YouTube, Threads, Instagram API 문서

MODEL: gpt-5.6 / code-builder

## 현재 구현 상태

| 영역 | 구현 상태 | 직접 근거 | 배포 상태 |
|---|---|---|---|
| v23 디자인 토큰 | 간격 6단계(4·8·12·16·24·32), 글자 7단계(12·13·15·17·20·24·30), 44px 터치 하한이 `globals.css`에 구현됨 | webpack production build 166/166, TypeScript exit 0 | 미배포 |
| 공통 UI 부품 | `Button`, `Stack`, `Section`, `Field` 신설. 기존 `Card`, `StatusBadge`, `EmptyState` 정합화 | `DesignSystem.test.tsx` 7건 통과 | 미배포 |
| 12px 글자 하한 | dashboard 전체의 9·10·11px 임의 클래스 451건을 `text-caption`으로 전환. 명시적 no-touch 영역 4곳은 소스 보존하고 CSS 안전망으로 실제 12px 렌더 강제 | `design-system-floor.contract.test.ts` 2건 통과 | 미배포 |
| Marketing Home | 플랫폼 집중, 성과 아이디어, 성과 수집 버튼을 공통 Button/Stack/Section/Card로 교체. 기존 API·상태 핸들러 유지 | `HomeDesignSystemIntegration.test.tsx` 1건 통과 | 미배포 |
| OSMU Studio | 생성·저장·발행·예약·중지·편집/재생성 버튼과 편집 필드를 공통 부품으로 교체. 두 PlatformPreview 호출과 발행 이력 패널은 원본 diff 0 | `studio-publish-ui.test.tsx` 4건 통과 | 미배포 |
| Channel Page | 연결 CTA, 탭, 수동 인증 토글, 인기글 추가/삭제, 설정 저장을 공통 Button/Stack/Section으로 교체 | `ChannelPageAutomation.test.tsx` 9건 통과 | 미배포 |
| R-02 F1 드래프트 본문 | `payload.text` 우선, 레거시 최상위 플랫폼 키 폴백, 진짜 빈 본문 경고, 불러오기 버튼 보존 | `studio-drafts-r02.test.ts` 3건과 Studio 발행 UI 4건 통과 | 미배포 |
| R-02 F2 채널 연결상태 | `channel_accounts`를 단일 판정 소스로 사용. active, expired, revoked, 없음 경계를 공용 헬퍼와 `/api/channel-config`에 연결 | 연결 헬퍼 5건, OAuth 상태 12건 통과 | 미배포 |
| R-02 F3 홈 저장소·패널 | overview, activity, weekly 계열을 DB 우선으로 전환하고 홈 중복 패널을 성과 요약 1블록, 발행물 리스트, 최근 활동으로 통합 | 홈 집계 3건, 디자인 계약 3건, 전체 Vitest 통과 | 미배포 |
| R-02 F4 Admin | active 채널만 집계. OAuth provider 카드는 기본 접힘, 상태 뱃지, `aria-expanded`·`aria-controls`를 가진 accordion으로 전환 | Admin API 22건, credential UI 3건 통과 | 미배포 |
| R-02 부수 수정 | ChannelPage에 원시 문자열로 보이던 유니코드 escape를 실제 한국어 안내문으로 교체 | TypeScript와 전체 Vitest 통과 | 미배포 |

## 검증 상태

- 테스트됨: 최신 기록 기준 전체 `npm test` 138 files, 1,121 passed, 11 skipped, 실패 0(2026-08-14 OAuth 후속 구현 검증).
- 테스트됨: `npx tsc --noEmit` exit 0.
- 테스트됨: `npm run build -- --webpack`, 166/166 정적 페이지 생성 및 route manifest 출력.
- 테스트됨: 2026-08-12 R-02와 디자인 토큰 보정 기준 전체 Vitest 131 files, 1061 passed, 10 skipped. R-02 관련 8 files, 62 tests 통과. `npx tsc --noEmit` exit 0. webpack production build 166/166.
- 관찰됨: 이전 code-builder 브라우저 세션에서 Studio 드래프트 불러오기, Settings 연결상태, Admin 접기, 홈 통합 화면이 렌더됐고 콘솔 오류 0으로 기록됐다. 다만 이번 후속 F2·F3 보정 뒤에는 현재 샌드박스가 Next dev의 `listen(0.0.0.0:3456)`을 EPERM으로 차단해 새 브라우저 세션을 열지 못했다.
- 미검증: 후속 보정분의 실제 브라우저 1440·1024·390 화면과 홈 before/after 정규 폭 캡처. 기존 `home-1024-*.png`의 실제 폭이 757px라 1024 증거로 승계하지 않는다.
- 테스트됨: `design-lint.sh dashboard/src` 결과 디자인 토큰 위반 0. 기존 임의 3px, JSX 인라인 style, 토큰 밖 hex를 시맨틱 토큰과 공용 class로 교정했다.
- 구현됨·미배포: R-05 채널 연결 준비상태 UX와 R-09 채널 capability 단일 소스가 후속 반영됐다. 상세와 별도 검증 기록은 아래 2026-08-13 항목에 보존한다.
- 미배포: stage/prod 반영과 실사용자 경로는 수행하지 않았다.

## 다음 작업

포트 바인딩이 허용된 QA 환경에서 `OSMU_AUTH_OPTIONAL=1 PORT=3456 npm run dev`를 제한시간 안에 기동한다. 정식 tenant token으로 Home, Studio, Settings, `/channels/threads`, `/operator/customers`를 1440·1024·390에서 확인하고 콘솔 오류 0, 홈 before/after 실제 폭, accordion 및 불러오기 클릭을 증명해야 QA gate를 닫을 수 있다.

---

🏷 STAMP | line: osmu | 생성: 2026-08-10 18:10 KST | model: gpt-5.6-codex | agent: code-builder
skills: SKILLS_SKIPPED(매칭되는 설치 스킬 없음) | 근거: DESIGN.md v23 · pipeline-state.osmu.md override · W3C WCAG 2.5.5 · Tailwind CSS theme variables
고민: no-touch 영역의 10·11px 소스를 바꾸지 않으면서 전역 12px 하한을 지키기 위해 CSS 안전망과 회귀 계약 테스트를 함께 넣었다.

SOURCES: DESIGN.md §v23 · dashboard/src 실제 코드 · https://www.w3.org/WAI/WCAG22/Understanding/target-size-enhanced · https://tailwindcss.com/docs/padding
MODEL: gpt-5.6-codex / code-builder

## R-02 build STAMP

🏷 STAMP | line: osmu | 생성: 2026-08-12 14:16 KST | model: gpt-5.6 | agent: code-builder

기반: `docs/fdd/fdd-r02-journey-fix-v1.0.0-opus.md` · `docs/fdd/migration-filestore-to-db-v1.0.0-opus.md` · `docs/fdd/test-plan-r02-v1.0.0-opus.md` · `DESIGN.md` v24 · commits `3fd63016`, `ea0509ab`

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. 저장소 code-builder 규율과 `dev.md`를 적용.

SOURCES: PostgreSQL row security documentation · WAI-ARIA APG accordion pattern · Next.js Route Handlers documentation · 위 FDD 3종 · dashboard 실제 코드와 테스트

MODEL: gpt-5.6 / code-builder

## 2026-08-13 R-02 홈 DB 컷오버 후속

### 구현 상태

| 영역 | 변경 | 검증 | 배포 |
|---|---|---|---|
| 홈 읽기 컷오버 | `overview`, `activity`, `weekly-report`, `weekly-summary` 기본 소스를 DB로 고정. DB 장애를 파일 성공으로 숨기지 않고 503 `home_db_unavailable`로 반환 | `home-data-cutover.test.ts` 9건, `home-data-source.test.ts` 3건 통과 | 미배포 |
| 롤백과 섀도우 | `HOME_DATA_SOURCE=file`에서만 파일 롤백. `SHADOW_HOME_DB=1`은 파일 응답과 DB 값을 함께 읽어 구조화 diff 기록 | 소스 선택, 키 순서 무관 diff 단위테스트 통과 | 미배포 |
| 홈 DB 집계 완결 | 최근 활동에 `queue_posts` 초안과 `published_posts` 발행·바이럴을 함께 반영. 주간 생성 건수와 바이럴도 DB에서 계산해 DB 모드의 `queue.json` 읽기를 제거 | `home-metrics.test.ts` 3건 통과. live Postgres 통합테스트 추가 | 미배포 |
| 안전 백필 | `queue.json` 백필을 한 DB 트랜잭션과 `ON CONFLICT DO NOTHING`으로 실행. 기존 DB 행을 낡은 파일로 덮지 않고 신규·기존·무효 건수를 구분 | `queue-store-migration.test.ts` 2건 통과 | 미배포 |
| 큐 dual-write 보강 | 수정, 이미지 추가, 텍스트 변형, bulk 승인·삭제, Figma export, sourcing import의 누락된 `queue_posts` 미러 연결 | `r02-acceptance-contract.test.ts`에서 전체 파일 writer 10개 연결 확인 | 미배포 |
| R-02 테스트 | 생성 실패 배너, 이력 불러오기, 빈 본문, 발행 버튼, 성공 permalink와 published 저장을 컴포넌트 테스트로 추가 | R-02 집중 9개 파일 60건 통과. 전체 Vitest 통과 | 미배포 |
| 브라우저 E2E | `scripts/verify-r02-e2e.mjs` 추가. 로그인, 고객 토큰, DB API 정합, Home, Studio 불러오기, 선택적 실 생성·수정·발행, 콘솔 오류와 스크린샷을 한 번에 검증 | 스크립트 구문 검증만 통과. 현재 샌드박스에서 서버·DB 접근 차단으로 런타임 미검증 | 미배포 |

### 검증과 남은 갭

- 테스트됨: `npx tsc --noEmit` exit 0.
- 테스트됨: 전체 `npm test` exit 0. 총 1,095건 중 1,084건 통과, 11건 skip, 실패 0. live DB 의존 테스트는 현재 로컬 네트워크 차단으로 skip.
- 테스트됨: Turbopack은 샌드박스 포트 바인딩 제한으로 실패했으나 `next build --webpack`은 166/166 정적 페이지 생성과 route manifest 출력까지 통과.
- 테스트됨: `design-lint.sh dashboard/src` 결과 디자인 토큰 위반 0.
- 미검증: `docker start osmu-pg`는 Docker 소켓 권한 거부. DB 55432는 호스트 listener만 보이고 샌드박스 연결은 차단. Next 3459는 `listen EPERM`으로 기동 실패.
- 미검증: 따라서 운영자 Bearer로 tenant token 발급, 실제 브라우저 렌더, 생성→수정→실발행, permalink와 DB 행 증가는 이번 세션에서 직접 관찰하지 못함.
- 문서 결함: `test-plan-r02-v1.0.0-opus.md`는 “16케이스”라고 쓰지만 표의 TC를 세면 A1, B1~3, C1, D1, E1~3, F1~2, G1~3, H1, I1~2, M1로 총 18건이다. 구현은 표의 18개 식별자를 기준으로 추적했다.
- Contract 단계는 미착수: cron과 외부 extension의 파일 쓰기 전환이 확인되지 않아 `queue.json` 파일 삭제는 하지 않았다. FDD도 이를 별도 승인 단계로 규정한다.

🏷 STAMP | line: osmu | 생성: 2026-08-13 02:10 KST | model: gpt-5 Codex | agent: code-builder

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. `dev.md`, `benchmarks.md`, 저장소 code-builder 규율 적용.

SOURCES: `docs/fdd/fdd-r02-journey-fix-v1.0.0-opus.md` · `docs/fdd/migration-filestore-to-db-v1.0.0-opus.md` · `docs/fdd/test-plan-r02-v1.0.0-opus.md` · `dashboard/src` · PostgreSQL INSERT/ON CONFLICT 공식문서 https://www.postgresql.org/docs/current/sql-insert.html

MODEL: gpt-5 Codex / code-builder

## 2026-08-13 R-02 E2E 하네스 하드닝

### 구현 상태

| 영역 | 변경 | 검증 | 배포 |
|---|---|---|---|
| 기본 origin | `verify-r02-e2e.mjs` 기본 URL을 Next.js dev canonical origin과 같은 `http://localhost:3459`로 변경 | 구문 검사와 source assertion 통과 | 미배포 |
| Studio draft load | API에서 loadable draft 존재를 확인하고 화면에서 `본문 없음` 경고가 없는 이력 버튼만 1초 간격으로 재시도 | 실제 스크립트 DOM 표현식으로 두 번째 loadable draft click 관찰 | 미배포 |
| Publish 대기 | 보이는 활성 `🚀 Publish (N)` 버튼을 최대 60초 폴링하고 관찰 문구와 클릭 횟수를 결과 JSON에 기록 | JSDOM에서 `🚀 Publish (3)` 관찰 | 미배포 |
| 원에러 보존 | 코디네이터가 선반영한 catch와 `rmSync` 재시도 cleanup을 유지 | 강제 실패 실행에서 `Chrome DevTools did not start: fetch failed`가 그대로 출력되고 9329 listener 잔류 0 | 미배포 |

### 검증 상태

- 커밋: `aac18ec6` (`test(dashboard): harden R-02 browser verification`).
- 테스트됨: `node --check dashboard/scripts/verify-r02-e2e.mjs` exit 0.
- 테스트됨: script와 이번 QA 문서 대상 `git diff --check` exit 0.
- 테스트됨: 스크립트에서 실제 `Runtime.evaluate` 표현식을 추출해 JSDOM에서 실행. `본문 없음` 경고가 있는 첫 항목 대신 본문 있는 두 번째 항목을 클릭했고 다음 폴링에서 `🚀 Publish (3)`을 관찰했다.
- 테스트됨: R-02 계약·디자인·Studio drafts API·grounding·publish UI 5 files, 23 tests 통과.
- 테스트됨: `design-lint.sh dashboard/src` 결과 디자인 토큰 위반 0.
- 관찰됨: `R02_BASE_URL=http://localhost:3456`, invalid non-secret test token, `R02_LIVE_PUBLISH=0`으로 실 스크립트를 실행했다. 샌드박스의 Chrome DevTools localhost 접속 차단으로 `Chrome DevTools did not start: fetch failed`에서 종료됐다.
- 미검증: 실 DB 기준 `overview.source=db`, Home, Studio, drafts, `consoleErrors=[]`, exit 0. 현재 워커는 Docker socket과 localhost TCP 접속이 모두 `operation not permitted`로 차단됐다.
- 변경하지 않음: `dashboard/src` 제품 코드, live SNS publish, DB 스키마, API 계약.

### 다음 QA 실행점

localhost와 Docker 접근이 가능한 coordinator에서 `osmu-pg`와 dashboard를 기동하고 operator Bearer로 tenant token을 발급한다. `R02_BASE_URL=http://localhost:<port> R02_TENANT_TOKEN=<발급값> R02_LIVE_PUBLISH=0 node scripts/verify-r02-e2e.mjs`를 실행한다. 종료증거는 exit 0, `overviewSource=db`, `studioDraftLoad=true`, `publishButtonText=🚀 Publish (N)`, `livePublish=false`, `consoleErrors=[]`다.

🏷 STAMP | line: osmu | 생성: 2026-08-13 06:15 KST | model: gpt-5.6 | agent: code-builder

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. 저장소 code-builder 규율과 `dev.md`를 적용.

SOURCES: `docs/fdd/test-plan-r02-v1.0.0-opus.md` · `dashboard/src/app/studio/page.tsx` · Next.js `allowedDevOrigins` 공식문서 https://nextjs.org/docs/app/api-reference/config/next-config-js/allowedDevOrigins · Playwright auto-waiting 공식문서 https://playwright.dev/docs/actionability

MODEL: gpt-5.6 / code-builder

## 2026-08-13 R-02 E2E 재실행 감사

### 결론

- 소스 변경 없음. 커밋 `3fd63016` 이후 FDD의 F1부터 F4와 파일스토어 migrate 단계는 현재 코드와 계약 테스트에 반영돼 있었다.
- Contract 단계는 cron과 외부 extension의 DB 쓰기 전환 확인 후 별도 승인하도록 FDD가 규정하므로 이번 범위에서 삭제나 추가 변경을 하지 않았다.
- 테스트됨: `npx tsc --noEmit` exit 0.
- 테스트됨: 전체 Vitest 136 files, 1,084 passed, 11 skipped, 실패 0. 실 DB 의존 11건은 환경 제한으로 skip.
- 테스트됨: `design-lint.sh dashboard/src` 결과 디자인 토큰 위반 0.
- 관찰됨: `docker start osmu-pg`는 Docker socket 접근 `operation not permitted`로 실패했다.
- 관찰됨: `.env.local`의 `DATABASE_URL`을 사용한 `pg_isready`와 `psql`은 `127.0.0.1:55432`에서 각각 exit 2, `Operation not permitted`로 실패했다.
- 관찰됨: `OSMU_AUTH_OPTIONAL=1 PORT=3461 npm run dev`는 `listen EPERM 0.0.0.0:3461`로 종료됐다. 제한시간 cleanup으로 잔류 서버는 남기지 않았다.
- 미검증: 운영자 Bearer로 tenant token 발급, 실 생성, 편집, 발행, permalink, `published_posts` 증가, 브라우저 console error 0. 서버와 DB가 뜨지 않아 제품 여정에는 진입하지 못했다.

### 다음 QA 실행점

후속 2026-08-13 06:15 KST 지시로 대체됐다. Docker와 localhost가 허용된 coordinator에서 `osmu-pg`와 dashboard 3461을 기동한다. 운영자 Bearer로 `/api/tenant-tokens`에 요청해 tenant token을 발급한 뒤 `R02_BASE_URL=http://localhost:3461 R02_TENANT_TOKEN=<발급값> R02_LIVE_PUBLISH=0 npm run e2e:r02`를 실행한다. 종료증거는 `overview.source=db`, Studio draft load, Publish 버튼 노출, `consoleErrors=[]`다. 실제 SNS 발행은 금지한다.

🏷 STAMP | line: osmu | 생성: 2026-08-13 05:50 KST | model: gpt-5 Codex | agent: code-builder

SKILLS_SKIPPED: 매칭되는 설치 코드 스킬 없음. `dev.md`, `benchmarks.md`, 저장소 code-builder 규율 적용.

SOURCES: `docs/fdd/fdd-r02-journey-fix-v1.0.0-opus.md` · `docs/fdd/migration-filestore-to-db-v1.0.0-opus.md` · `docs/fdd/test-plan-r02-v1.0.0-opus.md` · `dashboard/src` · Next.js Route Handlers https://nextjs.org/docs/app/getting-started/route-handlers · PostgreSQL INSERT https://www.postgresql.org/docs/current/sql-insert.html

MODEL: gpt-5 Codex / code-builder

## 2026-08-13 R-01~R-14 정적·단위·통합 교차감사

### 구현 및 배포 상태

- 제품 소스 변경 0, 테스트 소스 변경 0. `pipeline-state.osmu.md`가 build pending이고 승인된 prototype 핀이 없어 UI와 API 계약을 임의 수정하지 않았다.
- 코디네이터 제공 정보상 현재 브랜치는 prod 배포됨. 이번 worker는 prod 화면과 외부 제공자를 직접 관찰하지 못했으므로 라이브 동작은 미검증이다.
- 상세 대조표: `docs/qa/osmu-r01-r14-crosscheck-2026-08-13-v1-gpt-codex.md`.

### 확인된 미해결

- R-05: connect readiness가 `{available, reason}`만 반환해 고객 미연결과 Admin 앱 미준비를 안정적인 상태값으로 분리하지 못한다. 고객 UI의 `오픈 준비 중`도 연결되지 않았다.
- R-09: Settings의 텍스트 채널 8개, Sidebar의 영상 채널 2개, Studio 실발행 4개·미리보기 7개가 서로 다른 capability 목록을 쓴다. generic 채널과 Instagram 전용 화면의 탭 계약도 다르다.
- 파이프라인: current stage는 qa지만 plan만 승인됐고 design은 진행 중, build는 pending이다. PRD v7.3.5 승인 경로도 실제 `docs/notes/` 위치와 다르다.

### 검증 상태

- 테스트됨: `npx tsc --noEmit` exit 0.
- 테스트됨: R-02·OAuth·Admin·publish·인증·RLS 집중 Vitest 41 files, 453 passed, 7 skipped, 실패 0.
- 테스트됨: 전체 Vitest 136 files, 1,084 passed, 11 skipped, 실패 0.
- 테스트됨: `npm run build -- --webpack` exit 0, static pages 166/166.
- 테스트됨: `design-lint.sh dashboard/src` 위반 0.
- 미검증: 신규 회원가입, 실 OAuth callback, 실 SNS 발행과 permalink, 실 Postgres RLS·동시성, Admin 실화면, 1440·1024·390 브라우저 대조.

🏷 STAMP | line: osmu | 생성: 2026-08-13 19:20 KST | model: gpt-codex/gpt-5 | agent: code-builder

SKILLS_SKIPPED: 없음. 현재 제공된 스킬 중 code-builder 구현·감사에 매칭되는 스킬이 없다.

SOURCES: `docs/requests/2026-08-08_2026-08-10-chairman-requests.md` · FDD v1.0.0 3종 · PRD v7.3.5 · `dashboard/src` · `docs/qa/osmu-r01-r14-crosscheck-2026-08-13-v1-gpt-codex.md`

MODEL: gpt-codex/gpt-5 / code-builder

## 2026-08-14 OSMU OAuth 장기 토큰 내구성 회귀 수정

### 기존 구현 확인

- 존재: OAuth callback, Meta 단기 토큰 교환, `channel_accounts` 암호화 저장, Threads·Instagram `/me` 라이브 검증, YouTube `refresh_enc` 갱신 경로.
- 결함: Meta 장기 교환 실패 시 단기 토큰으로 내려갔고, `expires_in`을 버렸으며, 신원 검증 실패를 fallback ID로 덮을 수 있었다.
- 별건 이력: `git log -S` 및 `git log -G`로 `ChannelPage.tsx`의 `즉시 발행/예약` UI 이력을 조사했으나 해당 UI가 과거 채널 상세에 있었다가 삭제된 증거는 찾지 못했다. 과거 안내도 Queue 승인 후 자동발행이었고, 현재 실행 경로는 Studio, Queue, schedule API다. 이 별건은 수정하지 않았다.

### 변경 상태

| 영역 | 변경 | 검증 | 배포 |
|---|---|---|---|
| Meta 토큰 | Instagram·Threads 장기 교환에 `access_token` 또는 `expires_in`이 없으면 fail-closed. Facebook도 장기 user token 실패 시 page token 조회 금지 | 장기 교환 실패 시 미저장 테스트 통과 | 미배포 |
| 만료 저장 | 표준 OAuth·Meta·Facebook `expires_in`을 callback 기준 절대시각으로 변환해 `token_expires_at`에 저장 | callback 전달과 upsert 저장 테스트 통과 | 미배포 |
| 활성화 가드 | Threads·Instagram·X·YouTube `/me` 실패를 fallback ID로 덮지 않고 callback 저장 전 중단 | 지원 provider 4종 실패 경계 테스트 통과 | 미배포 |
| 연결 판정 | Meta 계정은 만료시각 없음, 만료, 잘못된 시각을 `reconnect`로 판정. 만료 access token+암호화 refresh token은 갱신 가능 연결로 유지 | readiness·channel-config 통합 테스트 통과 | 미배포 |
| YouTube refresh | 새 access token 저장 시 `expires_in`을 `token_expires_at`에 함께 갱신 | 선택 계정 암호화 refresh 테스트 통과 | 미배포 |

### 검증과 남은 경계

- 테스트됨: `npx tsc --noEmit` exit 0.
- 테스트됨: 전체 `npm test` exit 0. 138 files, 1,121 passed, 11 skipped, 실패 0.
- 테스트됨: OAuth·readiness·Threads publish 집중 9 files, 147 passed.
- 테스트됨: `design-lint.sh dashboard/src` 디자인 토큰 위반 0. UI 소스 변경은 없다.
- 미검증: 샌드박스 제약으로 실 OAuth, 운영 DB 새 행, Channel Info, Studio 실발행, permalink는 관찰하지 못했다.
- 미구현: Threads·Instagram long-lived token의 provider refresh endpoint 자동 호출. 현재는 만료 전 갱신 없이 만료 후 `reconnect`로 닫는다.
- 중간 커밋: 작업트리에 회장의 기존 미커밋 디자인 변경이 함께 있고, 현 샌드박스가 `.git` 쓰기를 허용하지 않아 이 build 변경만 분리 커밋하지 못했다.

🏷 STAMP | line: osmu | 생성: 2026-08-14 04:15 KST | model: gpt-codex/gpt-5 | agent: code-builder

SKILLS_SKIPPED: 없음. 현재 제공된 스킬 중 OAuth 인증 코드 구현에 매칭되는 코딩 스킬이 없다.

SOURCES: `docs/requests/2026-08-08_2026-08-10-chairman-requests.md` R-02-b/c/f · `docs/design-docs/channel-capability-and-readiness-contract-v1-opus.md` · `pipeline-state.osmu.md` override · `dashboard/src` · Meta Threads API https://www.postman.com/meta/threads/documentation/dht3nzz/threads-api · Google OAuth https://developers.google.com/identity/protocols/oauth2/web-server

MODEL: gpt-codex/gpt-5 / code-builder

## 2026-08-13 R-05 심사상태 UX, R-09 채널 capability 통일

### 구현 상태

- R-09: `dashboard/src/lib/channel-capabilities.ts`를 채널 그룹과 상세 탭의 단일 소스로 신설했다. Sidebar, Settings, Studio, ChannelPage, InstagramPage, MessagingPage를 같은 계약에 연결했다.
- R-09: 구조적으로 불가능한 탭만 제거하고 가능한 미구현 탭은 비활성 `연동 예정`으로 유지했다. 비활성 탭 클릭 시 `연동 예정입니다`를 안내한다.
- 보존: Threads Growth·Popular 실동작, Instagram Editor, 기존 8개 발행 채널, Studio 미리보기 7개와 실제 발행 4개 경계를 유지했다.
- R-05: `/api/connect/readiness`를 `connected`, `not_connected`, `opening_soon`, `publish_pending`, `error` enum으로 확장했다. 중앙 앱 credential, tenant 계정 연결, 외부 심사 요구 여부를 판정 소스로 사용한다.
- R-05: SocialConnectButton이 `미연결` 활성 연결과 `오픈 준비중` 회색 대기, `발행 준비중`, 재시도 오류를 구분한다. 기존 `{available}` 응답 fallback도 유지했다.
- 배포: 미배포. build override 범위에서 코드와 개발 소유 테스트만 반영했다.

### 검증 상태

- 커밋: `e8e87325` (`feat(dashboard): unify channel capability tabs`), `b7d7323e` (`feat(dashboard): expose channel readiness states`), `9a7d10d3` (`test(dashboard): align sidebar contract with channel SSOT`).
- 테스트됨: 전체 Vitest 138 files, 1,104 passed, 11 skipped, 실패 0. 실 DB 의존 테스트는 환경 조건에 따라 skip됐다.
- 테스트됨: `npx tsc --noEmit` exit 0. 빌드와 동시 실행해 `.next/types`가 교체된 첫 시도는 TS6053 경합으로 실패했고, 빌드 종료 후 단독 재실행은 exit 0이었다.
- 테스트됨: `npm run build -- --webpack` exit 0, static pages 166/166.
- 테스트됨: `design-lint.sh dashboard/src` 결과 디자인 토큰 위반 0.
- 미검증: 실 OAuth popup과 callback, 중앙 앱 credential 실설정, tenant 계정 실연결, 외부 플랫폼 심사상태, 실 Postgres 판정, 1440·1024·390 브라우저 렌더와 console error 0. 현재 sandbox는 Docker와 localhost를 허용하지 않는다.
- 기존 작업트리 주의: `git diff --check`는 이번 변경 밖 `session-state.osmu.md`의 기존 trailing whitespace 5건 때문에 exit 2다. 이번 구현 파일의 공백 결함으로 판정하지 않았고 해당 파일은 수정하지 않았다.

### 다음 QA 실행점

localhost와 Docker가 가능한 QA 환경에서 dashboard와 Postgres를 기동한다. credential 없음, credential 있음·tenant 미연결, 연결됨·심사 필요, 연결됨·심사 완료, DB 오류의 5상태로 `/api/connect/readiness` 응답과 Settings 버튼을 대조한다. Sidebar·Settings·Studio의 채널 목록, Threads Growth·Popular, Instagram Editor, Messaging Settings, 비활성 탭 안내를 1440·1024·390에서 클릭하고 console error 0을 확인한다.

🏷 STAMP | line: osmu | 생성: 2026-08-13 20:52 KST | model: gpt-codex/gpt-5 | agent: code-builder

SKILLS_SKIPPED: 없음. 현재 제공된 스킬 중 code-builder 구현에 매칭되는 스킬이 없다.

SOURCES: `docs/design-docs/channel-capability-and-readiness-contract-v1-opus.md` · `docs/qa/osmu-r01-r14-crosscheck-2026-08-13-v1-gpt-codex.md` · `docs/requests/2026-08-08_2026-08-10-chairman-requests.md` · `DESIGN.md` · W3C WAI-ARIA https://www.w3.org/TR/wai-aria/ · WAI-ARIA keyboard interface https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/

MODEL: gpt-codex/gpt-5 / code-builder
