{
  "id": "threads-queue",
  "name": "Threads Queue",
  "description": "Manage content queue for scheduled Threads publishing.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "queuePath": {
        "type": "string",
        "description": "Path to queue.json file. Defaults to data/queue.json relative to project root."
      }
    }
  }
}
