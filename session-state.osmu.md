## 2026-08-30 00:40 KST Claude 세션 (osmu 라인) 생성실·편집실 조 회수

핸드오프 기준: 이 파일(session-state.osmu.md).

★★ 격리 구멍 발견. 컨트롤러가 직접 재현했다.
  tests/isolation/tenant-api-attack-script.contract.test.ts TENANT-READ-01 실패.
  "/api/performance/learned-rules 읽기 경로가 실앱 공격 목록에서 빠졌습니다."
  원인: 어젯밤 커밋 822fa94a(성과실 조)가 새 읽기 API 를 만들며 교차 작업 공간
  공격 목록에 등록하지 않았다. 계약 테스트가 제 일을 한 것이다.
  ★한 워커가 "착수 전부터 실패하던 것"이라 보고했으나 틀렸다. 같은 밤 다른 조가 만든 것이다.
  ★내 앞선 보고 "1,474 전부 통과"도 정정한다. 그 시점엔 사실이었고
   822fa94a 이후에 깨졌다.
  ⇒ sonnet 조 발주(a7117e87). 목록 등록만 하지 말고 실제로 뚫리는지 먼저 확인하고,
    같은 밤 새로 생긴 다른 API 도 훑고, 파일 기반 저장이 작업 공간별로 갈리는지
    판정하라고 지시했다.

회수 완료: 생성실·편집실 구현 (opus, 커밋 f9feb84e 외)
- verify-agent-quality FAIL(Skill/WebSearch 0회). 코드 구현 판이라 벤치마크 축이
  적용되지 않는 경우로 판단하고 경고 라벨로 출고.
- 9건 중 8건 구현. "생성 큐" 문구는 실측하니 성과실 파일이라 손대지 않음(옳은 판단).
- 워커 실관측 증거:
  · 헤더 학습 정보: 생성실·편집실·발행실 머리줄에 "학습 정보 1 / 8" + 진행 막대.
  · 세 걸음 카드 문답: 입력칸 실측 0개. 업종 12장·대상 6장·말투 6장.
    걸음마다 "잘 모르겠습니다. 골라 주십시오".
  · 생성실 자유 입력칸 실측 0개. 목적을 고르면 주제 카드가 바뀐다.
  · "오늘 만들 수 있는 것" 사라짐. "영상을 이런 결로 만들어 드립니다" 로 대체.
  · 만들 종류 중복: 헤더 실측 "지금 만드는 것: 영상 같이 카드뉴스".
  · 편집실 미리보기: 자리표시자 제거. 플랫폼 5종 규격 전환(9:16 → 4:5,
    1080x1920 → 1080x1350). 플랫폼 UI 가 덮는 자리 점선 표시.
  · 임시저장: "여기까지를 판으로 고정" + 되돌리기 + 원본 내려받기.
  · 새로 시작: "만들던 것 39건이 그대로 있습니다". 아무것도 안 지운다.
  · "큐" 문구 생성실·편집실 실측 0건.
  · 대화창 전용 기능 5개.
- 증거: tsc 0, 새 계약 테스트 18건, 기존 9건을 새 계약에 맞춰 수정,
  브라우저 콘솔 오류 0, ui-token-audit 위반 0.
  캡처 /tmp/chair-create.png, chair-create-osmu.png, chair-learning.png,
  chair-edit.png, chair-edit-actions.png. 스크립트 /tmp/capture-chair.mjs.

★ 워커가 스스로 경고한 것 셋 (회장이 또 불만 낼 자리):
1. 편집실 미리보기는 여전히 실제 영상이 아니다. 장면·자막·규격 배치만 정확히 그린다.
   화면에 명시했지만 회장이 "미리보기 = 재생"으로 받으시면 같은 말씀이 또 나온다.
2. ★성과실 머리줄에만 학습 정보가 없다. 다른 세 방에는 붙였다.
   성과실 조가 같은 자리에 붙여야 네 방이 맞는다. 아직 안 됨.
3. "같이 만들기"는 화면과 상태까지다. 실제 파생 생성 로직은 lib/studio/generation 에 있고
   충돌 회피로 안 만졌다. 아직 안 됨.

★ 워커 판단 요청 3건:
1. qa-tracker 커밋이 훅에 막혀 스테이지에만 있다. 다른 조 미커밋 파일 때문.
   그 조가 커밋하면 풀린다. 남의 미완성 파일을 제 커밋에 넣지 않은 것은 옳다.
2. 업종·대상·말투 카드 문안은 워커 제안이다. 구조질문 문서도 unsourced 로 표시했다.
   회장이 실제 고객군 보고 갈아 끼울 자리.
3. src/components/shared/BrandSetupWizard.tsx 가 참조 0건이 됐다. 정리 여부 미정.

아직 도는 조:
- 안 터진 글 정리 실행 (sonnet, a7661e0d)
- 격리 구멍 (sonnet, a7117e87)

★ 아직 아무도 안 맡은 것:
- 발행실 대화창(회장 "왜 여긴 챗봇 없어?"). ★생성실 조가 끝났으니 이제 발주 가능.
  ★네 방 실렌더에서 발행실 단추가 5개뿐이다(다른 방 24~26). 함께 볼 것.
- 성과실 머리줄 학습 정보(위 경고 2번)
- "같이 만들기" 실제 파생 생성(위 경고 3번)
- X 채널 기본계정 토글 실렌더 확인
- 390 폭과 다크 모드
- 브라우저 콘솔 403 두 건 출처
- 실계정 로그인 완주

배포: PR #36 회장 머지 대기. 순서는 docs/releases/2026-08-29-배포-교착-해소-순서.md.
상품명: 회장 결정 대기. 추천 "OSMU 팩토리".

다음 액션:
1. 발행실 대화창 발주(이제 충돌 없음).
2. 두 조 회수 → verify.
3. 30건 대조표 재실행. ★프롬프트에 '~/.claude/standards/doc-review.md Read 필수' 명시.
4. 390 폭·다크·X 토글·403 점검 발주.

