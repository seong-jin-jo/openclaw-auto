## 2026-08-30 02:05 KST Claude 세션 (osmu 라인) 발행실 회수, 배포 준비 완료

핸드오프 기준: 이 파일(session-state.osmu.md).

★★ 회장 지시 갱신: "QA 꼼꼼히 안해도되니까 일단 나 컨텐츠생성 발행 하게좀"
   ⇒ 완결주의 중단. 회장이 실제로 쓰는 것이 최우선.

★ 전체 테스트 204파일 1,528건 전부 통과, 실패 0. 컨트롤러 직접 실행.
★ origin 에 push 완료. PR #36 이 81커밋 담고 있다. 회장 머지 대기.

회수 완료: 발행실 (opus, 커밋 20cd084f 3ff93758 7e5a3cda bcea79cb)
- 단추 5개에서 20개로. 네 방 실측 create 24 / edit 34 / publish 20 / performance 26.
- ★"발행실 5개"의 절반은 프로브가 틀린 것이었다. 프로브가 작업물을 옛 공용 키
  studio_work 에 심는데 발행실은 작업 공간별 키만 읽고 공용 키는 화면 뜨는 즉시 지운다
  (app/studio/page.tsx:307). 프로브 seed 키와 미리보기 선택자 둘 다 고쳤다.
- ★진짜 원인 셋을 밝혔다:
  1) 발행실 챗봇은 없던 게 아니라 안 보였다. 한 className 에 모바일 시트 규칙과
     데스크톱 기둥 규칙이 섞여 max-height:0px 로 계산됐다.
     실측 {w:320,h:2} → 고친 뒤 {w:320,h:705}.
  2) 예약이 캘린더에 영영 안 나오는 구조였다. 예약은 schedules 에 쌓이는데
     캘린더는 /api/queue 만 읽었다. 이것이 "예약 발행한건 어디서 확인해"의 실제 원인.
     캘린더가 /api/schedule 도 읽게 하고 예약 직후 /calendar?from=publish&date= 로 이었다.
  3) 계정 연결 링크가 /settings?tab=channels&channel=x 로 갔는데 설정 화면에
     channel 파라미터를 읽는 코드가 없다. /channels/<채널> 로 돌렸다.
- ★기본 계정 토글: /channels/{x,threads,facebook} 셋 다 열어 확인했더니 전부 0개다.
  Threads 전용이 아니라 연결된 계정이 0이라 셋 다 안 뜬 것이다.
  대조표가 코드만 보고 "부분"으로 둔 항목의 답.
- 새로 만든 것: 발행실 대화창 "여기서만 한 번에 되는 일" 5가지(해시태그 채널 규격대로
  나누기, 표시 이름 일곱 곳 통일, 한도 넘는 곳만 줄이기, 연결된 곳 전부 고르기, 전부 해제),
  자유 입력 12갈래 해석기, 전체 고르기, 발행 왕복 띠(DESIGN.md .tr59 · R193 규격에 있는데
  구현이 없던 것), 네 단추 설명 줄.
- 실동작 관찰: 한도 절단 before {threads:400,x:400} → after {threads:400,x:280}.
  "스레드 빼고 올려" → Threads 체크 해제. 예약 POST 200 → 그 날짜 캘린더에 표시.

★★ 콘솔 403 세 건 출처 확정됐다. 외부 자원이 아니라 우리 API 다.
  /api/performance/learned-rules, /api/cron-status, /api/threads/low-engagement-candidates
  셋 다 proxy.ts 의 TENANT_AWARE_PATHS 에 없어 운영자 전용으로 취급된다.
  ⇒ **고객으로 들어가면 성과실 새 화면 셋이 전부 비어 보이거나 오류가 난다.**
  ⇒ sonnet 조 발주(ae66d087). 여는 순간 격리 방어가 사라지므로 작업 공간 두 개로
    실제 공격해 보고 열라고 못 박았다. 성과실 머리줄 학습 정보도 같이 시켰다.

미검증(회장 로그인 필요):
- /api/publish 가 실제로 나가는 경로. 이 작업 공간에 연결된 채널이 0곳이다.
- 기본 계정 토글이 계정 2개 이상일 때 뜨는 모습.
- ★회장이 채널 하나만 연결하면 이 둘이 닫힌다.

아직 도는 조: 성과실 403 개방과 격리 재확인 (sonnet, ae66d087)

아직 안 맡은 것: "같이 만들기" 실제 파생 생성, 390 폭과 다크 모드, 30건 대조표 재실행.

배포: PR #36 회장 머지 대기(81커밋). 순서는
docs/releases/2026-08-29-배포-교착-해소-순서.md.
audit → expand-guard → Deploy → expand-member.
상품명: 회장 결정 대기. 추천 "OSMU 팩토리".

다음 액션:
1. 회장이 머지·배포하면 각 단계 gh 로 실측하고 배포 환경에서 네 방 직접 확인.
2. 403 조 회수 → 격리 테스트 재확인.
3. 회장이 채널 연결하면 실제 발행 경로 확인.
4. 30건 대조표 재실행.

