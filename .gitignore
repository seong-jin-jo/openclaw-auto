# OpenClaw 소스 (서버에서 별도 clone)
openclaw/

# 민감 파일 (서버에서 직접 생성)
.env
openclaw/.env

# 실제 설정 파일 (토큰 포함, 서버에서 example에서 복사)
config/openclaw.json
config/cron/jobs.json

# 데이터 (런타임)
data/
!data/.gitkeep

# 로그
*.log

# Python
__pycache__/
*.pyc

# OS
.DS_Store